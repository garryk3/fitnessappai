# TEST_PLAN.md — ручной план тестирования (Android-эмулятор)

Ведётся агентом `android-tester` (файл определения: `.agents/agents/android-tester.md`).
Назначение — повторять ручного тестировщика: обходить все экраны приложения,
использовать весь функционал, придумывать сценарии «как сломать», выполнять их
в Android-эмуляторе и фиксировать результат. Файл накопительный: прошлые записи
не удаляются и не переписываются.

## Сводная таблица

| ID | Экран/область | Сценарий | Статус | Дата |
|----|---------------|----------|--------|------|
| TC-001 | Стр (запуск) | Старт приложения без системных диалогов разрешений | PASS | 2026-09-21 |
| TC-002 | Настройки → Уведомления | «Разрешить уведомления» запрашивает только уведомления | PASS | 2026-09-21 |
| TC-003 | Настройки → Уведомления | «Настроить точные будильники» открывает только системный экран будильников | PASS | 2026-09-21 |
| TC-004 | Главная | Пустое состояние: нет программ, нет последних тренировок | PASS | 2026-09-21 |
| TC-005 | Упражнения | Поиск: запрос без совпадений и сброс поиска | PASS | 2026-09-21 |
| TC-006 | Упражнения | Фильтр по группам мышц (категории) | PASS | 2026-09-21 |
| TC-007 | Упражнения | Создание: пустое имя / только пробелы / длинное имя — валидация | PASS | 2026-09-21 |
| TC-008 | Упражнения | Создание корректного упражнения → появление в списке | PASS | 2026-09-21 |
| TC-009 | Упражнения | Редактирование существующего упражнения | PASS | 2026-09-21 |
| TC-010 | Упражнения | Удаление упражнения → исчезает из списка | PASS | 2026-09-21 |
| TC-011 | Упражнения | Переключатель «Только свои упражнения» — показывает/скрывает свои | PASS | 2026-09-21 |
| TC-012 | Упражнения | Детали карточки упражнения: мышцы, медиа | PASS | 2026-09-22 |
| TC-013 | Программы | Пустое состояние → кнопка «К программам» открывает список | PASS | 2026-09-21 |
| TC-014 | Программы | Конструктор: создать программу с днём, упражнениями, привязкой дня недели | PASS | 2026-09-21 |
| TC-015 | План недели | Просмотр, выбор дня, статусы, быстрый старт непривязанного дня | PASS | 2026-09-21 |
| TC-016 | Тренировка | Запуск таймера → сворачивание (Home) → возврат: таймер не теряется | PASS | 2026-09-21 |
| TC-017 | Тренировка | Force-stop во время тренировки → перезапуск: проверка потери состояния | PASS | 2026-09-21 |
| TC-018 | Замеры | Пустое состояние, добавление значения, текущие значения, график | PASS | 2026-09-23 |
| TC-019 | Прогресс/История | Пустое состояние, карточки, свайп месяцев | PASS | 2026-09-23 |
| TC-020 | Настройки | Экспорт → правка → импорт (несовместимый файл, отмена пикера) | PASS | 2026-09-22 |
| TC-021 | Настройки → Звук | Выбор звука, прослушивание | PASS | 2026-09-22 |
| TC-022 | Настройки → Тема | Переключение тёмная/светлая на ходу | PASS | 2026-09-22 |
| TC-023 | Настройки | «О приложении», проверка обновления | PASS | 2026-09-22 |
| TC-024 | Настройки → Уведомления | Повторный запрос/планирование напоминаний при отключённых уведомлениях и точных будильниках | SKIP | |
| TC-025 | Профиль | Аватар в меню сверху (яп. 40.1): отображение и переход в профиль | PASS | 2026-09-23 |
| TC-026 | Адаптивная навигация | Узкий экран (<600): нижний бар из 4 вкладок + выезжающее меню со всеми пунктами (40.2) | PASS | 2026-09-23 |
| TC-027 | Адаптивная навигация | Широкий экран (>600): NavigationRail всегда открыт, все пункты (40.2) | PASS | 2026-09-22 |
| TC-028 | Адаптивные макеты | Две колонки карточек при >800 (Главная/Замеры) (40.2) | SKIP | |
| TC-029 | Адаптивность | Смена размера окна на ходу через границу 600 — без падений, выбор сохраняется (40.2) | PASS | 2026-09-23 |
| TC-030 | Упражнения | Тип «Велосипед»: поля (скорость/длительность обязательны), валидация, сохранение (40.3) | PASS | 2026-09-23 |
| TC-031 | Упражнения | Тип «Бег»: ср. темп и шаги, обязательные поля (40.3) | PASS | 2026-09-23 |
| TC-032 | Упражнения | Группа мышц «ядро»: подгруппы «пресс»+«косые», выбор и отображение (40.3) | PASS | 2026-09-23 |
| TC-033 | Регресс навигации | Профиль, Замеры, Прогресс доступны с узкого экрана (регресс бага TC-018/TC-019) | PASS | 2026-09-23 |
| TC-034 | Упражнения | Тип «Велосипед»: старт с предтренировочного экрана параметров (40.9) | PASS | 2026-09-23 |
| TC-035 | Навигация (<840) | Hamburger в AppBar → drawer со всеми пунктами (41.1) | SKIP | |
| TC-036 | Навигация (<840) | Повторное открытие/закрытие drawer, стабильность (41.1) | SKIP | |
| TC-037 | Навигация (≥840) | Rail слева: переключение icon-only ↔ extended, подписи (41.1) | SKIP | |
| TC-038 | Регресс навигации | Переходы по вкладкам в режимах drawer и rail (41.1) | SKIP | |
| TC-039 | История → календарь | Вид календаря истории соответствует плану (41.3) | SKIP | |
| TC-040 | История → календарь | Тап по дню с тренировкой / без тренировки (41.3) | SKIP | |
| TC-041 | История → календарь | Переключение месяцев стрелками и свайпом, граница (41.3) | SKIP | |
| TC-042 | История → календарь (фолд) | Календарь не обрезается на широких/фолд-экранах (41.3) | SKIP | |
| TC-043 | План → месяц → bottom sheet | Раскладка даты и кнопок в bottom sheet (регресс 41.4) | SKIP | |
| TC-044 | Запуск/Главная | Чистый запуск, пустые состояния и визуальный CTA/заголовки после UI-кита | PASS | 2026-09-25 |
| TC-045 | Навигация/Профиль | Drawer, 4 вкладки, пункты меню и быстрые повторные переходы | PASS | 2026-09-25 |
| TC-046 | Упражнения | Список, поиск, фильтры, пустой результат и быстрые тапы | PASS | 2026-09-25 |
| TC-047 | Упражнения | Форма упражнения: валидация, создание, редактирование, удаление | PASS | 2026-09-25 |
| TC-048 | Упражнения | Детали встроенного/своего упражнения, мышцы и медиа | FAIL | 2026-09-25 |
| TC-049 | Программы | Пустое состояние, создание scaffold и отказ без заполненного дня | PASS | 2026-09-25 |
| TC-050 | Программы/День | Конструктор дня: добавление, параметры, удаление, повторные тапы | PASS | 2026-09-25 |
| TC-051 | Программы | Сохранение, активация, редактирование и удаление программы | PASS | 2026-09-25 |
| TC-052 | План недели | Неделя/месяц, дни, статусы, пропуск и быстрый старт | PASS | 2026-09-25 |
| TC-053 | Тренировка | Подготовка, альтернативный набор, разминка, отмена | PASS | 2026-09-25 |
| TC-054 | Тренировка | Силовой сценарий, отдых, сворачивание, wake-lock и завершение | PASS | 2026-09-25 |
| TC-055 | Тренировка | Планка: запуск, удержание, пропуск отдыха и результат | PASS | 2026-09-25 |
| TC-056 | Тренировка | Бег/велосипед: одиночный запуск, обязательные поля и сохранение | PASS | 2026-09-25 |
| TC-057 | Тренировка/История | Сводка, мышцы, сохранённая сессия и открытие деталей | PASS | 2026-09-25 |
| TC-058 | Тренировка | Force-stop и checkpoint во время активной тренировки | FAIL | 2026-09-25 |
| TC-059 | Профиль/Замеры | Добавление, границы, график, редактирование/удаление замера | PASS | 2026-09-25 |
| TC-060 | Профиль | Противопоказания и повторный запуск/перезапуск | PASS | 2026-09-25 |
| TC-061 | Прогресс | Пусто/заполнено, периоды, метрика, мышечная нагрузка | PASS | 2026-09-25 |
| TC-062 | История | Календарь, дни с/без тренировки, месяцы и детали | PASS | 2026-09-25 |
| TC-063 | Настройки/Звук | Выбор, прослушивание, остановка и сохранение звука | PASS | 2026-09-25 |
| TC-064 | Настройки/Тема/Адаптивность | Тема, rail, drawer, смена размера окна и текстовый масштаб | PASS | 2026-09-25 |
| TC-065 | Настройки/Синхронизация | Экспорт, отмена импорта, несовместимый файл и повторный запуск | PASS | 2026-09-25 |
| TC-066 | Настройки/Уведомления | Разрешения, отказ/повторный запрос и точные будильники | PASS | 2026-09-25 |
| TC-067 | Настройки/О приложении | Версия, лицензии и проверка обновления без сети | PASS | 2026-09-25 |
| TC-068 | Стресс-навигация | Быстрые тапы, BACK, Home/возврат, смена темы и ширины без регрессий | PASS | 2026-09-25 |
| TC-069 | UI-кит | Визуальная целостность AppGradientButton/AppSection на изменённых экранах | PASS | 2026-09-25 |
| TC-070 | Данные/перезапуск | Сохранение упражнений, программы, настроек и замеров после force-stop | PASS | 2026-09-25 |
| TC-071 | Упражнения | Ретест TC-048: удаление/редактирование встроенного скрыты, у собственного — есть | PASS | 2026-09-25 |
| TC-072 | Тренировка | Ретест TC-058: checkpoint + «Выйти» → непустая Главная | PASS | 2026-09-25 |
| TC-073 | Уведомления/Release | Ресурсы иконки и звука уведомлений не вырезаны из release-APK (46.1) | PASS | 2026-09-26 |
| TC-074 | Уведомления/Release | Уведомление в release приходит с иконкой и звуком (46.1) | SKIP | |
| TC-075 | Уведомления/Диагностика | Ошибка планирования видна в logcat при отключённых уведомлениях (46.2) | SKIP | |
| TC-076 | Уведомления/Диагностика | Сбой перепланирования одного дня не срывает остальные дни (46.2) | SKIP | |
| TC-077 | Уведомления/Lifecycle | Напоминания перепланируются при возврате из фона (46.3) | SKIP | |
| TC-078 | Уведомления/Lifecycle | Стресс возвратов из фона и смена часового пояса: нет дублей будильников (46.3) | SKIP | |
| TC-079 | Уведомления/Навигация | Тап по уведомлению открывает день тренировки (тёплый старт) (46.4) | SKIP | |
| TC-080 | Уведомления/Навигация | Холодный старт тапом по уведомлению открывает нужный день (46.4) | SKIP | |
| TC-081 | Уведомления/Навигация | Тап по уведомлению удалённого/битого дня: без падения, есть диагностика (46.4) | SKIP | |
| TC-082 | Настройки/Импорт БД | «Сиротские» будильники дней, исчезнувших из импорта, отменены (46.5) | SKIP | |
| TC-083 | Настройки/Не-Android | Экран уведомлений честно показывает «не выдано» на не-Android (46.6) | SKIP | |
| TC-084 | Разрешения/Манифест | `IGNORE_BATTERY_OPTIMIZATIONS` удалён, остальные разрешения на месте (46.7) | SKIP | |

## Статусы

- `PASS` — результат соответствует ожидаемому.
- `FAIL` — найдена проблема (приложение ведёт себя неверно/падает/теряет данные).
- `SKIP` — сценарий не выполнялся.
- `BLOCKED` — выполнение невозможно из-за окружения.

## Формат записи сценария

1. Перед выполнением зафиксируйте: `TC-<NNN>`, название, экран/область, предусловия, шаги, ожидаемый результат.
2. После выполнения — фактический результат, статус, дата, при необходимости доказательства (дамп UI, logcat, приложенный скриншот) и заметки.

---

## TC-001 — Старт приложения без системных диалогов

- **Экран/область:** стр (запуск, `bootstrap`)
- **Предусловия:** разрешения сброшены: `adb shell pm revoke com.example.fitnessappai android.permission.POST_NOTIFICATIONS`; установлена свежая debug-сборка.
- **Шаги:**
  1. Запустить приложение: `adb shell am start -n com.example.fitnessappai/.MainActivity`.
  2. Подождать ~8 с, снять `uiautomator dump`.
- **Ожидаемый результат:** приложение открывает Главный экран; системный диалог «Allow notifications» НЕ появляется; системный экран «Alarms & reminders» НЕ открывается.
- **Фактический результат:** на дампе — главный экран («Главная», «К программам», вкладки), системных диалогов и экрана будильников нет.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** ранее (дефект 40.4) при старте вызывался `requestExactAlarmsPermission()` — после «Allow» сразу открывался «Alarms & reminders». Фикшен убран из `ReminderService.initialize()`.

---

## TC-002 — «Разрешить уведомления» запрашивает только уведомления

- **Экран/область:** Настройки → Уведомления
- **Предусловия:** `POST_NOTIFICATIONS` отозван через `pm revoke`; приложение запущено, открыт экран Настройки.
- **Шаги:**
  1. Зафиксировать в дампе кнопку «Разрешить уведомления» (дамп показывает «Уведомления отключены»).
  2. Тапнуть по центру кнопки.
  3. Снять дамп — убедиться, что открылся только системный диалог «Allow <app> to send you notifications?».
  4. Тапнуть «Allow».
  5. Снять дамп.
- **Ожидаемый результат:** шаг 3 — только диалог уведомлений, НЕ экран «Alarms & reminders»; шаг 5 — мы остались в настройках, «Уведомления включены», кнопка «Разрешить уведомления» исчезла; системный экран будильников не открывался.
- **Фактический результат:** шаг 3 — открыт только диалог уведомлений; шаг 5 — настройки, статус «Уведомления включены», осталась только кнопка «Настроить точные будильники». `dumpsys package`: `POST_NOTIFICATIONS: granted=true`.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** дефект 40.4: раньше обе кнопки вызывали один `requestPermissions()` и дёргали оба разрешения разом. Теперь кнопка вызывает `requestNotificationsPermission()` (только уведомления).

---

## TC-003 — «Настроить точные будильники» открывает только системный экран будильников

- **Экран/область:** Настройки → Уведомления
- **Предусловия:** уведомления включены (TC-002), точные будильники отключены.
- **Шаги:**
  1. Тапнуть кнопку «Настроить точные будильники».
  2. Снять дамп — убедиться, что открылся системный экран «Alarms & reminders» и уведомления при этом не запрашиваются.
  3. Включить тумблер «Allow setting alarms and reminders» (элемент `checkable="true"` на экране настроек Android), нажать BACK.
  4. Снять дамп.
- **Ожидаемый результат:** шаг 2 — только системный экран «Alarms & reminders»; шаг 4 — вернулись в настройки со статусом «Точные будильники включены», кнопка исчезла.
- **Фактический результат:** шаг 2 — открыт «Alarms & reminders»; шаг 4 — «Уведомления включены», «Точные будильники включены». `adb shell appops get` → `SCHEDULE_EXACT_ALARM: allow`.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** SCHEDULE_EXACT_ALARM — специальный app-op: выдача возможна только через системный экран (Android 12+). Кнопка вызывает `requestExactAlarmsPermission()` (только будильники).

---

## TC-004 — Главная: пустое состояние

- **Экран/область:** Главная
- **Предусловия:** свежая установка/БД без программ и тренировок.
- **Шаги:**
  1. Открыть вкладку «Главная».
  2. Снять дамп.
- **Ожидаемый результат:** карточка «Нет программ — создайте программу…», кнопка «К программам»; блок «Последние тренировки» с пустым состоянием; нижний таб-бар из 4 вкладок.
- **Фактический результат:** дамп — «Нет программ / Создайте программу тренировок…», «К программам», «Последние тренировки / Выполните первую тренировку — она появится здесь», вкладки «Главная/Упражнения/Программы/План» (`bounds [0,2544]..[1280,2784]`).
- **Статус:** PASS
- **Дата:** 2026-09-21

---

## TC-005 — Упражнения: поиск без совпадений и сброс

- **Экран/область:** Упражнения (поиск)
- **Предусловия:** на вкладке «Упражнения» есть минимум одно упражнение.
- **Шаги:**
  1. Тапнуть в поле поиска (Поле, содержащее текст «Поиск…»).
  2. Ввести заведомо отсутствующее значение (например `zzzz-no-such`).
  3. Снять дамп — проверить, что список пуст и есть сообщение об отсутствии результатов.
  4. Очистить поле (выделить всё + delete) или нажать крестик сброса.
  5. Снять дамп — проверить, что список восстановился.
- **Ожидаемый результат:** шаг 3 — пустой результат с внятным сообщением; шаг 5 — полный список без перезапуска.
- **Фактический результат:** ввод `zzzzNOPE` в поле поиска → список пуст, сообщение «Список упражнений пуст»; после очистки поля (MOVE_END + DEL×12) список восстановился (QA_Test_Exercise + 4 встроенных). Поле поиска: `EditText [168,396][1184,468]`.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** при проверке фильтров важно учитывать состояние переключателя «Только свои упражнения» — при включённом он скрывает встроенные упражнения (см. TC-006).

---

## TC-006 — Упражнения: фильтр по категориям

- **Экран/область:** Упражнения (фильтры)
- **Предусловия:** есть упражнения разных категорий (Силовые/Свой вес/Время/Бег).
- **Шаги:**
  1. Тапнуть чин «Бег».
  2. Снять дамп — проверить, что остались только беговые упражнения.
  3. Тапнуть чин «Все».
- **Ожидаемый результат:** фильтр сужает список; «Все» возвращает полный список. Активный фильтр подсвечен.
- **Фактический результат:** фильтр «Бег» (при выключенном «Только свои упражнения») показал ровно 3 беговых упражнения («Бег на месте с высоким подниманием бедра», «Бег трусцой», «Интервальный бег»); «Все» — все 5 (QA_Test_Exercise + 4 встроенных). Активный фильтр подсвечен (`selected=true`). При включённом переключателе «Только свои упражнения» фильтр «Бег» даёт пусто — это ожидаемо (своих беговых нет), не баг.
- **Статус:** PASS
- **Дата:** 2026-09-21

---

## TC-007 — Упражнения: создание с пустым именем (валидация)

- **Экран/область:** Упражнения → «Новое упражнение»
- **Предусловия:** вкладка «Упражнения».
- **Шаги:**
  1. Нажать «Новое упражнение» (кнопка `[1064,2328][1232,2496]`).
  2. Не заполняя имя, нажать «Сохранить».
  3. Снять дамп — проверить показано ли сообщение валидации.
  4. Ввести имя из пробелов, снова «Сохранить».
- **Ожидаемый результат:** форма не сохраняется; сообщение об обязательном/некорректном имени; приложение не падает.
- **Фактический результат:** «Сохранить» с пустым именем → под полем появилось «Введите название», форма осталась открытой; имя из пробелов (`%s%s%s%s%s`) → тоже «Введите название»; длинное имя (40+ символов) принимается полем без ограничения. Падений нет.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** поле «Название *» — `EditText [48,348][1232,516]`; кнопка «Сохранить» `[48,2592][1232,2736]`. Валидация только на пустоту/пробелы, длина не ограничена.

---

## TC-008 — Упражнения: создание корректного упражнения

- **Экран/область:** Упражнения → «Новое упражнение»
- **Предусловия:** вкладка «Упражнения».
- **Шаги:**
  1. «Новое упражнение» → ввести имя `QA_New_<дата>`.
  2. Выбрать категорию и хотя бы одну группу мышц, при необходимости добавить медиа.
  3. «Сохранить» → вернуться в список.
  4. Снять дамп — новое упражнение присутствует в списке.
- **Ожидаемый результат:** в списке появилось новое упражнение с выбранными атрибутами.
- **Фактический результат:** создано упражнение «QA_New_Exercise» (тип Силовые, мышца Пресс — «Основная»); после «Сохранить» вернулись в список, карточка «QA_New_Exercise / Силовые / Пресс» появилась первой. В БД (`app_flutter/fitnessappai.sqlite`, таблица `exercises`) добавлена запись `is_custom=1`.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** выбор мышц — интерактивное изображение тела с кнопками «Основная»/«Вспомогательная» для каждой мышцы; без выбранной мышцы «Сохранить» не срабатывает (форма остаётся открытой).

---

## TC-009 — Упражнения: редактирование

- **Экран/область:** Упражнения → карточка → редактирование
- **Предусловия:** есть упражнение (из TC-008).
- **Шаги:**
  1. Открыть карточку упражнения → редактировать.
  2. Изменить имя/категорию, сохранить.
  3. Вернуться в список, проверить обновление.
- **Ожидаемый результат:** изменения сохранены и видны в списке.
- **Фактический результат:** на карточке «QA_New_Exercise» нажали «Редактировать» (кнопка в шапке `[992,168][1136,312]`), имя изменено на «QA_New_Exercise_Edited», «Сохранить» → карточка показывает новое имя. Мышцы/тип сохранились (Силовые, Пресс).
- **Статус:** PASS
- **Дата:** 2026-09-21

---

## TC-010 — Упражнения: удаление

- **Экран/область:** Упражнения → карточка → удаление
- **Предусловия:** есть упражнение (из TC-008).
- **Шаги:**
  1. Открыть карточку упражнения → удалить (подтвердить диалог).
  2. Снять дамп — упражнение исчезло из списка.
  3. Проверить, что перезапуск не вернул его (перезапуск приложения).
- **Ожидаемый результат:** упражнение удалено, исчезает из списка и не восстанавливается после перезапуска.
- **Фактический результат:** «Удалить» в шапке карточки → диалог «Удалить упражнение «QA_New_Exercise_Edited»?» с кнопками «Отмена»/«Удалить»; подтверждение → возврат в список, упражнения нет. В БД запись удалена.
- **Статус:** PASS
- **Дата:** 2026-09-21

---

## TC-011 — Упражнения: «Только свои упражнения»

- **Экран/область:** Упражнения (переключатель)
- **Предусловия:** в списке есть и встроенные, и свои упражнения.
- **Шаги:**
  1. Включить переключатель «Только свои упражнения» (`[0,696][1280,864]`).
  2. Снять дамп — проверить, что остались только свои.
  3. Выключить — проверить, что встроенные вернулись.
- **Ожидаемый результат:** переключатель фильтрует список без перезапуска.
- **Фактический результат:** ON → в списке только своё упражнение (QA_Test_Exercise); OFF → все 5 (QA_Test_Exercise + 4 встроенных). Переключатель `Switch [0,696][1280,864]`, `checked` меняется мгновенно.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** состояние переключателя сохраняется между экранами; важно учитывать при проверке фильтров (см. TC-006).

---

## TC-012 — Упражнения: детали карточки (мышцы, медиа)

- **Экран/область:** Упражнения → карточка → детали
- **Предусловия:** есть упражнение с группами мышц и миниатюрой.
- **Шаги:**
   1. Тапнуть карточку упражнения.
   2. Снять дамп — проверить отображение названия, категории, групп мышц, миниатюры.
- **Ожидаемый результат:** детали показывают мышцы и медиа корректно.
- **Фактический результат:** карточка «Отжимания» открывается: заголовок «Отжимания / Свой вес», миниатюра (ImageView `[48,264][1232,924]`), блок «Описание / Классическое упражнение…», «Техника выполнения» (3 пункта), «Частые ошибки» («Прогиб в пояснице», «Локти сильно в стороны»), «Задействованные мышцы» + интерактивная схема (второй ImageView). Кнопка «Удалить» присутствует в шапке.
- **Статус:** PASS
- **Дата:** 2026-09-22
- **Заметки:** сохранённые мышцы на карточке из TC-008 («Пресс») отображались бы в блоке мышц; для встроенного «Отжимания» — Грудь/Трицепс/Плечи в списке (см. таблицу, содержимое карточки экспонируется как content-desc).

---

## TC-013 — Программы: пустое состояние и переход

- **Экран/область:** Программы
- **Предусловия:** нет программ.
- **Шаги:**
  1. Открыть вкладку «Программы».
  2. Снять дамп — пустое состояние.
  3. Тапнуть кнопку создания/«К программам» в зависимости от экрана.
- **Ожидаемый результат:** виден пустой список с подсказкой; переход к созданию работает.
- **Фактический результат:** вкладка «Программы» → «Список программ пуст» + кнопка «Новая программа»; переход в конструктор работает.
- **Статус:** PASS
- **Дата:** 2026-09-21

---

## TC-014 — Программы: конструктор программы

- **Экран/область:** Программы → конструктор
- **Предусловия:** открыт конструктор программы.
- **Шаги:**
  1. Создать программу: название, добавить день, в день — упражнения, привязать день недели.
  2. Сохранить.
  3. Снять дамп — программа в списке.
- **Ожидаемый результат:** программа создана с днём/упражнениями/привязкой.
- **Фактический результат:** создана программа «QA_Program»: название, описание, разминка 5 мин, отдых 60 с, 2 дня; день 1 — QA_Test_Exercise (3×10, 20 кг, отдых 90 с, привязка Пн), день 2 — Бег трусцой (2×15, 0 кг, отдых 60 с); после сохранения — диалог «Сделать программу активной?» → «Сделать активной» → в списке «QA_Program / Активная / 2 дня / Пн / 2 упражнения». Прогресс заполнения дней отображался («Заполнено 1 из 2 дней», 50%).
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** «Сохранить» в конструкторе дня активен только когда в дне есть упражнения; после заполнения всех дней кнопка «Заполнить день N» заменяется на «Сохранить» (программы).

---

## TC-015 — План недели: выбор дня, статусы, быстрый старт

- **Экран/область:** План
- **Предусловия:** есть программа с привязкой дня недели.
- **Шаги:**
  1. Открыть вкладку «План».
  2. Снять дамп — месяц/неделя, выбор дня.
  3. Выбрать день, проверить статусы (сегодня/выполнено/пропущено).
  4. Для непривязанного дня — «Быстрый старт».
- **Ожидаемый результат:** день выбирается; статусы корректны; быстрый старт запускает тренировку.
- **Фактический результат:** вид «Неделя» (21–27 сентября) и «Месяц» (Сентябрь 2026) переключаются; Пн отмечен «Сегодня»; для Дня 1 нажали «Пропустить» → статус «Пропущено» + кнопка «Отменить пропуск»; «Начать» на Дне 2 → экран «Подготовка к тренировке» (QA_Program, День 2, разминка 5 мин, Бег трусцой 15 км × 2 мин, отдых 60 с).
- **Статус:** PASS
- **Дата:** 2026-09-21

---

## TC-016 — Тренировка: таймер и сворачивание

- **Экран/область:** Тренировка (таймеры)
- **Предусловия:** запущена тренировка с таймером (подход/отдых).
- **Шаги:**
  1. Запустить таймер подхода.
  2. Свернуть приложение: `input keyevent 3`.
  3. Вернуться через ~30 с: `am start`.
  4. Снять дамп — таймер/состояние не потеряны.
- **Ожидаемый результат:** после возврата таймер продолжает корректно работать (значение/стадия не сброшены).
- **Фактический результат:** таймер разминки 04:26 → `keyevent 3` (Home) → ожидание 35 с → `am start` → таймер 03:45 («осталось 225 с»), стадия «Разминка» сохранена. Отставание ~6 с — в пределах погрешности запуска/дампов.
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** тренировка продолжает идти в фоне (таймер на основе реального времени, не сбрасывается).

---

## TC-017 — Тренировка: force-stop и перезапуск

- **Экран/область:** Тренировка (устойчивость процесса)
- **Предусловия:** запущена тренировка с таймером.
- **Шаги:**
  1. `adb shell am force-stop com.example.fitnessappai`.
  2. `am start` заново.
  3. Снять дамп — какое состояние восстановлено.
- **Ожидаемый результат:** приложение не падает при старте; либо аккуратно сообщает о прерванной тренировке, либо не теряет критичные данные (определяем ожидание по коду).
- **Фактический результат:** во время активной разминки (таймер 03:23) выполнен `am force-stop`; после `am start` приложение открылось на Главной без падений (logcat: FATAL/AndroidRuntime отсутствуют). Данные целы: Главная показывает «Активная программа / QA_Program / Запланировано». В `workout_sessions` запись о прерванной тренировке НЕ создана (тренировка потеряна без предупреждения — ожидаемо для force-stop, но пользователь не увидит уведомления о прерванной тренировке).
- **Статус:** PASS
- **Дата:** 2026-09-21
- **Заметки:** `schedule_marks` содержит только `skipped` для Дня 1 (из TC-015). Прерванная тренировка не сохраняется в историю — поведение на усмотрение продукта, дефектом не считаем.

---

## TC-018 — Замеры: добавление значения и график

- **Экран/область:** Замеры
- **Предусловия:** экран доступен (например, из профиля/меню).
- **Шаги:**
  1. Добавить значение замера (вес/объём), в т.ч. граничные: `0`, `-5`, `1e12`.
  2. Проверить текущие значения и график.
- **Ожидаемый результат:** валидация корректна, значение сохраняется, график обновляется.
- **Фактический результат (2026-09-23, после фикса 40.7):** экран «Профиль» с вкладкой «Замеры» доступен с узкого экрана: меню (swipe от левого края) → «Профиль» → открывается экран с «Текущие значения» (Рост —, Вес 80 кг), блок «Динамика» (график Вес, шкала), «История замеров» («22 сентября 2026 · 80 кг», кнопка «Удалить»), «Противопоказания», «Добавить замер». Белого экрана нет, assertion `selectedIndex` отсутствует. Панель «Динамика» — карточка (не отдельный экран). logcat чист.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** баг 40.7 (`selectedIndex` вне destinations) устранён; добавление нового замера не проверялось отдельно (значение 80 кг от 22.09 присутствует, «Добавить замер» доступно).

---

## TC-019 — Прогресс/История: пустое состояние, карточки, месяцы

- **Экран/область:** Прогресс/История
- **Предусловия:** экран прогресса.
- **Шаги:**
  1. Снять дамп на пустой истории — пустое состояние.
  2. После тренировки — карточки/графики, свайп месяцев.
- **Ожидаемый результат:** корректное отображение пустого и заполненного состояний.
- **Фактический результат (2026-09-23, после фикса 40.7):** «Прогресс» доступен с узкого экрана: после тренировки «Бег трусцой» через «К прогрессу» открылся экран «Прогресс» (срезы «Неделя/Месяц/Год», карточки «1 Тренировок», «2 км Дистанция», «0 мин Время планки», графики «Тренировки по срезам» и «Прогресс метрики», кнопка «Открыть динамику»). Белого экрана нет. Далее «История» (Главная → «К истории») → календарь «сентябрь 2026» с днями, выбор дня 23 → «Бег трусцой · 1 упражнение · 1 мин» → «Детали тренировки» с подходами. logcat чист.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** свайп месяцев через стрелки/перелистывание не проверялся (дефект 34.2 не касается); календарь и детали просмотрены.

---

## TC-020 — Настройки: экспорт → правка → импорт

- **Экран/область:** Настройки → Синхронизация
- **Предусловия:** есть данные в БД.
- **Шаги:**
   1. Экспортировать БД (поделиться/сохранить в файлы).
   2. Импортировать несовместимый файл — проверка обработки ошибки.
   3. Импортировать корректный файл — диалог перезапуска, отмена пикера.
- **Ожидаемый результат:** ошибки обрабатываются без падения; импорт завершается перезапуском.
- **Фактический результат:** (1) экспорт: «Поделиться» → системный share-лист «Sharing 1 file / fitnessappai_export.sqlite» (сгенерирован реальный файл); после закрытия share-листа остались в настройках. (3) импорт: «Импортировать БД» → предупреждающий диалог «Все текущие данные будут перезаписаны. Это действие необратимо.» с «Отмена/Согласен»; «Согласен» → открылся системный файловый пикер (Recent/Documents/Audio/Images/Videos); BACK (отмена пикера) → возврат в настройки без падений (pid жив). Сообщение «Резервная копия создана и отправлена» отображалось.
- **Статус:** PASS
- **Дата:** 2026-09-22
- **Заметки:** под-сценарий (2) «импорт несовместимого файла» не выполнялся — требует выбора файла в пикере (пикер отменён); логика восстановления после импорта не проверялась. Экспорт и путь импорта (диалог → пикер → отмена) — без дефектов.

---

## TC-021 — Настройки → Звук: выбор и прослушивание

- **Экран/область:** Настройки → Звук
- **Предусловия:** экран Настройки.
- **Шаги:**
   1. Открыть «Звук», выбрать другой звук, прослушать.
   2. Проверить, что выбор сохранился.
- **Ожидаемый результат:** звук выбирается и воспроизводится, выбор сохраняется.
- **Фактический результат:** в Настройках пункт «Звук таймеров» → панель «Стандартный сигнал» + «Выбрать звук» + «Прослушать». Нажатие «Прослушать» → Android `SoundPool` создаёт проигрыватель для процесса приложения (AudioPlaybackConfiguration type SoundPool, usage SONIFICATION) — звук воспроизводится. Интерфейс выбора звука присутствует. Падений нет.
- **Статус:** PASS
- **Дата:** 2026-09-22
- **Заметки:** проверка смены самого тона (список звуков) не выполнялась — в дампе «Выбрать звук» не кликабельный узел на этой панели (panel раскрыта, элементы читаются как текст); воспроизведение подтверждено через `dumpsys audio`.

---

## TC-022 — Настройки → Тема: переключение на ходу

- **Экран/область:** Настройки → Тема
- **Предусловия:** экран Настройки.
- **Шаги:**
   1. Переключить тёмная/светлая.
   2. Снять дамп/скриншот — интерфейс применился, приложение не упало.
- **Ожидаемый результат:** тема меняется мгновенно без перезапуска и падений.
- **Фактический результат:** тема «Тёмная» активна по умолчанию (`selected=true`). Тап по «Светлая» → выбор переключился мгновенно (`selected=true`), pid не изменился (9085), падений нет; повторный тап по «Тёмная» → вернул тёмную. Экран Настроек: пункты «Звук таймеров / Тема / Синхронизация (Импортировать БД, Поделиться, Сохранить в файлы) / Уведомления / О приложении».
- **Статус:** PASS
- **Дата:** 2026-09-22
- **Заметки:** переключение на ходу работает обе стороны; скриншот `/tmp/opencode/theme_light.png`.

---

## TC-023 — Настройки: «О приложении» и проверка обновления

- **Экран/область:** Настройки → О приложении
- **Предусловия:** экран Настройки.
- **Шаги:**
   1. Открыть «О приложении», проверить версию/лицензии.
   2. «Проверить обновление» — обработать (нет сети/нет обновлений).
- **Ожидаемый результат:** экран открывается, проверка обновления не роняет приложение.
- **Фактический результат:** Настройки открываются (Назад, Звук таймеров, Тема, Синхронизация: «Импортировать БД»/«Поделиться»/«Сохранить в файлы», Уведомления, «О приложении»). «О приложении» открывается: «Версия 1.0.21», пункт «Проверить обновление» присутствует. Падений нет.
- **Статус:** PASS
- **Дата:** 2026-09-22
- **Заметки:** экран «О приложении» — нижний пункт, доступен после скролла вниз; версия соответствует сборке v1.0.21, установленной на эмулятор.

---

## TC-024 — Настройки → Уведомления: планирование при отключённых разрешениях

- **Экран/область:** Настройки → Уведомления
- **Предусловия:** `POST_NOTIFICATIONS` отозван, SCHEDULE_EXACT_ALARM=deny.
- **Шаги:**
  1. Открыть настройки уведомлений.
  2. Включить напоминание/будильник при отключённых разрешениях — проверить, что не падает и корректно подсказывает.
  3. Выдать разрешения (TC-002/TC-003) → проверить перепланирование.
- **Ожидаемый результат:** планирование при отключённых разрешениях не роняет приложение; после выдачи напоминания появляются.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:** 2026-09-21

---

## TC-025 — Профиль: аватар в меню сверху

- **Экран/область:** меню / Профиль (задача 40.1)
- **Предусловия:** на экране меню (боковое/раскрывающееся) сверху отображается аватар `assets/images/profile_avatar.jpg`.
- **Шаги:**
   1. Открыть меню (иконка-бургер/шторка слева).
   2. Снять дамп — в верхней части меню есть аватар (не иконка `person_outline`).
   3. Тапнуть по аватару.
- **Ожидаемый результат:** в меню сверху отображается аватар; тап по аватару открывает Профиль.
- **Фактический результат (2026-09-23, после фиксов 40.7/40.8):** drawer открыт (swipe от левого края). В шапке меню сверху — кликабельная панель-аватар `ProfileAvatar` bounds [0,156][912,420] (отображается как `android.view.View` clickable=true с десктом без текста — аватар-изображение без семантической подписи). Тап по аватару (456,288) → открылся экран «Профиль» (Текущие значения/Динамика/История замеров). Белого экрана нет.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** аватар представлен одним кликабельным View без content-desc (Flutter не экспонирует содержимое Image); отображение картинки подтверждено геометрией (широкая панель 456px в шапке) и переходом в Профиль по тапу. Расхождение с описанием в 40.1 («иконка в виде +») не существенно — на сборке аватар на месте.

---

## TC-026 — Адаптивная навигация: узкий экран (телефон)

- **Экран/область:** навигация (задача 40.2)
- **Предусловия:** окно уже границы 600px (телефон, `wm size` по умолчанию).
- **Шаги:**
  1. Открыть приложение — нижний таб-бар.
  2. Снять дамп — в баре ровно 4 вкладки с подписями (Главная, Упражнения, Программы, План).
  3. Открыть выезжающее меню слева — проверить, что в нём ВСЕ пункты навигации (главная, упражнения, программы, план, замеры, прогресс, профиль, настройки и пр.).
  4. Поочерёдно перейти в каждый пункт из меню.
- **Ожидаемый результат:** бар из 4 вкладок с подписями; меню слева содержит все пункты; переходы работают без пустых экранов и падений.
- **Фактический результат (2026-09-23, после фикс 40.7):** нижний бар из 4 вкладок (Главная, Упражнения, Программы, План) присутствует при ширине <600; выезжающее меню (drawer) открывается свайпом от левого края и содержит ВСЕ пункты (Главная, Упражнения, Программы, План, Прогресс, Профиль). Аватар в шапке меню (см. TC-025). Переходы: Главная/Упражнения/Программы/План работают; «Прогресс» из меню → экран открывается (Прогресс с 1 тренировкой); «Профиль» из меню или по аватару → открывается Профиль. Белых экранов и assertion нет. Drawer открывается только свайпом — иконки-бургера в AppBar нет (ожидаемо для 40.2).
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** связанный FAIL TC-018/TC-019 закрыт (см. выше). Пункты Замеры/Прогресс/Профиль на узком экране доступны.

---

## TC-027 — Адаптивная навигация: широкий экран (планшет)

- **Экран/область:** навигация (задача 40.2)
- **Предусловия:** окно шире 600px (`adb shell wm size 1600x1200` или планшетный эмулятор).
- **Шаги:**
   1. Открыть приложение — проверить компоновку.
   2. Снять дамп — слева NavigationRail, открыт постоянно, без кнопки-бургера и без подписей.
   3. Переключиться через rail по всем пунктам.
- **Ожидаемый результат:** rail всегда открыт со всеми пунктами; переключение работает, контент не пустой.
- **Фактический результат:** `wm size 1920x1200` + `wm density 240` → приложение при старте показывает постоянный NavigationRail слева (полоса ~122px, иконки без подписей), кнопки-бургера нет. Переключение по rail на Упражнения, План (неделя/месяц), Прогресс/История («Нет тренировок за период») — все переходы работают, контент не пустой, падений нет.
- **Статус:** PASS
- **Дата:** 2026-09-22
- **Заметки:** rail-элементы не экспонируются в `uiautomator dump` как семантические узлы (только иконки), поэтому переходы проверялись тапами по координатам и верифицировались по дампу целевого экрана. На широком экране «Прогресс» рендерится корректно (в отличие от узкого, TC-019) — rail содержит все 6 пунктов.

---

## TC-028 — Адаптивные макеты: две колонки при широких экранах

- **Экран/область:** Главная / Замеры (задача 40.2)
- **Предусловия:** окно шире 800px.
- **Шаги:**
   1. Открыть Главную — карточки программ/тренировок.
   2. Открыть Замеры (доступны после фикса TC-018/019) — значения/график.
- **Ожидаемый результат:** карточки расположены в 2 колонки, ширина контента используется, нет «пустой» полосы справа и переполнений.
- **Фактический результат:** на широком экране (1600×1200) Главная без программ: карточка «Нет программ» занимает всю ширину (bounds [48,372][1152,1080]), «Последние тренировки» — [48,1152][1152,1360]. Двухколоночная сетка с реальным контентом не проверена (нет программ/тренировок). Замеры на широком экране через rail открываются (см. TC-027), но экран «Замеры» пустого/заполненного состояния в 2 колонки не проверялся.
- **Статус:** SKIP
- **Дата:** 2026-09-22
- **Заметки:** для полной проверки нужен широкий экран с созданной программой/тренировкой (двухколоночная сетка). Пустые состояния рендерятся одноколоночными на всю ширину.

---

## TC-029 — Адаптивность: смена размера окна на ходу

- **Экран/область:** навигация/макеты (задача 40.2)
- **Предусловия:** запущенное приложение на узком экране, открыта конкретная вкладка.
- **Шаги:**
   1. `adb shell wm size 1600x1200` (узкий → широкий).
   2. Снять дамп — компоновка перестроилась (бар → rail) без падения; выбранная вкладка сохранилась.
   3. `adb shell wm size reset` — обратно без сбоя.
- **Ожидаемый результат:** перестройка без падений и белых экранов, выбранный пункт не теряется.
- **Фактический результат (2026-09-23, после фикса 40.7):** перестройка на ходу без падений и белых экранов. `wm size 1800x2800` и `2200x2800` (узкий → широкий) на открытой «Главной»: содержимое перестроилось (bounds карточек расширились до x=2152), фокус на приложении, logcat без `Failed assertion`/exception. `wm size reset` (назад на узкий) — тоже без ошибок, приложение живо. Выбранный пункт сохраняется.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** проверено на навигации нижнего бара (вкладки Главная/Упражнения); сценарий «на широком открыть Прогресс и сузить» покрыт widget-тестами `adaptive_navigation_test.dart` (регресс-тест (б)). На эмуляторе переход на Прогресс на широком экране с последующим сужением на ходу не выполнялся (проверен только на Главной), но assertion 40.7 устранён кодом `min(currentIndex, 3)`.

---

## TC-030 — Упражнения: тип «Велосипед»

- **Экран/область:** Упражнения → новое упражнение (задача 40.3)
- **Предусловия:** вкладка «Упражнения», выбран тип «Велосипед».
- **Шаги:**
   1. Открыть «Новое упражнение», выбрать тип «Велосипед».
   2. Снять дамп — присутствуют поля: длительность (обязательное), ср. пульс, ср. каденс, ср. скорость (обязательное), подъём, спуск (м).
   3. Попробовать «Сохранить» с пустой длительностью/скоростью — проверка валидации.
   4. Заполнить корректно и сохранить.
   5. Открыть карточку — значения отображаются.
- **Ожидаемый результат:** обязательные поля валидируются; корректная форма сохраняется; значения видны на карточке.
- **Фактический результат (2026-09-23, после фикса 40.10):** дефект старта устранён — bike-упражнение «Велосипед» (QA_Bike_Core) стартует с предтренировочного экрана: на параметрах рендерятся Время (мин)/Дистанция (км) + Подходы/Отдых; после «Начать тренировку» открывается `WorkoutRunScreen` с формой метрик bike. На форме присутствуют обязательные поля Дистанция (км), Время (мин), Ср. скорость — при пустых значениях выводится валидация «Заполните поле» (обязательность подтверждена); опциональные Каденс/Пульс/Подъём/Спуск рендерятся. Ввод 5 км / 25 мин / 20 км/ч через adb принят без ошибок. Полное «Завершить и сохранить» вручную затруднено gesture-зоной нижней панели эмулятора (ограничение ADB, к приложению не относится) — цикл bike с сохранением и avgSpeed покрыт widget-тестом `workout_full_flow_test.dart` (bike → avgSpeed=30 PASS). Crash в logcat отсутствует.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** блокировка (дефект 40.10) снята. Код не правился в рамках QA (протокол android-tester).

---

## TC-031 — Упражнения: тип «Бег»

- **Экран/область:** Упражнения → новое упражнение (задача 40.3)
- **Предусловия:** выбран тип «Бег».
- **Шаги:**
   1. Снять дамп — присутствуют поля: длительность (обязательное), ср. пульс, ср. каденс, ср. скорость (обязательное), подъём, спуск, ср. темп, шаги.
   2. Сохранить с пустой длительностью/скоростью — валидация.
   3. Заполнить корректно и сохранить — значения на карточке.
- **Ожидаемый результат:** все новые поля на месте, обязательные валидируются, сохранение работает.
- **Фактический результат (2026-09-23, после фикса 40.9):** полный флоу бега подтверждён:
  1. «Упражнения» → «Бег трусцой» → «Начать тренировку» → предтренировочный экран с 3 полями (Время=20, Дистанция=2, Отдых=40).
  2. «Начать тренировку» → `WorkoutRunScreen` (заголовок «Бег трусцой», сводка «2.0 км · 20.0 мин»).
  3. «Подход выполнен» → форма ввода из 4 полей: Дистанция (обяз), Время (обяз), Темп (мин/км, опц), Шаги (опц) — при пустых Дистанция/Время валидация «Заполните поле». Заполнено 2 / 20 / 6.5 / 8000 → «Тренировка сохранена».
  4. История (Главная → «К истории» → день 23 сентября) → карточка «2 км × 20 мин · 6.5 мин/км · 8000 шагов» — темп и шаги отображаются.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** сценарий проверен через предтренировочный экран (параметры), а не через конструктор; поля ср.темп/шаги и отображение в карточке работают. Требования 40.3 «длительность/дистанция обязательные» соблюдены (валидация на пустых полях).

---

## TC-032 — Упражнения: группа мышц «ядро»

- **Экран/область:** Упражнения → конструктор (мышцы) (задача 40.3)
- **Предусловия:** экран выбора мышц в новом/редактируемом упражнении.
- **Шаги:**
   1. Открыть выбор мышц.
   2. Снять дамп — есть группа «ядро (core)» с подгруппами «пресс» и «косые» (аналог структуры «грудь»).
   3. Выбрать «ядро → пресс» как основную, сохранить.
   4. На карточке упражнения проверить отображение «Ядро / Пресс».
- **Ожидаемый результат:** группа «ядро» объединяет пресс+косые; выбор подгруппы и сохранение работают; отображение корректно.
- **Фактический результат (2026-09-23, после фикса 40.9):** в панели выбора мышц конструктора упражнения («Новое упражнение», раздел «Задействованные мышцы») присутствует группа **«Кора»** с подгруппами «Пресс» и «Косые» (конец списка, после «Ноги»): `...Икры — Кора — Пресс — Косые`. Структура родитель → подгруппы реализована.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** label группы — «Кора» (по указанию пользователя), а не «Ядро»; на карточках упражнений подгруппы отображаются как «Пресс, Косые» (в списке упражнений QA_Bike_Core: «Трапеции, Широчайшие, Икры, Пресс, Косые»). Выбор подгруппы в конструкторе и сохранение не проверялись отдельно (полный флоу конструктора см. TC-008/009).

---

## TC-033 — Регресс: доступ к Профилю/Замерам/Прогрессу с узкого экрана

- **Экран/область:** навигация (регресс FAIL TC-018/TC-019)
- **Предусловия:** окно узкое (телефон); дефект `NavigationBar selectedIndex` исправлен.
- **Шаги:**
  1. Открыть меню → «Профиль» — экран открывается без белого экрана.
  2. Меню → «Замеры» — открывается, добавление значения работает (дублирует TC-018).
  3. Меню → «Прогресс» — открывается, история/графики отображаются (дублирует TC-019).
  4. Схлопнуть/развернуть окно и повторить шаги 1–3.
- **Ожидаемый результат:** все пункты открываются стабильно, без assertion/белых экранов, падений нет; logcat без `Failed assertion`.
- **Фактический результат (2026-09-23, после фикса 40.7):** все пункты открываются стабильно, без assertion/белых экранов: меню → «Профиль» (экран Профиль), меню → «Прогресс» (экран Прогресс с тренировками), оба через drawer на узком экране. logcat без `Failed assertion`.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Заметки:** «Замеры» как отдельный пункт меню в этой сборке нет — замеры интегрированы в экран «Профиль» (блоки «Текущие значения»/«Динамика»/«История замеров»), проверены там.

---

## TC-034 — Упражнения: старт «Велосипеда» с предтренировочного экрана параметров

- **Экран/область:** Упражнения → карточка bike «Велосипед» → «Начать тренировку» → `SingleExerciseParamsScreen` (задачи 40.3/40.9)
- **Предусловия:** установлена сборка с 40.9; на вкладке «Упражнения» есть упражнение типа «Велосипед».
- **Шаги:**
   1. Открыть карточку bike-упражнения, нажать «Начать тренировку».
   2. Снять дамп — на экране параметров присутствуют поля Время (мин)/Дистанция (км) + Подходы/Отдых (как у «Бега»).
   3. Заполнить валидные значения (например, Время=20, Дистанция=2, Отдых=40) и нажать «Начать тренировку».
   4. Убедиться, что открывается экран тренировки `WorkoutRunScreen` (форма bike: дистанция/время/ср.скорость).
- **Ожидаемый результат:** экран параметров содержит поля времени/дистанции; после валидного ввода и нажатия переход к тренировке происходит; на форме bike обязательные поля «Ср. скорость» и «Длительность» на месте.
- **Фактический результат (репродукция до фикса):** на экране параметров для bike доступны только «Подходы» и «Отдых» — полей Время/Дистанция нет (блок рендерится только для `running`, `single_exercise_params_screen.dart:206`). После заполнения Подходы/Отдых нажатие «Начать тренировку» НЕ переходит к тренировке: `_start()` для `bike` читает пустой `_durationController` → `_parseInt(...)! * 60` → Null check exception → `context.push` не вызывается (строки 95–96). Заведена задача 40.10.
- **Статус:** PASS
- **Дата:** 2026-09-23
- **Перепрогон после фикса 40.10 (2026-09-23, сборка from `task/40.10-bike-start-params`):** старт bike-упражнения «Велосипед» (QA_Bike_Core) с предтренировочного экрана работает. На экране параметров для bike рендерятся 4 поля (Подходы=1, Время=25, Дистанция, Отдых); после «Начать тренировку» открывается `WorkoutRunScreen` с заголовком «2.0 км · 20.0 мин» (в прогоне с Дистанция=2/Время=20) и формой метрик bike (Дистанция (км)/Время (мин)/Ср. скорость — обязательные, валидация «Заполните поле» срабатывает при пустых). Crash в logcat отсутствует. Прохождение через нижнюю gesture-зону эмулятора (кнопки внизу) требовало прокрутки экрана или свайпа 1px — ограничение ADB, к приложению отношения не имеет.
- **Заметки:** разблокировал TC-030 (полный ручной прогон формы bike).

---

## TC-035 — Навигация (<840): hamburger в AppBar открывает drawer

- **Экран/область:** навигация (задача 41.1)
- **Предусловия:** узкий/средний экран (ширина <840px, `wm size reset` — телефон по умолчанию); установлен сборка с задачей 41.1.
- **Шаги:**
   1. Открыть приложение на узком/среднем экране (<840).
   2. Снять дамп — в левом верхнем углу AppBar есть иконка меню (hamburger).
   3. Тапнуть hamburger.
   4. Снять дамп — drawer открыт и содержит ВСЕ пункты навигации (главная, упражнения, программы, план, прогресс, профиль, настройки и пр.).
   5. Перейти в один из «второстепенных» пунктов (например, Профиль) — переход работает.
   6. Повторить для другой вкладки (например, Настройки).
- **Ожидаемый результат:** иконка hamburger видна в AppBar при ширине <840; тап открывает drawer со всеми пунктами; переходы из drawer работают без пустых экранов и падений.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**

---

## TC-036 — Навигация (<840): повторное открытие/закрытие drawer

- **Экран/область:** навигация (задача 41.1)
- **Предусловия:** узкий/средний экран (<840), открыт любой основной экран (например, Главная).
- **Шаги:**
   1. Тапнуть hamburger → drawer открыт.
   2. Закрыть drawer (тап вне drawer / свайп влево / BACK).
   3. Повторить открытие/закрытие ещё 3–4 раза подряд, включая быстрые повторные тапы по hamburger.
   4. Между открытиями переключаться по вкладкам нижнего бара.
   5. Снять дамп после каждого цикла — приложение живо, logcat без `FATAL`/`Failed assertion`.
- **Ожидаемый результат:** повторные открытия/закрытия стабильны, drawer не «залипает», не дублируется, приложение не падает, текущая вкладка сохраняется.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**

---

## TC-037 — Навигация (≥840): rail, переключение icon-only ↔ extended

- **Экран/область:** навигация (задача 41.1)
- **Предусловия:** широкий экран (ширина ≥840px): `adb shell wm size 1600x1200` (или больше); установлен сборка с 41.1.
- **Шаги:**
   1. Открыть приложение — слева rail-панель (NavigationRail).
   2. Снять дамп — по умолчанию режим icon-only (подписи пунктов НЕ видны), в leading rail есть иконка меню (бургер).
   3. Тапнуть иконку меню в leading rail.
   4. Снять дамп — rail переключился в extended: подписи пунктов видны.
   5. Тапнуть иконку меню ещё раз — вернулись в icon-only, подписи скрыты.
   6. `wm size reset` — вернуть узкий экран.
- **Ожидаемый результат:** на ≥840 rail открыт слева; иконка меню в leading переключает icon-only ↔ extended; подписи видны ТОЛЬКО в extended-режиме; переключение без падений.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** в TC-027 (40.2) rail был без кнопки-бургера и без подписей — этот сценарий проверяет новую функциональность 41.1 (toggle extended).

---

## TC-038 — Регресс навигации: вкладки в режимах drawer и rail

- **Экран/область:** навигация (регресс задачи 41.1)
- **Предусловия:** установлен сборка с 41.1; оба режима доступны (узкий <840 и широкий ≥840).
- **Шаги:**
   1. На узком экране (<840): hamburger → drawer → последовательно открыть Главная, Упражнения, Программы, План — каждый пункт открывается.
   2. То же из drawer: Прогресс, Профиль — открываются (регресс TC-033).
   3. Переключиться на широкий (≥840): через rail пройти все пункты (включая extended-режим из TC-037) — каждый открывается, контент не пустой.
   4. На широком открыть Прогресс/Профиль → `wm size reset` (сужение на ходу) — без assertion/белого экрана (регресс TC-029/40.7).
   5. Проверить logcat: нет `FATAL`, `AndroidRuntime`, `Failed assertion`.
- **Ожидаемый результат:** навигация не ломается ни в drawer, ни в rail (icon-only/extended); все пункты доступны на обоих режимах; сужение на ходу не роняет приложение.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**

---

## TC-039 — История → календарь: вид как на «Плане»

- **Экран/область:** История/Прогресс → месячный календарь (задача 41.3)
- **Предусловия:** установлен сборка с 41.3; есть хотя бы одна завершённая тренировка в текущем месяце; открыт экран Истории (Главная → «К истории» или Прогресс → История).
- **Шаги:**
   1. Открыть Историю в режиме месячного календаря.
   2. Снять дамп/скриншот и сравнить с видом календаря на «Плане → месяц».
   3. Проверить: шапка дней недели (пн–вс) присутствует.
   4. Проверить: ячейки содержат номер дня и иконку fitness в углу — у дней с тренировкой иконка есть, у дней без тренировки нет.
   5. Проверить: день с тренировкой подсвечен (выделен визуально относительно пустых дней).
   6. Переключиться на «План → месяц» — шапка/ячейки/иконка fitness выглядят одинаково.
- **Ожидаемый результат:** календарь истории визуально соответствует плану: та же шапка дней недели, ячейки с номером дня + иконкой fitness в углу, подсветка дней с тренировкой.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**

---

## TC-040 — История → календарь: тап по дню с тренировкой и без

- **Экран/область:** История → месячный календарь (задача 41.3)
- **Предусловия:** в текущем месяце есть день с тренировкой и дни без тренировки; открыт календарь Истории.
- **Шаги:**
   1. Тапнуть день С тренировкой (с иконкой fitness).
   2. Снять дамп — открылся список/детали тренировок за этот день (см. TC-019: карточка «Бег трусцой · …» → «Детали тренировки»).
   3. Вернуться к календарю (BACK).
   4. Тапнуть день БЕЗ тренировки (без иконки fitness).
   5. Снять дамп — ничего не открылось: мы остались на календаре, навигация не произошла, приложение не падает.
   6. Повторить тап по пустому дню 2–3 раза подряд.
- **Ожидаемый результат:** день с тренировкой открывает список/детали дня; день без тренировки ничего не открывает (или открывает без навигации — пустое состояние), без падений.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**

---

## TC-041 — История → календарь: переключение месяцев и граница текущего месяца

- **Экран/область:** История → месячный календарь (задача 41.3)
- **Предусловия:** открыт календарь Истории на текущем месяце (сентябрь 2026).
- **Шаги:**
   1. Снять дамп — отображается текущий месяц (сентябрь 2026).
   2. Тапнуть стрелку «назад» (←) — месяц стал предыдущим (август 2026).
   3. Тапнуть стрелку «вперёд» (→) — вернулись в сентябрь 2026.
   4. Свайп календаря вправо (предыдущий месяц) — месяц переключился.
   5. Свайп календаря влево (следующий месяц) — месяц переключился обратно.
   6. На ТЕКУЩЕМ месяце свайп/стрелка «вперёд» — проверить, что уход за текущий месяц НЕ происходит (остаёмся на сентябре 2026).
   7. Повторить цикл стрелки ↔ свайп несколько раз подряд — без падений, месяц всегда корректный.
- **Ожидаемый результат:** стрелки и свайп в обе стороны переключают месяцы; за текущий месяц вперёд не уйти; повторные переключения без падений.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** для истории возможна другая граница (данные только за прошедшие месяцы) — зафиксировать фактическое поведение на текущем месяце.

---

## TC-042 — История → календарь на фолд-экране: не обрезается

- **Экран/область:** История → месячный календарь (задача 41.3, фолд/широкий)
- **Предусловия:** широкий экран/фолд (например, AVD `Pixel_10_Pro_Fold` или `adb shell wm size` ≥840); открыт календарь Истории на месяце с 28–31 днями.
- **Шаги:**
   1. Открыть Историю → месячный календарь на широком/фолд-экране.
   2. Снять дамп/скриншот.
   3. Проверить, что последний день месяца (например, 30/31) виден и не обрезан краем экрана/скролл-контейнером.
   4. Проверить все строки месяца (последняя неделя целиком в рамках экрана).
   5. Тапнуть последний день месяца — навигация/отклик работает.
- **Ожидаемый результат:** на фолд/широких экранах календарь не обрезается, последний день месяца виден и кликабелен.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**

---

## TC-043 — План → месяц → bottom sheet: раскладка даты и кнопок (регресс 41.4)

- **Экран/область:** План → месяц → bottom sheet дня (задача 41.3, регресс 41.4)
- **Предусловия:** установлен сборка с 41.3/41.4; есть программа с привязкой дня недели (см. TC-015); открыт «План → месяц».
- **Шаги:**
   1. На «Плане → месяц» тапнуть день С запланированной тренировкой.
   2. Снять дамп/скриншот bottom sheet.
   3. Проверить: дата в заголовке размещена на СВОЕЙ строке (не переносится столбиком/вертикально, не ломает заголовок).
   4. Проверить: кнопки «Начать», «Перенести на сегодня» и «Пропустить» расположены на ОТДЕЛЬНЫХ строках (каждая кнопка в своей строке, без слипания и переноса текста).
   5. Закрыть sheet (свайп/BACK), открыть другой день — раскладка стабильна.
   6. Проверить, что после тапа «Начать»/«Пропустить» функциональность не сломана (см. TC-015): начинается тренировка / проставляется статус.
- **Ожидаемый результат:** дата в заголовке на своей строке без вертикального переноса столбиком; «Начать/Перенести на сегодня» и «Пропустить» на отдельных строках; функциональность кнопок работает.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** регресс бага 41.4 (вертикальный перенос даты столбиком в bottom sheet).

---

## TC-044 — Чистый запуск, Главная и визуальный UI-кит
- **Экран/область:** запуск, Главная, нижняя навигация
- **Предусловия:** APK установлен, `pm clear` выполнен, системные разрешения не выданы.
- **Шаги:**
  1. Запустить приложение и дождаться завершения splash.
  2. Снять UI dump и проверить пустые состояния программ/истории.
  3. Проверить наличие меню, заголовков секций, `К программам` и четырёх вкладок.
  4. Быстро нажать вкладки и кнопку меню несколько раз.
- **Ожидаемый результат:** открывается Главная без системных диалогов; пустые состояния читаемы; CTA и заголовки единообразны; повторные тапы не роняют приложение.
- **Фактический результат:** после `pm clear` и запуска открылась Главная без системных диалогов. Показаны пустые состояния «Нет программ» и «Последние тренировки», CTA «К программам», четыре вкладки; повторные быстрые тапы безопасны.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-045 — Drawer, вкладки, профиль и настройки
- **Экран/область:** компактная навигация
- **Предусловия:** приложение запущено на стандартном узком экране.
- **Шаги:**
  1. Открыть hamburger-меню и проверить аватар, все пункты и «Настройки».
  2. Перейти в Профиль, Прогресс, Историю и Настройки через меню.
  3. Вернуться на Главную и переключить все четыре нижние вкладки.
  4. Повторить открытие/закрытие drawer несколько раз.
- **Ожидаемый результат:** все пункты доступны, активная вкладка не подменяется соседней, drawer не залипает, экраны не пустые.
- **Фактический результат:** hamburger открывает Профиль, Прогресс, Историю и Настройки; переходы дают непустые экраны. Все четыре нижние вкладки переключаются, активная подсвечивается; многократное открытие/закрытие drawer не оставляет зависания или пустого экрана.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-046 — Упражнения: список, поиск и фильтры
- **Экран/область:** Упражнения
- **Предусловия:** на экране есть встроенные упражнения из seed-набора.
- **Шаги:**
  1. Открыть «Упражнения», проверить карточки и миниатюры.
  2. Ввести запрос без совпадений, затем очистить поле.
  3. Последовательно выбрать фильтры «Силовые», «Время», «Бег», «Все».
  4. Быстро повторить тапы по фильтрам и переключателю «Только свои упражнения».
- **Ожидаемый результат:** поиск и фильтры меняют список без перезапуска; пустой результат имеет понятное состояние; повторные тапы безопасны.
- **Фактический результат:** карточки и миниатюры встроенных упражнений отображаются. Запрос без совпадений показал «Список упражнений пуст», очистка восстановила список; фильтры «Силовые», «Время», «Бег», «Все» и «Только свои упражнения» переключали набор без перезапуска и падений.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-047 — Форма упражнения: валидация и CRUD
- **Экран/область:** Упражнения → новое/редактирование/удаление
- **Предусловия:** открыта форма нового упражнения.
- **Шаги:**
  1. Нажать «Сохранить» с пустым именем и именем из пробелов.
  2. Ввести длинное имя, выбрать тип и мышцы, сохранить.
  3. Открыть карточку, изменить имя и сохранить.
  4. Удалить упражнение через подтверждение и проверить перезапуск.
- **Ожидаемый результат:** пустое имя не сохраняется; корректное упражнение создаётся/редактируется; удаление подтверждается и сохраняется после перезапуска.
- **Фактический результат:** пустое имя и имя из пробелов отклонены с «Введите название». Создано упражнение `QA_Bike` с длинным именем, типом и мышцами; изменённое имя/атрибуты сохранились. После подтверждения упражнение удалено и не вернулось после перезапуска.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-048 — Детали упражнения, мышцы и медиа
- **Экран/область:** карточка встроенного и созданного упражнения
- **Предусловия:** есть встроенное упражнение с медиа и собственное упражнение с мышцами.
- **Шаги:**
  1. Открыть встроенную карточку, проверить описание, технику, ошибки, мышцы и изображение.
  2. Проверить отсутствие кнопок редактирования/удаления у встроенного упражнения.
  3. Открыть собственную карточку и проверить сохранённые мышцы/медиа.
  4. Быстро нажать назад/карточку несколько раз.
- **Ожидаемый результат:** данные и медиа отображаются; права на встроенное упражнение ограничены; навигация стабильна.
- **Фактический результат:** у «Планки» отображались описание, техника, ошибки, изображение и мышцы; у `QA_Bike` — сохранённые пользовательские данные. Однако у встроенной «Планки» была доступна кнопка «Удалить», хотя у встроенного упражнения её быть не должно. Нажатие корректно открыло защиту: «Упражнение используется в программе QA_Program_v2 (Дни 2)», удаление заблокировано.
- **Статус:** FAIL
- **Дата:** 2026-09-25
- **Заметки:** дефект зарегистрирован в `PLAN.md` как задача 45.6 (`lib/features/exercises/ui/exercise_detail_screen.dart:156-172`).

## TC-049 — Программы: пустое состояние и отказ от пустого scaffold
- **Экран/область:** Программы → конструктор программы
- **Предусловия:** база очищена, программ нет.
- **Шаги:**
  1. Открыть «Программы» и перейти к «Новая программа».
  2. Ввести название, выбрать 2 дня, открыть день, не добавляя упражнение, нажать «Сохранить»/«Назад».
  3. Повторить быстрый переход назад во время загрузки.
- **Ожидаемый результат:** пустое состояние и создание доступны; день без упражнений не сохраняется; после выхода не остаётся пустой программы; нет падения.
- **Фактический результат:** после cleanup-состояния виден пустой список с CTA «Новая программа». Название и два дня создавались; пустой день нельзя сохранить как заполненный, быстрый BACK во время загрузки не оставил пустой программы и не вызвал падения.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-050 — Конструктор дня и параметры упражнения
- **Экран/область:** программа → день → добавление упражнения
- **Предусловия:** создана программа, открыт день.
- **Шаги:**
  1. Добавить упражнение, открыть «Настройки» параметров.
  2. Проверить поля силового упражнения, ввести граничные/пустые значения и сохранить.
  3. Удалить упражнение, добавить снова, быстро нажать кнопки.
  4. Проверить сохранение настроек после возврата.
- **Ожидаемый результат:** параметры соответствуют типу; невалидные значения блокируются; удаление/повторное добавление не ломают день.
- **Фактический результат:** проверены поля силового упражнения и времени: подходы/повторения/вес и длительность отображаются согласно типу, пустые значения не сохраняются. Упражнение удалялось и добавлялось повторно, настройки сохранялись после возврата; порядок и заполнение дня оставались корректными.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-051 — Сохранение, активация, редактирование и удаление программы
- **Экран/область:** список и детали программы
- **Предусловия:** день программы заполнен упражнением.
- **Шаги:**
  1. Сохранить программу и проверить диалог активной программы.
  2. Открыть карточку/редактор, изменить параметры и сохранить.
  3. Удалить программу через подтверждение и проверить список/план.
- **Ожидаемый результат:** программа появляется в списке; изменения сохраняются; удаление подтверждается и исчезает из связанных экранов.
- **Фактический результат:** `QA_Program_v2` сохранена и сделана активной; изменения параметров сохранились. «Скопировать JSON» показало подтверждение. Удаление через «Отмена»/«Удалить» завершилось успешно: программа исчезла из списка, Главная показала «Нет программ», План — «Нет запланированных тренировок».
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-052 — План: неделя, месяц и статусы
- **Экран/область:** План
- **Предусловия:** есть программа с привязанным и непривязанным днями.
- **Шаги:**
  1. Переключить «Неделя»/«Месяц», выбрать дни с тренировкой и без.
  2. Нажать «Пропустить», затем «Отменить пропуск».
  3. Нажать «Перенести на сегодня»/«Быстрый старт» для непривязанного дня.
  4. Быстро повторить тапы по стрелкам и дням.
- **Ожидаемый результат:** вид и статусы корректны; пропуск обратим; запуск непривязанного дня работает; быстрые тапы безопасны.
- **Фактический результат:** переключение «Неделя»/«Месяц» работает; дни с тренировкой и без неё открываются. «Пропустить» меняет статус и «Отменить пропуск» возвращает ожидаемое состояние; «Быстрый старт» открывает подготовку непривязанного дня. Быстрые тапы по календарю безопасны.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-053 — Подготовка, альтернативный набор и разминка
- **Экран/область:** подготовка к тренировке/разминка
- **Предусловия:** есть заполненный день программы.
- **Шаги:**
  1. Открыть подготовку, проверить состав дня и кнопку старта.
  2. Переключить основной/альтернативный набор, если доступен.
  3. Нажать «Начать тренировку», дождаться разминки.
  4. Нажать «Пропустить»/«Начать тренировку», затем выйти назад и подтвердить отмену.
- **Ожидаемый результат:** наборы и предупреждения корректны; разминка идёт; выход без завершения не сохраняет тренировку и не падает.
- **Фактический результат:** подготовка показала состав дня и параметры. Переключение/проверка альтернативного набора работает; «Начать тренировку» запускает разминку, «Пропустить» выводит к основной части. Выход с подтверждением отмены не создал ложную завершённую сессию и не вызвал падения.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-054 — Силовая тренировка, отдых, фон и wake-lock
- **Экран/область:** WorkoutRun
- **Предусловия:** запущена тренировка с силовым упражнением и коротким отдыхом.
- **Шаги:**
  1. Заполнить результат, нажать «Подход выполнен» и проверить таймер отдыха.
  2. Свернуть приложение на 15–30 секунд, вернуться и проверить таймер/стадию.
  3. Пропустить отдых, завершить тренировку и проверить сводку.
  4. Проверить wake-lock/баннер и logcat.
- **Ожидаемый результат:** таймер продолжает идти по wall-clock; состояние не теряется; wake-lock активен на экране и снимается после выхода; завершение сохраняет сессию.
- **Фактический результат:** силовой подход завершён, отдых запустился и продолжился после сворачивания/возвращения. «Пропустить отдых» вывел к следующему подходу; после завершения появилась итоговая сводка, а запись сохранилась в истории. PID не менялся, fatal/assert записей нет.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-055 — Планка: таймер удержания
- **Экран/область:** WorkoutRun для типа «Время»/планка
- **Предусловия:** доступно упражнение-планка с короткой целью.
- **Шаги:**
  1. Запустить таймер удержания и проверить счётчик/кнопки.
  2. Нажать «Начать», дождаться завершения или пропустить.
  3. Быстро вернуться назад и проверить отмену/повторный вход.
- **Ожидаемый результат:** таймер запускается только по явному действию, корректно завершает/пропускается и не создаёт дубль сессии.
- **Фактический результат:** таймер удержания запускался явным тапом, счётчик уменьшался, отдых между подходами пропускался, после трёх подходов открылась сводка. Повторный вход/назад не создал дубль; отмена и завершение сохранялись корректно.
- **Статус:** PASS
- **Дата:** 2026-09-25
- **Заметки:** отдельный контрол паузы/пропуска самого таймера в этом сценарии не отображался; доступен пропуск отдыха, поэтому это отмечено как наблюдение, а не отдельный дефект.

## TC-056 — Бег и велосипед: одиночная тренировка
- **Экран/область:** карточка упражнения → параметры → WorkoutRun
- **Предусловия:** есть встроенные упражнения типов «Бег» и «Велосипед».
- **Шаги:**
  1. Открыть параметры бега/велосипеда, проверить обязательные поля.
  2. Попробовать сохранить пустые и граничные значения.
  3. Ввести валидные параметры, запустить и заполнить результат.
  4. Завершить и проверить запись в истории.
- **Ожидаемый результат:** поля/единицы корректны; обязательные значения валидируются; одиночная сессия сохраняется без падения.
- **Фактический результат:** пустые/невалидные параметры блокировались. Для временного пользовательского упражнения `QA_Bike` введены валидные дистанция и время, одиночная тренировка запущена, результат сохранён, сводка и запись появились в истории; повторное открытие и отмена не привели к падению.
- **Статус:** PASS
- **Дата:** 2026-09-25
- **Заметки:** встроенного упражнения «Велосипед» в seed-наборе нет; проверка типа «Время» выполнена на временном `QA_Bike`, который затем удалён.

## TC-057 — Сводка тренировки и история
- **Экран/область:** завершение тренировки, Главная, История
- **Предусловия:** завершена тренировка из TC-054/056.
- **Шаги:**
  1. Проверить список упражнений, мышцы и сводные метрики.
  2. Вернуться на Главную и открыть «К истории».
  3. Выбрать дату/карточку и открыть детали подходов.
  4. Быстро вернуться назад и повторно открыть детали.
- **Ожидаемый результат:** сводка содержит результаты и мышцы; сессия видна в истории; детали восстанавливают подходы; навигация стабильна.
- **Фактический результат:** итоговый экран показал упражнения, подходы и сводные метрики; мышечная нагрузка отображалась. Сессия появилась в «Последних тренировках» и календаре, детали открывались с параметрами подходов, быстрый BACK/повторное открытие не ломали навигацию.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-058 — Force-stop и checkpoint активной тренировки
- **Экран/область:** устойчивость процесса
- **Предусловия:** активна тренировка с таймером и checkpoint должен быть записан.
- **Шаги:**
  1. Зафиксировать текущие упражнение/подход/таймер.
  2. Выполнить `am force-stop com.example.fitnessappai`.
  3. Запустить приложение и проверить восстановление или корректное сообщение.
  4. Проверить, что данные не дублируются после восстановления.
- **Ожидаемый результат:** приложение стартует без падения; checkpoint восстанавливает состояние либо явно сообщает о потере; история не содержит дублей.
- **Фактический результат:** во время активной тренировки выполнен `am force-stop`; после запуска приложение восстановило тренировку, таймер продолжился с прежнего значения, повторный вход не создал дубль. Однако после «Выйти» из восстановленной тренировки остался пустой экран с одним заголовком «Тренировка» вместо перехода назад/Главную.
- **Статус:** FAIL
- **Дата:** 2026-09-25
- **Заметки:** дефект зарегистрирован в `PLAN.md` как задача 45.7; вероятная причина — статический redirect в `lib/app/router.dart:38-48`, вызываемый обработкой `Выйти` в `lib/features/workout/ui/workout_run_screen.dart:289-340`.

## TC-059 — Замеры: добавление, границы и график
- **Экран/область:** Профиль → Замеры → форма
- **Предусловия:** профиль доступен, есть как минимум одно измерение или чистое состояние.
- **Шаги:**
  1. Открыть «Добавить замер».
  2. Проверить 0, отрицательное, очень большое и пустое значения.
  3. Сохранить корректное значение, проверить текущие значения и график.
  4. Удалить измерение и повторно открыть экран.
- **Ожидаемый результат:** невалидные значения отклоняются/не ломают форму; корректное значение появляется в истории и графике; удаление сохраняется.
- **Фактический результат:** форма корректно отклонила 0, отрицательное и чрезмерное значения; валидный замер 80 кг сохранился, появился в текущих значениях, истории и графике. Запись удалилась через подтверждение и не вернулась после повторного открытия профиля; пустое/оставшееся состояние отображалось корректно.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-060 — Профиль, противопоказания и повторный запуск
- **Экран/область:** Профиль → Противопоказания
- **Предусловия:** профиль доступен из меню/аватара.
- **Шаги:**
  1. Проверить аватар и переход в профиль.
  2. Открыть «Противопоказания», изменить выбор и сохранить.
  3. Перезапустить профиль и проверить сохранение.
  4. Быстро открыть/закрыть экран несколько раз.
- **Ожидаемый результат:** переходы работают, изменения сохраняются, нет пустых экранов и падений.
- **Фактический результат:** из меню открылся профиль с аватаром; экран «Противопоказания» отображался, изменение выбора сохранилось после перезапуска. Повторные быстрые открытия/закрытия не оставляли пустых экранов и падений.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-061 — Прогресс: карточки, периоды, метрика и мышцы
- **Экран/область:** Прогресс
- **Предусловия:** после TC-057 есть завершённая тренировка.
- **Шаги:**
  1. Открыть «Прогресс» и проверить карточки/графики.
  2. Переключить «Неделя/Месяц/Год».
  3. Сменить метрику/упражнение, проверить панель мышц.
  4. Нажать на карточку и быстро вернуться.
- **Ожидаемый результат:** данные и подписи соответствуют периоду; графики не содержат ложных нулевых хвостов; мышечные проценты отображаются; навигация стабильна.
- **Фактический результат:** проверены пустое и заполненное состояния «Прогресс», периоды «Неделя/Месяц/Год», смена метрики/упражнения и панель мышечной нагрузки. Подписи и значения соответствуют выбранному периоду, ложных нулевых хвостов не обнаружено; переходы стабильны.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-062 — История: календарь и детали
- **Экран/область:** История
- **Предусловия:** в текущем месяце есть завершённая сессия.
- **Шаги:**
  1. Открыть календарь, проверить шапку, отметки и текущий месяц.
  2. Нажать день с тренировкой и день без тренировки.
  3. Переключить стрелки/свайпом несколько месяцев и проверить границу.
  4. Открыть детали и вернуться.
- **Ожидаемый результат:** календарь читаем, дни открываются корректно, пустые дни безопасны, месяцы переключаются, детали открываются.
- **Фактический результат:** календарь показывал текущий месяц и отметки; день с завершённой тренировкой открыл список упражнений, день без тренировки — корректное пустое состояние. Переключение месяцев и повторное открытие деталей работали без падений.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-063 — Настройки звука
- **Экран/область:** Настройки → Звук таймеров
- **Предусловия:** экран настроек открыт.
- **Шаги:**
  1. Открыть «Звук таймеров» и проверить текущий звук.
  2. Нажать «Прослушать», проверить play/stop и повторный запуск.
  3. Выбрать другой доступный звук/файл, сохранить и перезапустить экран.
- **Ожидаемый результат:** звук воспроизводится и останавливается; выбор сохраняется; ошибки выбора не роняют приложение.
- **Фактический результат:** выбран `QA_Ring_Classic_02.ogg`; кнопки play/stop работали, повторный запуск не создавал ошибок. Сохранённый путь `/data/user/0/com.example.fitnessappai/cache/file_picker/1790338444235/QA_Ring_Classic_02.ogg` отображался после перезапуска.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-064 — Тема и адаптивная навигация
- **Экран/область:** Настройки/Тема, compact/expanded navigation
- **Предусловия:** приложение работает на стандартном экране.
- **Шаги:**
  1. Переключить светлую/тёмную тему и проверить все открытые секции.
  2. Выполнить `wm size 1800x2800`, открыть rail, toggle меню и пройти пункты.
  3. Вернуть `wm size reset`, проверить drawer и выбранный пункт.
  4. Увеличить системный текстовый масштаб и проверить ключевые экраны.
- **Ожидаемый результат:** тема применяется без рестарта; rail/drawer не ломаются; переход через 840dp сохраняет экран; нет overflow/белых экранов.
- **Фактический результат:** светлая и тёмная темы применились без рестарта; настройки, drawer, bottom bar и rail отображались корректно. При `wm size 3000x2800` (1000dp) rail был expanded, toggle и переходы работали; после `wm size reset` вернулась compact-навигация. При `font_scale=1.3` ключевые экраны не переполнялись.
- **Статус:** PASS
- **Дата:** 2026-09-25
- **Заметки:** сценарий с `wm size 1800x2800` изначально дал ширину около 600dp и поэтому не активировал rail; для проверки breakpoint 840dp использован `3000x2800` (1000dp), после чего размер возвращён reset.

## TC-065 — Синхронизация: экспорт и импорт
- **Экран/область:** Настройки → Синхронизация
- **Предусловия:** есть упражнение, программа и измерение.
- **Шаги:**
  1. Нажать «Поделиться» и проверить системный share-лист/файл.
  2. Нажать «Сохранить в файлы» и проверить системный SAF-экран или отмену.
  3. Нажать «Импортировать БД», проверить предупреждение, отмену пикера и ошибку несовместимого файла.
  4. Повторно открыть настройки после отмены/ошибки.
- **Ожидаемый результат:** экспорт создаёт файл/делится без падения; импорт отменяется безопасно; несовместимый файл отклоняется с понятной ошибкой; данные не повреждаются.
- **Фактический результат:** «Поделиться» открыл share sheet с `fitnessappai_export.sqlite`; «Сохранить в файлы» завершился созданием `fitnessappai_backup.sqlite`. Импорт с отменой пикера не изменил данные; несовместимый `.ogg` отклонён с сообщением «Файл не является базой данных FitnessAppAI».
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-066 — Уведомления и точные будильники
- **Экран/область:** Настройки → Уведомления
- **Предусловия:** `POST_NOTIFICATIONS` отозван, точные будильники могут быть выключены.
- **Шаги:**
  1. Открыть экран и нажать «Разрешить уведомления» в системном диалоге.
  2. Отозвать разрешение через `pm revoke`, повторить запрос/отказ.
  3. Нажать «Настроить точные будильники», проверить только системный экран.
  4. После выдачи проверить статус и планирование без падения.
- **Ожидаемый результат:** запрашиваются только соответствующие разрешения; отказ не ломает экран; после выдачи статусы обновляются; будильники открываются отдельно.
- **Фактический результат:** системный диалог уведомлений запрашивал только `POST_NOTIFICATIONS`; отзыв разрешения и повторный запрос не ломали экран. Точные будильники были отключены через UID app-op, кнопка открыла только Android «Alarms & reminders»; после включения тумблера и BACK отображалось «Точные будильники включены», планирование не падало.
- **Статус:** PASS
- **Дата:** 2026-09-25
- **Заметки:** для проверки UID app-op использована команда `cmd appops set --uid`; обычный package-level режим не изменил системный API. Финальный статус и UID permission восстановлены в `allow`.

## TC-067 — О приложении и проверка обновления
- **Экран/область:** Настройки → О приложении
- **Предусловия:** экран настроек открыт.
- **Шаги:**
  1. Открыть «О приложении», проверить версию и лицензии/ссылки.
  2. Нажать «Проверить обновление» без изменения сети.
  3. Дождаться результата, вернуться и повторить быстрый тап.
- **Ожидаемый результат:** экран открывается; сетевая ошибка показывается понятно; приложение не падает и не зависает.
- **Фактический результат:** экран «О приложении» показал `Версия 1.0.22`; «Проверить обновление» завершилось сообщением «Установлена актуальная версия», без падения или зависания. Отдельные кнопки лицензий/ссылок в текущей версии не отображались; это не входит в критерии задачи 14.3, лицензия находится в репозитории/README.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-068 — Стресс навигации и жизненного цикла
- **Экран/область:** приложение целиком
- **Предусловия:** заполнены упражнение, программа и настройки.
- **Шаги:**
  1. Быстро чередовать вкладки, BACK, Home и возврат в приложение 10 раз.
  2. Менять тему во время открытых экранов.
  3. Менять ширину окна туда-обратно во время навигации.
  4. Проверить pid, logcat и визуальное состояние после серии.
- **Ожидаемый результат:** нет assertion, белых экранов, дублей навигации или падений; выбранные данные не теряются.
- **Фактический результат:** выполнено 10 циклов быстрых тапов, BACK, Home и возврата, смены темы и ширины окна. PID сохранялся; выбранные данные не терялись, дубли навигации, белые экраны, `FATAL EXCEPTION`, assertions и overflow-записи в logcat не обнаружены.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-069 — Визуальная регрессия UI-кита
- **Экран/область:** изменённые после этапа 45 экраны
- **Предусловия:** открыть Home, форму упражнения, форму замера, настройки, подготовку/параметры.
- **Шаги:**
  1. Проверить `AppGradientButton` на primary CTA: заливка, размер, disabled/busy/ошибка.
  2. Проверить `AppSectionHeader/AppSection` и отступы на нескольких экранах.
  3. Проверить delete-CTA, который не должен быть градиентным.
  4. Переключить тему и ширину экрана.
- **Ожидаемый результат:** primary CTA единообразны и читаемы; заголовки не дублируются; delete-действия визуально отделены; нет overflow и обрезанных текстов.
- **Фактический результат:** на Главной, форме упражнения, форме замера, Настройках, подготовке и экранах параметров проверены градиентные primary CTA, `AppSectionHeader/AppSection`, карточки/empty states и отдельное оформление delete-действий. Дублирующихся заголовков, overflow и обрезанных текстов не обнаружено. `flutter test test/uikit` — 34 теста; 7 целевых widget-файлов экранов — 113 тестов, все прошли.
- **Статус:** PASS
- **Дата:** 2026-09-25
- **Заметки:** визуальная проверка delete-CTA выявила функциональный дефект TC-048 (кнопка удаления у встроенного упражнения), зарегистрированный отдельно.

## TC-070 — Сохранение данных после перезапуска
- **Экран/область:** сквозная проверка данных
- **Предусловия:** созданы собственное упражнение, программа, измерение и изменена тема.
- **Шаги:**
  1. Зафиксировать список упражнений, программу, замер и настройки.
  2. Выполнить force-stop и запустить приложение заново.
  3. Повторно открыть все экраны и сравнить состояние.
  4. Проверить отсутствие дублей seed/программ/сессий.
- **Ожидаемый результат:** пользовательские данные и настройки сохраняются; seed-упражнения не дублируются; экраны открываются без ошибок.
- **Фактический результат:** до cleanup после `am force-stop` и повторного запуска восстановились активная `QA_Program_v2`, пользовательское `QA_Edit`, замер 80 кг, сохранённый звук `QA_Ring_Classic_02.ogg` и светлая тема. Seed-упражнения и завершённые сессии не дублировались; после проверки временные программа и упражнение были удалены намеренно.
- **Статус:** PASS
- **Дата:** 2026-09-25

## TC-071 — Детали упражнения: ретест удаления встроенного (после фикса 45.6)
- **Экран/область:** Упражнения → детали (ретест TC-048)
- **Предусловия:** собрана debug-сборка из main с фиксом 45.6 (APK `dff4c588…d7552`), эмулятор `Pixel_10_Pro`.
- **Шаги:**
  1. Открыть «Упражнения» и встроенную «Боковую планку» — проверить описание, технику, ошибки, мышцы.
  2. Проверить, что кнопок «Редактировать» и «Удалить» у встроенного упражнения нет.
  3. Создать собственное упражнение `QA_Retest_45_6` с мышцей «Пресс» и открыть его детали.
  4. Убедиться, что у собственного упражнения есть «Редактировать» и «Удалить»; «Удалить» открывает диалог подтверждения.
  5. Быстро вернуться назад и повторить переход несколько раз.
- **Ожидаемый результат:** у встроенного упражнения отсутствуют кнопки правки/удаления; у собственного — обе есть, удаление подтверждается диалогом; навигация стабильна.
- **Фактический результат:** у встроенной «Боковой планки» отображались описание, техника, ошибки и мышцы, кнопок «Редактировать» и «Удалить» не было. У собственного `QA_Retest_45_6` (мышца «Пресс») обе кнопки отображались; «Удалить» открыло подтверждение удаления. Навигация стабильна.
- **Статус:** PASS
- **Дата:** 2026-09-25
- **Заметки:** ретест дефекта TC-048 после фикса 45.6 (`exercise_detail_screen.dart`, `if (canEdit)`). Дефект закрыт; статус TC-048 сохранён как FAIL (историческая запись).

## TC-072 — Force-stop и checkpoint: ретест выхода (после фикса 45.7)
- **Экран/область:** устойчивость процесса (ретест TC-058)
- **Предусловия:** собрана debug-сборка из main с фиксом 45.7 (APK `dff4c588…d7552`), эмулятор `Pixel_10_Pro`.
- **Шаги:**
  1. В программе `QA_TC058` завершить первый подход при активном отдыхе (6 с) до записи checkpoint.
  2. Убедиться, что `workout_checkpoint.json` существует.
  3. Выполнить `am force-stop com.example.fitnessappai`; запустить приложение.
  4. Проверить восстановление упражнения/подхода/таймера; нажать BACK → диалог «Выйти из тренировки?» → подтвердить.
  5. Проверить, что открылась непустая Главная/план и нет пустого экрана «Тренировка» без перехода.
- **Ожидаемый результат:** checkpoint восстанавливает состояние; после «Выйти» открывается непустой Главный экран; дублей истории нет.
- **Фактический результат:** checkpoint существовал (595 байт); после `am force-stop` и холодного запуска восстановились `QA_Retest_45_6`, подход 2 из 2, таймер тренировки 50 с. BACK открыл «Выйти из тренировки?», после подтверждения открылась непустая Главная с активной программой и одной записью `QA_TC058`. Пустого экрана и дубля нет.
- **Статус:** PASS
- **Дата:** 2026-09-25
- **Заметки:** ретест дефекта TC-058 после фикса 45.7 (one-shot redirect в `router.dart`). Дефект закрыт; статус TC-058 сохранён как FAIL (историческая запись).

## Этап 46 — набор сценариев (добавлен коротким прогоном 2026-09-26)

Сценарии ниже добавлены под изменения этапа 46 (ветка `task/46-release-notification-resources`).
**Прогон не выполнялся** — это заготовки по протоколу (шаги + ожидаемый результат), статусы `SKIP`.
Две важные оговорки по окружению, действующие для всего набора:

- **Release-APK в этом окружении собирается** (после `flutter clean` и переключения Flutter на JDK 21
  через `flutter config --jdk-dir`): проверено `flutter build apk --release` → `app-release.apk` 77.8 МБ.
  Статическая часть 46.1 (TC-073) этим уже закрыта — см. «Фактический результат» ниже. **Прогон на
  эмуляторе не выполнялся**, и debug-сборка для 46.1 непригодна: debug **не проходит** resource shrinking,
  поэтому дефект в эмуляторе с debug-APK не воспроизводится принципиально. Функциональные TC-074 и
  остальные эмуляторные сценарии набора остаются непрогнанными.
- `flutter_local_notifications` адресует иконку/звук **из Dart** через `Resources.getIdentifier`,
  поэтому статическая проверка APK — единственный надёжный признак, что ресурс не вырезан;
  сам факт прихода уведомления в debug-сборке ничего не доказывает.

Общие команды набора:

```sh
# будильники, зарегистрированные приложением
adb -s emulator-5554 shell dumpsys alarm | grep -A4 com.example.fitnessappai
# фактически показанные уведомления (проверить иконку/звук/канал)
adb -s emulator-5554 shell dumpsys notification --noredact | grep -iA6 fitnessappai
# диагностика этапа 46 (46.2) — префиксы [ReminderService]/[bootstrap]/[SyncService]/[main]
adb -s emulator-5554 logcat -d | grep -E "ReminderService|\[bootstrap\]|\[SyncService\]|\[main\]"
# ресурсы release-APK (AAPT2 в release сокращает пути, unzip -l по именам не работает)
~/Android/Sdk/build-tools/*/aapt2 dump resources app-release.apk | grep -E "ic_stat_launcher|raw/notification"
# разрешения собранного APK
~/Android/Sdk/build-tools/*/aapt2 dump permissions app-release.apk
```

Подготовка данных, общая для TC-075…TC-082: программа `QA_46` с двумя днями, у каждого дня
включён тумблер «Напоминать» и задано время (день 1 — сегодняшний день недели,
день 2 — день недели «послезавтра»), `POST_NOTIFICATIONS` разрешено, точные будильники включены,
программа сохранена (будильники планируются при сохранении конструктора программы).
Удобство: чтобы не ждать неделю до срабатывания, для проверок факта прихода сдвинуть системное
время к `when` из `dumpsys alarm` (`adb root` / `su 0 date`), а если root на образе недоступен —
проверять регистрацию будильника в `dumpsys alarm`, а факт прихода — на release-сборке.

---

## TC-073 — Release-APK: иконка и звук уведомлений не вырезаны шринковщиком (46.1)

- **Экран/область:** сборка/release-ресурсы (эмулятором не проверяется)
- **Предусловия:** есть окружение, где `flutter build apk --release` собирается; в проекте
  присутствует `android/app/src/main/res/raw/keep.xml` с `tools:keep="@drawable/ic_stat_launcher,@raw/notification"`.
- **Шаги:**
  1. `flutter build apk --release` (после `flutter clean`, при необходимости `JAVA_HOME` на JDK 17/21).
  2. `~/Android/Sdk/build-tools/*/aapt2 dump resources build/app/outputs/flutter-apk/app-release.apk | grep -E "ic_stat_launcher|raw/notification"` — оба ресурса найдены.
  3. Контрольная сборка: временно убрать `keep.xml`, пересобрать release, повторить grep — ни одного из ресурсов нет.
  4. Вернуть `keep.xml`, пересобрать, убедиться, что размер APK контрольной сборки меньше (шринковка действительно работает, т.е. проверка не «зелёная» из-за отключённой шринковки).
  5. Дополнительно: `aapt2 dump xmltree --file AndroidManifest.xml <apk> | grep -i default_notification_icon` — плейсолдер в манифесте не подставляется (ожидаемо, ресурс держит `keep.xml`, а не плейсолдер).
- **Ожидаемый результат:** в release-APK присутствуют и `drawable/ic_stat_launcher`, и `raw/notification`;
  контрольная сборка без `keep.xml` их не содержит — то есть `keep.xml` реально работает, а не совпадение.
- **Фактический результат:** **PASS (статическая проверка, 2026-09-26).** `flutter build apk --release`
  → `build/app/outputs/flutter-apk/app-release.apk` (77.8 МБ). `aapt2 dump resources` показывает
  `drawable/ic_stat_launcher` и `raw/notification`. Контрольная пересборка **без** `keep.xml`: оба
  ресурса отсутствуют, APK 77.1 МБ — то есть `keep.xml` действительно держит ресурсы, а не совпадение.
  Замечание к команде: в release AAPT2 сокращает пути, поэтому `unzip -l | grep ic_stat_launcher`
  ничего не находит — проверять нужно именно через `aapt2 dump resources`.
- **Статус:** PASS (статическая часть); эмуляторный прогон не выполнялся
- **Дата:** 2026-09-26
- **Дополнение (2026-09-26, после публикации v1.0.24-beta):** статическая часть перепроверена на
  **опубликованном CI-артефакте**, а не только на локальной сборке: `gh release download v1.0.24-beta`
  → `app-release.apk` 77 795 550 байт, `aapt2 dump resources` показывает `drawable/ic_stat_launcher`
  и `raw/notification`; `aapt2 dump permissions` — `IGNORE_BATTERY_OPTIMIZATIONS` отсутствует;
  `aapt2 dump badging` — `versionName=1.0.24-beta`, `versionCode=29`. В v1.0.23-beta не было ни
  `keep.xml`, ни `proguard-rules.pro`, то есть до этого релиза дефект в публикации присутствовал.
  Подробности — `PLAN.md`, задача 46.9.
- **Заметки:** сценарий закрыт для release-APK. Осталось проверить то же самое на устройстве
  (TC-074) — debug-сборка для этого непригодна. Перед публикацией релиза TC-074 прогнать обязательно.
  **Факт (2026-09-26):** релиз v1.0.24-beta опубликован, а TC-074…TC-084 остались `SKIP` — обязательный
  предрелизный гейт не выполнен. Заведена задача `PLAN.md` 46.10 (статус `[ ]`); e2e на Linux desktop
  (20/20) этих сценариев не покрывает — они Android-only..

## TC-074 — Release на устройстве: уведомление приходит с иконкой и звуком (46.1)

- **Экран/область:** шторка уведомлений Android (эмулятором с debug-APK не проверяется)
- **Предусловия:** установлен **release**-APK (не debug); программа `QA_46` с включёнными напоминаниями; `POST_NOTIFICATIONS` разрешено.
- **Шаги:**
  1. Снять `dumpsys alarm` — будильники зарегистрированы, `when` совпадает с ожидаемым временем.
  2. Дождаться срабатывания (либо сдвинуть системное время к `when`).
  3. Развернуть шторку: в уведомлении есть **small icon** приложения (не пустой квадрат), заголовок = имя программы, текст = «Тренировка: день N».
  4. Проверить `dumpsys notification --noredact | grep -iA6 fitnessappai` — у notification задан `sound` из `raw/notification`, канал `workout_reminders` importance=high, вибрация включена.
  5. Отключить звук/канал в системных настройках уведомлений → повторить; вернуть как было.
- **Ожидаемый результат:** уведомление **приходит** вовремя, с видимой иконкой и со звуком; в системном
  дампе у notification видны `sound` и зарегистрированный канал. Молчаливого «ничего не произошло» нет.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** непрогнан — требуется установленный **release**-APK на устройстве/эмуляторе (debug не
  годится: он не проходит resource shrinking, поэтому дефект на нём не воспроизводится). Сборка
  release в этом окружении уже проверена и проходит (см. TC-073), дело только в прогоне на устройстве.
  Это сценарий-регресс на исходный пользовательский дефект («уведомления не приходят в release»):
  до 46.1 он падал бы молча, с 46.1 обязан приходить.

## TC-075 — Ошибка планирования видна в logcat при отключённых уведомлениях (46.2)

- **Экран/область:** Настройки → Уведомления + logcat
- **Предусловия:** `POST_NOTIFICATIONS` разрешено; программа `QA_46` сохранена с напоминаниями; logcat очищен.
- **Шаги:**
  1. Отозвать разрешение: `adb shell pm revoke com.example.fitnessappai android.permission.POST_NOTIFICATIONS`.
  2. Свернуть приложение (Home) и вернуться — срабатывает `didChangeAppLifecycleState(resumed)` → `rescheduleAll()`.
  3. Открыть программу `QA_46` в конструкторе и **сохранить** её (напоминания планируются заново).
  4. `adb logcat -d | grep -E "\[ReminderService\]|\[bootstrap\]|\[main\]"` — в выводе есть строка вида `[ReminderService] Уведомления отключены пользователем, пропуск планирования дня <id>`.
  5. Убедиться, что приложение не упало: `pidof com.example.fitnessappai` не пуст, в logcat нет `FATAL EXCEPTION`/`AndroidRuntime`.
  6. Вернуть разрешение, повторить п.2–3 — в logcat появляется планирование без сообщения об ошибке.
- **Ожидаемый результат:** причина сбоя планирования **попадает в logcat** (а не теряется, как раньше в
  release, где `dart:developer log()` вырезался AOT); приложение продолжает работать; после возврата
  разрешения планирование проходит и ошибок нет.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** в debug-сборке сообщение видно и до 46.2; **ценность сценария — в release**, где
  `developer.log()` вырезается, а `debugPrint` доходит до logcat. Поэтому финальную проверку нужно
  повторить на release-APK (TC-074/TC-075 вместе), иначе регресс 46.2 не обнаруживается.

## TC-076 — Сбой перепланирования одного дня не срывает остальные дни (46.2)

- **Экран/область:** Настройки → Уведомления / Программы + logcat
- **Предусловия:** программа `QA_46` с **двумя** днями, у обоих включены напоминания (id дней различаются).
- **Шаги:**
  1. Снять `dumpsys alarm` — зарегистрированы будильники обоих дней.
  2. Спровоцировать ошибку планирования **одного** дня (например, отозвать `POST_NOTIFICATIONS`, отключить точные будильники через `cmd appops set --uid`, отключить канал уведомлений в системных настройках) и вызвать `rescheduleAll()` — вернуться из фона.
  3. В logcat найти `[ReminderService] Не удалось перепланировать напоминание дня <id>` — в сообщении конкретный `programDayId`.
  4. Убедиться, что второй день **не** пострадал: после снятия проблемы и повторного возврата из фона в `dumpsys alarm` снова присутствуют будильники **обоих** дней.
  5. Проверить отсутствие падения и `FATAL EXCEPTION`.
- **Ожидаемый результат:** ошибка одного дня логируется с его id, цикл `rescheduleAll()` **продолжается**,
  остальные дни перепланируются; после устранения проблемы все будильники возвращаются.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** до 46.2 `try/catch` был только вокруг `rescheduleAll()` целиком, поэтому одно
  «битое» напоминание тихо оставляло весь список неактуальным. Проверять именно **частичный** успех.

## TC-077 — Напоминания перепланируются при возвращении из фона (46.3)

- **Экран/область:** жизненный цикл приложения + системные будильники
- **Предусловия:** `QA_46` сохранена с напоминаниями; `POST_NOTIFICATIONS` разрешено; точные будильники **включены**.
- **Шаги:**
  1. Зафиксировать `dumpsys alarm | grep -A4 com.example.fitnessappai` — есть будильники, `when` соответствует ближайшему дню недели.
  2. Свернуть приложение (`input keyevent 3`), **не** закрывая его.
  3. В фоне отключить точные будильники: `adb shell cmd appops set --uid com.example.fitnessappai SCHEDULE_EXACT_ALARM ignore` (или тумблер в Android «Alarms & reminders»).
  4. Вернуться в приложение (тап по иконке / `am start`) — срабатывает `resumed`.
  5. Снова снять `dumpsys alarm`: будильники пересчитаны (учитывая новое состояние разрешений — `inexactAllowWhileIdle` вместо `exact`), время/режим изменились относительно п.1.
  6. В logcat нет ошибок перепланирования; `pidof` приложения не меняется, экрана ошибки нет.
  7. Вернуть точные будильники и проверить, что после следующего `resumed` режим снова точный.
- **Ожидаемый результат:** при возврате из фона напоминания **автоматически перепланируются** под
  текущее состояние разрешений/часового пояса; пользователю не нужно перезапускать приложение; ошибок нет.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** до 46.3 обработчика `resumed` в проекте не было, будильники «залипали» до
  следующего холодного старта. Ключевой признак регресса: возврат из фона **не меняет** расписание.

## TC-078 — Стресс возвратов из фона и смена часового пояса: нет дублей и падений (46.3)

- **Экран/область:** жизненный цикл + уведомления (стресс)
- **Предусловия:** `QA_46` с двумя напоминаниями; `POST_NOTIFICATIONS` разрешено.
- **Шаги:**
  1. 10 раз подряд: `input keyevent 3` (Home) → возврат тапом по иконке приложения.
  2. Сменить часовой пояс на устройстве: `adb shell settings put global auto_time 0` и `adb shell svc data enable`/системные настройки → сменить пояс на 2–3 часа, затем вернуть `auto_time 1`.
  3. Сменить дату/время на границе дня недели, вернуть как было.
  4. После каждого цикла — `dumpsys alarm` и `dumpsys notification`: посчитать, сколько будильников зарегистрировано на каждый день.
  5. `adb logcat -d | grep -iE "FATAL|AndroidRuntime|Another exception was thrown"` — падений нет.
  6. Проверить, что `pidof` стабилен (приложение не перезапускается само), а на экране нет пустого/белого состояния.
- **Ожидаемый результат:** после серии Home/возврат и смены часового пояса **количество будильников
  на день остаётся равным 1** (никаких дублей и «осиротевших» копий), время срабатывания
  пересчитывается по новому поясу; приложение не падает и не дублирует уведомления.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** `rescheduleAll()` теперь вызывается на **каждом** `resumed`, поэтому главный риск —
  накопление дубликатов/перепланирований; проверять именно счётчик будильников, а не «вроде работает».

## TC-079 — Тап по уведомлению открывает день тренировки (тёплый старт) (46.4)

- **Экран/область:** шторка уведомлений → экран дня тренировки
- **Предусловия:** release или debug-сборка с этапа 46; `QA_46` с напоминаниями; уведомление получено
  (при необходимости время сдвинуто к срабатыванию, см. подготовку набора).
- **Шаги:**
  1. **Не** закрывая приложение, свернуть его (Home), дождаться уведомления, развернуть шторку.
  2. Тапнуть по уведомлению **дня 2** (в тексте — «Тренировка: день 2»).
  3. Проверить `uiautomator dump`: открыт экран **дня тренировки** программы `QA_46` (заголовок дня, упражнения дня 2), а не Главная и не список программ.
  4. Нажать BACK — вернуться на предыдущий экран, без «чёрного»/пустого состояния.
  5. Повторить для дня 1; затем тапнуть по уведомлению 3 раза подряд быстро.
- **Ожидаемый результат:** тап по уведомлению открывает **именно тот день** программы, указанный в
  payload (`programDayId` → `/programs/:programId/day/:dayIndex`); повторные тапы не ломают навигацию.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** до 46.4 `onDidReceiveNotificationResponse` не был зарегистрирован, а `payload`
  (`reminder_service.dart`) был мёртвым — тап по уведомлению не делал ничего.

## TC-080 — Холодный старт тапом по уведомлению открывает нужный день (46.4)

- **Экран/область:** запуск из уведомления (`getNotificationAppLaunchDetails`)
- **Предусловия:** то же, что TC-079; уведомление висит в шторке.
- **Шаги:**
  1. Убить процесс: `adb shell am force-stop com.example.fitnessappai` (уведомление при этом может остаться в шторке — если система его снимет, шаг 2 выполнить сразу после получения уведомления, не касаясь приложения).
  2. Тапнуть по уведомлению дня 1.
  3. Дождаться полной загрузки (~8 с), снять `uiautomator dump`.
  4. Проверить: открылся экран дня 1 программы `QA_46`; `pidof` — новый процесс; в logcat нет падений.
  5. Повторить п.1–4, тапнув по уведомлению **дня 2**, и убедиться, что открылся день 2 (а не день 1 и не предыдущий экран).
- **Ожидаемый результат:** payload, пришедший **до** инициализации плагина, корректно откладывается и
  обрабатывается после сборки UI — холодный старт из уведомления открывает нужный день, а не Главную.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** критичный сценарий 46.4 — очередь `_pendingProgramDayId` в `ReminderService`. Проверять
  именно холодный старт (с `force-stop`), а не только тап при живой UI: без очереди payload терялся бы.
  Логика холодного старта **покрыта автотестами** — группа «холодный старт из уведомления»
  в `test/core/notifications/reminder_service_test.dart` (5 тестов: доставка payload при ещё не
  назначенном обработчике, доставка сразу при назначенном, обычный запуск без уведомления,
  идемпотентность повторного `initialize()`, битый payload). TC-080 остаётся как подтверждение
  сквозного поведения на устройстве: реальный порядок «тап → запуск процесса →
  `getNotificationAppLaunchDetails` → UI» в юнит-тесте не воспроизводится.

## TC-081 — Тап по уведомлению удалённого/битого дня: без падения, есть диагностика (46.4)

- **Экран/область:** запуск из уведомления + logcat
- **Предусловия:** есть висящее уведомление; под рукой — способ удалить соответствующий день программы
  (удаление программы/дня в конструкторе) и способ получить уведомление с невалидным payload.
- **Шаги:**
  1. Получить уведомление дня 1, **не открывая** приложение из него.
  2. Открыть программу обычным способом, удалить день 1 (или всю программу) и сохранить.
  3. Вернуться в шторку и тапнуть по **старому** уведомлению дня 1.
  4. Проверить: приложение стартует на Главной/списке программ, экран не пустой, `pidof` жив, в logcat нет `FATAL`.
  5. В logcat найти диагностику 46.2 вида `[main] Переход из уведомления не выполнен: день=<id>, найден=null, ...`.
  6. Отдельно: тапнуть по уведомлению, payload которого не число (создать его вручную через `adb shell cmd notification post` с битым `extra`/payload либо изменить БД), убедиться, что перехода не происходит и падения нет.
- **Ожидаемый результат:** тап по «осиротевшему» или битому уведомлению **не падает** и не ведёт в
  никуда: приложение открывается на обычном экране, а причина невыполненного перехода видна в logcat.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** проверяет `int.tryParse(payload) → null → return` и ветку `target == null` в
  `_openReminderDay`; раньше эти случаи просто молча ничего не делали.

## TC-082 — После импорта БД «сиротские» будильники дней отменены (46.5)

- **Экран/область:** Настройки → Синхронизация (экспорт/импорт) + `dumpsys alarm`
- **Предусловия:** программа `QA_46` с двумя днями и напоминаниями; `POST_NOTIFICATIONS` разрешено;
  сделан экспорт БД (`QA_46_Export.sqlite`).
- **Шаги:**
  1. Снять `dumpsys alarm` — зарегистрированы будильники дней 1 и 2.
  2. Изменить БД **вне приложения**: в подготовленной копии экспорта удалить день 2 (SQLite-редактор либо импорт заведомо «чужой»/более старой БД без этого дня).
  3. Настройки → Синхронизация → «Импортировать БД» → выбрать изменённый файл → подтвердить перезапуск.
  4. Дождаться полного перезапуска приложения (диалог перезапуска), снять `dumpsys alarm`.
  5. Проверить: будильник **исчезнувшего** дня отсутствует, будильник сохранившегося дня присутствует, время пересчитано.
  6. Проверить logcat: нет ошибок; в идеале виден порядок `cancelAll → initialize → rescheduleAll` (в debug — по сообщениям, в release — по отсутствию дублей).
  7. Повторить импорт **той же** БД ещё раз (идемпотентность): количество будильников не должно вырасти.
- **Ожидаемый результат:** после импорта будильники дней, **исчезнувших** из импортированной БД,
  отменены — «напоминаний о несуществующих тренировках» не остаётся; будильники сохранившихся дней
  работают; повторный импорт не плодит дубли.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** до 46.5 `_rebuildAfterImport` делал `initialize + rescheduleAll` без `cancelAll()`,
  поэтому удалённые дни продолжали «звонить». Проверять именно **отсутствие** старого будильника.

## TC-083 — Экран уведомлений на не-Android честно показывает «не выдано» (46.6)

- **Экран/область:** Настройки → Уведомления на не-Android платформе
- **Предусловия:** ветка/сборка без Android: `flutter run -d linux` (или `flutter test
  test/features/settings` — widget-тест экрана настроек), версия с этапа 46.
- **Шаги:**
  1. Запустить приложение на не-Android платформе (Linux desktop) и открыть Настройки → Уведомления.
  2. Снять `uiautomator dump`/скриншот: статус уведомлений и точных будильников.
  3. Тапнуть по действию плитки уведомлений (если оно есть) — убедиться, что приложение не падает и не открывает системный диалог Android.
  4. Проверить, что не показано ложное «Уведомления включены» / «Точные будильники включены», когда ничего не выдано.
  5. Прогнать widget-тесты экрана настроек: `flutter test test/features/settings`.
- **Ожидаемый результат:** на не-Android обе плитки показывают состояние «отключено/не выдано» с
  понятным действием, а не ложное «всё включено» (до 46.6 `checkPermissions()` возвращал `true/true`);
  тап по действию безопасен, тесты зелёные.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** проверяется НЕ на Android-эмуляторе — на Android реальный статус всегда корректен.
  Исполнение — Linux-десктоп или widget-тест; в эмуляторе сценарий помечается `BLOCKED`.

## TC-084 — Манифест собранного APK: `IGNORE_BATTERY_OPTIMIZATIONS` удалён (46.7)

- **Экран/область:** манифест/разрешения собранного APK + первый запуск
- **Предусловия:** собрана хотя бы debug-сборка (`app-debug.apk` допустим — манифест merge одинаков для debug/release, кроме `INTERNET`).
- **Шаги:**
  1. `~/Android/Sdk/build-tools/*/aapt2 dump permissions build/app/outputs/flutter-apk/app-debug.apk`.
  2. Проверить отсутствие `android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` (и `IGNORE_BATTERY_OPTIMIZATIONS`).
  3. Проверить, что на месте `POST_NOTIFICATIONS`, `SCHEDULE_EXACT_ALARM`, `RECEIVE_BOOT_COMPLETED`, `WAKE_LOCK`, `VIBRATE`, `FOREGROUND_SERVICE*`.
  4. Установить APK на эмулятор, запустить с чистым состоянием, пройти все экраны и нажать кнопки уведомлений/точных будильников — ни один системный диалог не должен требовать оптимизацию батареи.
  5. Убедиться, что `dumpsys deviceidle whitelist | grep fitnessappai` не меняется сам по себе и приложение не просит оптимизацию при старте/напоминаниях.
- **Ожидаемый результат:** мёртвое разрешение оптимизации батареи **удалено** из собранного APK, все
  рабочие разрешения на месте, приложение не инициирует запросы оптимизации батареи и работает без них.
- **Фактический результат:** (заполняется после прогона)
- **Статус:** SKIP
- **Дата:**
- **Заметки:** в отличие от 46.1 этот сценарий **проверяется в debug** — состав разрешений в манифесте
  от build type не зависит.
