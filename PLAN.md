# План разработки FitnessAppAI (Flutter, Android 12+, offline-first)

> Файл с планом и отслеживанием статусов реализации.
> Обновление статусов: для каждой задачи ставьте `[x]` в строке **Статус** и заполняйте таблицу прогресса в конце файла.

---

## 0. Решения по проекту

| Вопрос | Решение |
|---|---|
| Интеграция LLM | Отложена после MVP; в MVP — только интерфейс генерации (задача 6.1) |
| GGUF-модель | Определяется на Этапе 6 (кандидат — Qwen2.5 1.5B/3B) |
| Начальная база упражнений | Экспорт из открытой БД **wger** + адаптация на русский в JSON-ассет |
| Анимации упражнений | **Animated WebP**, хранение **файлами** в app storage (путь в БД) |
| Источник анимаций в MVP | Бандл-ассеты для стартовой базы + добавление вручную для кастомных |
| Схема мускулатуры | **CustomPainter**: ~15 групп мышц, вид спереди/сзади, Path-регионы |
| Расписание | **Жёсткое**: день недели закреплён за тренировочным днём; статусы выполнение/перенос/пропуск |
| Бег | Ручной ввод дистанции и времени, GPS не используется |
| Прерванная тренировка | Отбрасывается (не сохраняется) |
| Противопоказания | Теги на упражнении + профиль пользователя (предупреждения, решение за пользователем) |
| Метрики тела | Рост, вес + 8 обхватов (шея, грудь, талия, бёдра, бицепс, предплечье, бедро, икра), всё опционально |
| Напоминания | По тренировочным дням: своё время на день, задаётся пользователем, можно отключить |
| Синхронизация | MVP: ручной экспорт/импорт файла БД; интерфейс `SyncService` для будущего облака |
| CI | `flutter analyze` + `dart format --set-exit-if-changed` + `flutter test` + `flutter build apk --debug` |
| Название / applicationId | FitnessAppAI / `com.example.fitnessappai`; `minSdk = 31` |

## 1. Окружение и блокеры

- **URL GitHub-remote не передан.** Без него невозможны PR в main — согласовать или создать через `gh repo create` перед задачей 0.1.
- Flutter 3.44.8 / Dart 3.12.2 установлены. Окружение — WSL2, Android SDK на Windows-диске. AVD для Android 12+ ещё не создан (нужен для интеграционных тестов и проверки адаптива).
- Все flutter-команды выполняются с `workdir: fitnessappai/`.
- Сгенерированные Drift-файлы (`*.g.dart`) коммитятся в репозиторий (CI их не перегенерирует).

## 2. Git-процесс

- Каждая задача = ветка `task/<NN>-<slug>` от `main` → PR в `main` → зелёный CI → merge (строго в порядке зависимостей).
- Все проверки — в CI; локально можно запускать те же команды.

## 3. CI workflow (`.github/workflows/ci.yml`)

```yaml
name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: stable
          channel: stable
          cache: true
      - run: flutter pub get
      - run: dart format --output=none --set-exit-if-changed lib test tool integration_test
      - run: flutter analyze --fatal-infos
      - run: flutter test
      - run: flutter build apk --debug
      - uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
```

## 4. Стек пакетов

- **Runtime:** `drift`, `drift_flutter`, `signals`, `signals_flutter`, `go_router`, `fl_chart`, `intl`, `uuid`, `collection`, `path_provider`, `path`, `wakelock_plus`, `flutter_local_notifications`, `timezone`, `file_picker`, `share_plus`.
- **Dev:** `drift_dev`, `build_runner`, `flutter_lints`, `mocktail`, `fake_async`, `integration_test` (SDK).
- Анимированный WebP рендерится нативным кодеком Flutter (без пакетов). Схема мышц — CustomPainter (без пакетов).
- `flutter_signals` на pub.dev — нерабочая заглушка; используем `signals` + `signals_flutter` (v7.x).

## 5. Структура проекта

```
fitnessappai/
  lib/
    main.dart
    app/                # app.dart, router.dart, theme/, responsive/
    core/
      database/         # app_database.dart, tables/, converters/
      di/               # service_locator.dart
      l10n/             # app_ru.arb, gen_l10n
      media/            # media_store.dart, media_cache.dart
      utils/
    features/
      exercises/        # data/ domain/ ui/
      programs/         # data/ domain/ ui/
      workout/          # data/ domain/ ui/
      progress/         # data/ domain/ ui/
      profile/          # data/ domain/ ui/
      llm/              # domain/ (интерфейс + DTO + парсер)
      sync/             # domain/ + ui/
  assets/
    data/exercises_seed.json
    exercises/          # *.webp (анимации)
  tool/                 # export_wger.dart
  integration_test/
```

## 6. Схема БД (Drift)

- `app_meta` — key/value: версия данных, флаг `seeded`.
- `muscle_groups` — id, key, labelRu, view (front/back), regionKey (для Path-региона диаграммы). Справочник ~15 групп.
- `contraindication_tags` — id, key, labelRu; `exercise_contraindications` — M2M.
- `user_profiles` — одна строка (id=1); `user_contraindications` — M2M.
- `exercises` — id, name, description, instructions, commonMistakes (JSON-список), type TEXT (strength/plank/running), thumbnailPath, animationPath, isCustom, createdAt, updatedAt.
- `exercise_muscles` — exerciseId, muscleGroupId, intensity TEXT (primary/secondary).
- `programs` — id, name, description, daysCount, createdAt, updatedAt.
- `program_days` — id, programId (CASCADE), dayIndex (0–6), dayOfWeek (1=Пн…7=Вс, nullable).
- `program_day_exercises` — id, dayId (CASCADE), exerciseId (SET NULL), orderIndex, sets, reps, durationSeconds, weightKg, distanceMeters, restSeconds, isAlternative BOOL.
- `workout_reminders` — id, programDayId (CASCADE), hour, minute, enabled.
- `workout_sessions` — id, programId (SET NULL), programName (копия), programDayId (SET NULL), dayIndex (копия), variant TEXT (main/alternative), performedDate, startedAt, endedAt, status.
- `workout_set_results` — id, sessionId (CASCADE), exerciseId (SET NULL), exerciseName/exerciseType (копии), setIndex, reps, weightKg, durationSeconds, distanceMeters, completedAt.
- `schedule_marks` — id, programDayId, weekStart, status TEXT (skipped).
- `body_measurements` — id, date, heightCm, weightKg, neckCm, chestCm, waistCm, hipsCm, bicepsCm, forearmCm, thighCm, calfCm (кроме date — все nullable).

**Статусы недели (вычисляемые):** `performed` (сессия на неделе), `rescheduled` (сессия есть, но дата ≠ закреплённый день), `skipped` (отметка в `schedule_marks`), иначе `pending`.

**Метрики по типам:** strength → sets+reps+weightKg; plank → sets+durationSeconds; running → distanceMeters+durationSeconds.

## 7. Каталог мышц (15 групп)

пресс, косые, грудь, плечи, бицепс, трицепс, предплечья, трапеции, широчайшие, поясница, ягодицы, квадрицепсы, бицепс бедра, икры, шея.

## 8. Пример seed-данных

```json
{
  "version": 2,
  "exercises": [
    {
      "name": "Приседания с гирей",
      "type": "strength",
      "description": "Гирю держать обеими руками на груди, присесть до параллели, встать, сохраняя спину прямой.",
      "instructions": "1. Ноги на ширине плеч, носки чуть врозь. 2. Опускайтесь, отводя таз назад. 3. В нижней точке колени не уходят внутрь.",
      "commonMistakes": ["Кругление поясницы", "Колени уходят внутрь", "Отрыв пяток"],
      "muscles": { "primary": ["квадрицепсы", "ягодицы"], "secondary": ["пресс"] },
      "contraindications": ["колени", "спина"],
      "animation": "goblet_squat.webp",
      "source": "wger"
    }
  ]
}
```

Стартовый набор (мин. 15): силовые — «Приседания с гирей», «Рывок гири», «Махи гирей», «Жим гири над головой», «Подтягивания на турнике», «Отжимания на брусьях», «Отжимания от пола», «Становая тяга с гирей»; планка — «Планка на локтях», «Боковая планка», «Планка с подъёмом ноги»; бег — «Бег на выносливость», «Бег на дистанцию», «Интервальный бег».

---

# Этап 0. Подготовка проекта

### Задача 0.1: Настройка проекта и CI
- **Статус:** [x] выполнена
- **Модуль:** проект
- **Ветка:** `task/0.1-setup`
- **Описание:** Проект `fitnessappai/` создан. Довести до рабочего состояния: поднять `minSdk = 31` в `android/app/build.gradle.kts`, проверить `applicationId`/namespace, обновить `analysis_options.yaml`, `.gitignore`, README. Удалить counter-пример из `lib/main.dart` (минимальный экран «FitnessAppAI»). Создать `.github/workflows/ci.yml` (см. раздел 3). Первый коммит всех файлов в ветке `task/0.1-setup`, PR в `main`.
- **Входные данные:** существующий проект; URL GitHub-remote (блокер).
- **Выходные данные:** собираемый проект Android 12+; зелёный CI.
- **Критерии приемки:** (1) `minSdk=31`; (2) CI: analyze 0 замечаний, format check, `flutter test`, `flutter build apk --debug`; (3) APK-артефакт загружается.
- **Тесты:** smoke-тест приложения.
- **Зависимости:** нет.

### Задача 0.2: Зависимости, структура папок, ServiceLocator
- **Статус:** [x] выполнена
- **Модуль:** core / di
- **Ветка:** `task/0.2-structure-di`
- **Описание:** Добавить в `pubspec.yaml` все пакеты раздела 4. Создать структуру `lib/` (раздел 5). Реализовать `ServiceLocator` (`registerLazySingleton`/`registerFactory`/`reset`).
- **Входные данные:** Задача 0.1.
- **Выходные данные:** `flutter pub get` без конфликтов; каркас; DI.
- **Критерии приемки:** (1) `pub get` успешен; (2) `analyze` чистый; (3) smoke-тест DI.
- **Тесты:** unit-тест ServiceLocator (register/get/reset, singleton).
- **Зависимости:** Задача 0.1.

### Задача 0.3: Темизация (Material 3, тёмная)
- **Статус:** [x] выполнена
- **Модуль:** app / theme
- **Ветка:** `task/0.3-theme`
- **Описание:** `AppTheme`: `ColorScheme.fromSeed(seed, brightness: dark)`, `themeMode: ThemeMode.dark`, стили NavigationBar/NavigationRail, Card, FilledButton, SegmentedButton, SearchBar, диалогов. Подключить в MaterialApp.
- **Входные данные:** Задача 0.2.
- **Выходные данные:** тёмная M3-тема.
- **Критерии приемки:** (1) themeMode dark; (2) seed-цвет применяется.
- **Тесты:** widget-тест темы.
- **Зависимости:** Задача 0.2.

### Задача 0.4: Локализация (русский)
- **Статус:** [x] выполнена
- **Модуль:** core / l10n
- **Ветка:** `task/0.4-l10n`
- **Описание:** `l10n.yaml`, `gen-l10n`, `app_ru.arb` (все строки: навигация, общие кнопки, типы упражнений, мышцы, статусы расписания и т.д.). Подключить delegates, `supportedLocales: [ru]`, `locale: ru`.
- **Входные данные:** Задача 0.2.
- **Выходные данные:** UI на русском; все строки из ARB.
- **Критерии приемки:** (1) gen-l10n без ошибок; (2) русские строки отображаются.
- **Тесты:** widget-тест локализации.
- **Зависимости:** Задача 0.2.

### Задача 0.5: Роутинг и каркас навигации
- **Статус:** [x] выполнена
- **Модуль:** app / navigation
- **Ветка:** `task/0.5-routing`
- **Описание:** `go_router`: ShellRoute (вкладки: Упражнения, Программы, План, Прогресс, Профиль) + роуты: `/exercises/:id`, `/exercises/new`, `/exercises/:id/edit`, `/programs/new`, `/programs/:id/edit`, `/program-day/:dayId/exercise-params`, `/workout/prepare/:programDayId`, `/workout/run`, `/history`, `/history/:id`, `/profile`, `/sync`, `/contraindications`. Placeholder-экраны вкладок.
- **Входные данные:** Задачи 0.3, 0.4.
- **Выходные данные:** навигация по вкладкам и роутам.
- **Критерии приемки:** (1) вкладки переключаются; (2) deep-link на placeholder-роуты работает; (3) `404`.
- **Тесты:** widget-тесты навигации.
- **Зависимости:** Задачи 0.3, 0.4.

### Задача 0.6: Адаптивная навигация (планшеты, складные)
- **Статус:** [x] выполнена (PR #6)
- **Модуль:** app / responsive
- **Ветка:** `task/0.6-responsive`
- **Описание:** Breakpoints (compact <600dp, medium 600–840, expanded ≥840). На expanded — `NavigationRail` вместо `NavigationBar`; на планшетах — two-pane (список+детали). Helper-класс `AppBreakpoints`, виджеты `AdaptiveNavigation`, `TwoPaneLayout`. Проверить на AVD с профилем Samsung Fold и планшета.
- **Входные данные:** Задача 0.5.
- **Выходные данные:** оболочка, адаптирующаяся к ширине экрана.
- **Критерии приемки:** (1) на ≥840dp NavigationRail; (2) two-pane на планшете; (3) нет overflow на 320–1280dp.
- **Тесты:** widget-тесты с разными `surfaceSize` (480/800/1200dp).
- **Зависимости:** Задача 0.5.

# Этап 1. Базовая инфраструктура данных

### Задача 1.1: Domain-модели и валидаторы
- **Статус:** [x] выполнена (PR #7)
- **Модуль:** core / domain
- **Ветка:** `task/1.1-models`
- **Описание:** Модели (immutable, `copyWith`, `==`): `ExerciseType`, `Exercise` (включая instructions/commonMistakes/thumbnailPath/animationPath), `MuscleGroup`, `ExerciseMuscle`, `Program`, `ProgramDay`, `ProgramDayExercise` (isAlternative), `WorkoutSession`, `WorkoutSetResult`, `ScheduleMark`, `BodyMeasurement`, `UserProfile`, `WorkoutReminder`. Валидаторы: `ExerciseValidator`, `ProgramValidator` (дни 1–7, уникальный dayIndex, у дня ≥1 основное упражнение), `ProgramDayExerciseValidator` (метрики по типу).
- **Входные данные:** Задача 0.2.
- **Выходные данные:** чистые модели без зависимостей от БД/Flutter.
- **Критерии приемки:** (1) модели immutable; (2) валидаторы покрывают все типы и структуру дней; (3) `analyze` чистый.
- **Тесты:** unit-тесты моделей и валидаторов (день без упражнений невалиден, кол-во дней вне 1–7 отклоняется).
- **Зависимости:** Задача 0.2.

### Задача 1.2: Drift-инфраструктура
- **Статус:** [x] выполнена
- **Модуль:** core / database
- **Ветка:** `task/1.2-drift-core`
- **Описание:** `AppDatabase` с инжектируемым `QueryExecutor` (`driftDatabase(name: 'fitnessappai')` в проде, `NativeDatabase.memory()` в тестах), таблица `app_meta`, `MigrationStrategy`, `drift_dev schema dump`, `build_runner`; `*.g.dart` коммитить. Регистрация в ServiceLocator.
- **Входные данные:** Задачи 0.2, 1.1.
- **Выходные данные:** подключение к SQLite; миграционный каркас.
- **Критерии приемки:** (1) open/close in-memory БД; (2) round-trip `app_meta`; (3) schema_version генерируется.
- **Тесты:** unit-тесты открытия/закрытия и app_meta.
- **Зависимости:** Задачи 0.2, 1.1.

### Задача 1.3: Таблицы Drift всех фич и конвертеры
- **Статус:** [x] выполнена
- **Модуль:** core / database
- **Ветка:** `task/1.3-drift-tables`
- **Описание:** Все таблицы раздела 6, конвертеры (ExerciseType↔TEXT, DateTime↔int, JSON для commonMistakes), индексы на FK. Сидер справочников `muscle_groups` и `contraindication_tags` (идемпотентно, при создании БД). FK-политики раздела 6.
- **Входные данные:** Задачи 1.1, 1.2.
- **Выходные данные:** полная типобезопасная схема.
- **Критерии приемки:** (1) round-trip по каждой таблице; (2) конвертеры; (3) справочники сидятся идемпотентно; (4) build_runner без ошибок.
- **Тесты:** unit-тесты round-trip всех таблиц; FK-поведение (SET NULL/CASCADE); конвертеры.
- **Зависимости:** Задачи 1.1, 1.2.

### Задача 1.4: MediaStore (файлы анимаций и изображений)
- **Статус:** [x] выполнена
- **Модуль:** core / media
- **Ветка:** `task/1.4-media-store`
- **Описание:** `MediaStore`: `ensureAppMediaDir()`, `copyAssetToStorage(assetPath) → path`, `importFromPicker() → path` (file_picker), `deleteFile(path)`, кэш `path → ImageProvider` (`MediaCache` с `cacheWidth`). Анимированный WebP — нативный `Image.file`.
- **Входные данные:** Задачи 0.2, 1.1.
- **Выходные данные:** сервис работы с медиафайлами.
- **Критерии приемки:** (1) копирование ассета→файл; (2) импорт извне; (3) удаление; (4) кэш возвращает provider без дублей.
- **Тесты:** unit/widget-тесты на временной директории: copy/import/delete/cache.
- **Зависимости:** Задачи 0.2, 1.1.

# Этап 2. Управление упражнениями

### Задача 2.1: Репозиторий упражнений (бизнес-логика)
- **Статус:** [x] выполнена
- **Модуль:** features / exercises / data
- **Ветка:** `task/2.1-exercise-repository`
- **Описание:** `ExerciseRepository`: `getAll()`, `getById(id)`, `search(String)`, `getByType(ExerciseType)`, `create(Exercise, List<ExerciseMuscle>)`, `update(...)`, `delete(id)` (файлы медиа удаляются через MediaStore), `getMuscles(exerciseId)`, `setMuscles(...)`, `getContraindications/setContraindications`.
- **Входные данные:** Задачи 1.3, 1.4.
- **Выходные данные:** CRUD + мышцы + подсказки.
- **Критерии приемки:** (1) CRUD; (2) поиск/фильтр; (3) мышцы (primary/secondary) сохраняются; (4) удаление чистит файлы.
- **Тесты:** unit-тесты (in-memory + tmp-медиа): CRUD, поиск, мышцы, теги, удаление файлов.
- **Зависимости:** Задачи 1.3, 1.4.

### Задача 2.2: Seed-данные (wger + мышцы + анимации)
- **Статус:** [x] выполнена
- **Модуль:** features / exercises / data + tool
- **Ветка:** `task/2.2-seed`
- **Описание:** `tool/export_wger.dart`: экспорт из wger API (exerciseinfo, muscle) с маппингом категорий и мышц. Вручную дополнить: русские `instructions`/`commonMistakes`, `contraindications`, имена файлов анимаций; подобрать лицензионно чистые анимированные webp (мин. 15 упражнений, все 3 типа, файлы в `assets/exercises/`; если источник недоступен — временно статичные webp, отметить в README). `ExerciseSeeder`: при первом запуске копирует анимации через MediaStore, парсит JSON, вставляет упражнения+мышцы+теги, ставит флаг `seeded`. Идемпотентность.
- **Входные данные:** Задачи 2.1, 1.4.
- **Выходные данные:** `assets/data/exercises_seed.json` + `assets/exercises/*.webp` + автозагрузка.
- **Критерии приемки:** (1) JSON валиден, все 3 типа, мышцы и подсказки; (2) повторный сид не дублирует; (3) анимации скопированы в storage и пути записаны; (4) парсер устойчив к «грязному» JSON.
- **Тесты:** unit-тесты парсера и седера (идемпотентность, копирование файлов).
- **Зависимости:** Задачи 2.1, 1.4.

### Задача 2.3: Экран списка упражнений (UI)
- **Статус:** [x] выполнена
- **Модуль:** features / exercises / ui
- **Ветка:** `task/2.3-exercise-list`
- **Описание:** `ExerciseListController` (signals: items, query, typeFilter, isLoading; дебаунс поиска 300 мс). Экран: AppBar «Упражнения», SearchBar, FilterChip (Все/Силовые/Планка/Бег), `ListView.builder` карточек (миниатюра из MediaCache, название, бейдж типа, бейдж «⚠ противопоказания», список мышц), FAB «+» (создать вручную), пустое/loading состояния, pull-to-refresh. Адаптив: two-pane на планшетах.
- **Входные данные:** Задачи 2.1, 0.5, 0.6.
- **Выходные данные:** экран списка упражнений.
- **Критерии приемки:** (1) список из БД; (2) поиск и фильтр обновляют список; (3) миниатюры из кэша; (4) тап → детали; (5) пустое состояние.
- **Тесты:** widget-тесты (мок-репозиторий, mocktail): отображение, фильтр, поиск, пустое состояние, навигация.
- **Зависимости:** Задачи 2.1, 0.5, 0.6.

### Задача 2.4: Экран деталей упражнения (UI)
- **Статус:** [x] выполнена
- **Модуль:** features / exercises / ui
- **Ветка:** `task/2.4-exercise-detail`
- **Описание:** Экран деталей: анимированная подсказка (`Image.file` из MediaCache, обводка, обработка ошибок загрузки), название, бейдж типа, описание, секция «Техника выполнения» (instructions), секция «Частые ошибки» (commonMistakes), секция «Задействованные мышцы» (мини-схема мускулатуры с подсветкой, виджет из 2.6), секция «Противопоказания» (красные бейджи + текст). Кнопки «Редактировать»/«Удалить» — только для кастомных (удаление с confirm).
- **Входные данные:** Задачи 2.1, 2.3, 2.6.
- **Выходные данные:** экран просмотра упражнения.
- **Критерии приемки:** (1) данные по id; (2) анимация рендерится, ошибка обрабатывается; (3) секции скрываются при отсутствии данных; (4) для не-кастомных кнопки скрыты; (5) удаление с подтверждением.
- **Тесты:** widget-тесты: отображение, скрытие секций, диалог удаления, ошибка изображения.
- **Зависимости:** Задачи 2.1, 2.3, 2.6.

### Задача 2.5: Экран создания/редактирования упражнения (UI)
- **Статус:** [x] выполнена
- **Модуль:** features / exercises / ui
- **Ветка:** `task/2.5-exercise-form`
- **Описание:** Форма: «Название» (обязательное), dropdown «Тип», «Описание», «Техника выполнения» (multiline), «Частые ошибки» (список строк с добавлением/удалением), «Задействованные мышцы» (выбор primary/secondary из каталога с мини-схемой), «Противопоказания» (выбор тегов), «Анимация» (file_picker webp → MediaStore → preview). Кнопка «Сохранить» → create/update.
- **Входные данные:** Задачи 2.1, 2.6, 1.4.
- **Выходные данные:** форма создания/редактирования.
- **Критерии приемки:** (1) пустое название не сохраняется; (2) выбор файла анимации работает; (3) мышцы/теги/подсказки сохраняются; (4) `isCustom=true`.
- **Тесты:** widget-тесты: валидация, выбор анимации (mock file_picker), создание/редактирование.
- **Зависимости:** Задачи 2.1, 2.6, 1.4.

### Задача 2.6: Компонент схемы мускулатуры (CustomPainter)
- **Статус:** [x] выполнена
- **Модуль:** features / exercises / ui (shared)
- **Ветка:** `task/2.6-muscle-diagram`
- **Описание:** `MuscleDiagramPainter` с Path-регионами для ~15 групп мышц в двух видах (front/back), каждая группа — `Path`, привязана через `regionKey`. Виджет `MuscleDiagram({highlights: Map<String, double>, view})`: подсветка региона с интенсивностью (opacity/цвет по проценту). Используется в 2.4, 3.3/3.4, 5.2. Регионы схематичные, но узнаваемые (округлые формы по позициям мышц).
- **Входные данные:** Задачи 1.1, 0.3.
- **Выходные данные:** переиспользуемый виджет схемы.
- **Критерии приемки:** (1) рендер без ошибок в обоих видах; (2) подсветка регионов по `highlights`; (3) прозрачные цвета для тёмной темы.
- **Тесты:** widget-тесты: рендер, корректная подсветка, отсутствие ошибок при пустых данных.
- **Зависимости:** Задачи 1.1, 0.3.

# Этап 3. Управление программами (тренировочные дни)

### Задача 3.1: Репозиторий программ (бизнес-логика)
- **Статус:** [x] выполнена
- **Модуль:** features / programs / data
- **Ветка:** `task/3.1-program-repository`
- **Описание:** `ProgramRepository`: `getPrograms()` (с кол-вом дней и упражнений), `getProgram(id)` (дни + основные и альтернативные упражнения), `create(Program, List<ProgramDay>)`, `update(...)`, `delete(id)`, `addDay`, `updateDay`, `removeDay`, `reorderDays`, `addExerciseToDay(dayId, exerciseId, isAlternative)`, `updateExercise`, `removeExercise`, `reorderExercises(dayId, List)`. Валидация через `ProgramValidator`/`ProgramDayExerciseValidator` (1.1) в транзакции.
- **Входные данные:** Задачи 1.3, 1.1, 2.1.
- **Выходные данные:** CRUD программ с днями и альтернативами.
- **Критерии приемки:** (1) создание программы с днями атомарно; (2) альтернативный набор хранится отдельно (isAlternative); (3) реордер дней и упражнений; (4) невалидная структура отклоняется.
- **Тесты:** unit-тесты (in-memory): CRUD, транзакции, альтернативы, реордер, валидация.
- **Зависимости:** Задачи 1.3, 1.1, 2.1.

### Задача 3.2: Экран списка программ (UI)
- **Статус:** [x] выполнена
- **Модуль:** features / programs / ui
- **Ветка:** `task/3.2-program-list`
- **Описание:** `ProgramListController` (signals). Экран: AppBar «Программы», список карточек (название, кол-во дней «N дней», дни недели, кол-во упражнений), FAB «+», пустое состояние, меню «Редактировать»/«Удалить». Тап → конструктор (3.3).
- **Входные данные:** Задачи 3.1, 0.5.
- **Выходные данные:** экран списка программ.
- **Критерии приемки:** (1) список из БД; (2) пустое состояние; (3) удаление с подтверждением; (4) переход в конструктор.
- **Тесты:** widget-тесты: список, пустое состояние, удаление, навигация.
- **Зависимости:** Задачи 3.1, 0.5.

### Задача 3.3: Конструктор программы, часть 1 — параметры и дни (UI)
- **Статус:** [x] выполнена
- **Модуль:** features / programs / ui
- **Ветка:** `task/3.3-program-builder-days`
- **Описание:** Первый шаг конструктора: название (обязательное), описание, выбор кол-ва дней (Stepper/SegmentedButton 1–7), настройка каждого дня: день недели (`DropdownButton` Пн…Вс / «без привязки»), реордер дней (`ReorderableListView`). Диалог настройки дня: день недели + статус напоминания (время задаётся в 7.6).
- **Входные данные:** Задачи 3.1, 3.2.
- **Выходные данные:** параметры программы и дни.
- **Критерии приемки:** (1) выбор 1–7 дней; (2) назначение дня недели; (3) реордер дней; (4) сохранение черновика программы.
- **Тесты:** widget-тесты: выбор дней, день недели, реордер.
- **Зависимости:** Задачи 3.1, 3.2.

### Задача 3.4: Конструктор программы, часть 2 — наполнение дней (UI)
- **Статус:** [x] выполнена (PR #17)
- **Модуль:** features / programs / ui
- **Ветка:** `task/3.4-program-builder-exercises`
- **Описание:** Второй шаг: для каждого дня — список упражнений (карточки: название, параметры, «настройки», «удалить», реордер), кнопка «+ Добавить упражнение» (выбор из БД), вкладка/кнопка «Альтернативный набор» (добавление упражнений с `isAlternative=true`), панель «Задействованные мышцы» (MuscleDiagram, суммирует primary/secondary по текущему набору дня), предпросмотр прогресса наполнения (сколько дней заполнено). Сохранение → 3.1.
- **Входные данные:** Задачи 3.1, 3.3, 2.6.
- **Выходные данные:** наполненная программа.
- **Критерии приемки:** (1) добавление/удаление/реордер упражнений дня; (2) альтернативный набор редактируется отдельно; (3) схема мышц обновляется реактивно; (4) невалидные позиции блокируют сохранение.
- **Тесты:** widget-тесты: добавление упражнения, альтернативы, реордер, обновление схемы мышц.
- **Зависимости:** Задачи 3.1, 3.3, 2.6.

### Задача 3.5: Экран параметров упражнения в дне (UI)
- **Статус:** [x] выполнена (PR #18)
- **Модуль:** features / programs / ui
- **Ветка:** `task/3.5-day-exercise-params`
- **Описание:** Экран параметров позиции (роут `/program-day/:dayId/exercise-params`). Поля по типу упражнения: strength → «Подходы», «Повторения», «Вес (кг)», «Отдых (сек)»; plank → «Подходы», «Время (сек)», «Отдых»; running → «Дистанция (км)», «Время (мин)», «Отдых». Числовые поля с валидацией (границы из 1.1). «Сохранить» → `updateExercise`.
- **Входные данные:** Задачи 3.1, 1.1.
- **Выходные данные:** конфигурация позиции.
- **Критерии приемки:** (1) набор полей по типу; (2) невалидные значения не сохраняются; (3) значения отображаются в списке дня.
- **Тесты:** widget-тесты: поля по типу, валидация, сохранение.
- **Зависимости:** Задачи 3.1, 1.1.

# Этап 4. Выполнение тренировок и расписание

### Задача 4.1: Контроллер тренировки (бизнес-логика)
- **Статус:** [x] выполнена (PR #19)
- **Модуль:** features / workout / domain
- **Ветка:** `task/4.1-workout-controller`
- **Описание:** Чистый класс `WorkoutController` (signals): `currentExerciseIndex`, `currentSet`, `completedSets`, `results`, `phase` (exercise/rest/finished), `restRemainingSeconds`. Методы: `start(List<ProgramDayExercise>)`, `setResult(values)`, `confirmSet()`, `startRest()/skipRest()`, `nextExercise()`, `completeWorkout() → (WorkoutSession, List<WorkoutSetResult>)`, `cancelWorkout()` (ничего не сохраняет). `RestTimer` через `Timer.periodic` (инжектируемый для тестов); для планки — таймер удержания.
- **Входные данные:** Задачи 1.1, 3.1.
- **Выходные данные:** тестируемая логика сессии.
- **Критерии приемки:** (1) переходы подходов/упражнений; (2) таймер отдыха; (3) фиксация результатов всех типов; (4) complete/cancel.
- **Тесты:** unit-тесты (fake_async): 3×8 с отдыхом 60с, планка, бег, завершение, отмена.
- **Зависимости:** Задачи 1.1, 3.1.

### Задача 4.2: Репозиторий тренировки (бизнес-логика)
- **Статус:** [x] выполнена (PR #20)
- **Модуль:** features / workout / data
- **Ветка:** `task/4.2-workout-repository`
- **Описание:** `WorkoutRepository.saveSession(WorkoutSession, List<WorkoutSetResult>)` в транзакции (копии programName/exerciseName/type, programDayId, dayIndex, variant). `getSessions(programDayId, weekStart)`, `getSessionsBetween(start, end)`, `getSession(id)` с результатами. `markSkipped(programDayId, weekStart)`, `clearSkip(...)`, `getSkips(weekStart)`.
- **Входные данные:** Задачи 1.3, 4.1.
- **Выходные данные:** запись истории и отметки пропусков.
- **Критерии приемки:** (1) атомарное сохранение; (2) чтение сессий по неделе/периоду; (3) отметки пропусков.
- **Тесты:** unit-тесты (in-memory): сохранение, чтение, skips.
- **Зависимости:** Задачи 1.3, 4.1.

### Задача 4.3: Экран «План недели» (UI)
- **Статус:** [x] выполнена
- **Модуль:** features / workout / ui
- **Ветка:** `task/4.3-week-plan`
- **Описание:** Экран (вкладка «План»): сетка недели (Пн–Вс), для каждой даты — запланированные тренировочные дни программ (по dayOfWeek) с вычисленными статусами (pending/performed/rescheduled/skipped). Действия: «Начать» (переход в 4.4), «Перенести на сегодня» (запуск тренировочного дня, не соответствующего текущему дню недели), «Пропустить» (markSkipped), «Отменить пропуск». Сворачиваемое в список на узких экранах.
- **Входные данные:** Задачи 4.2, 3.1, 0.5, 0.6.
- **Выходные данные:** планировщик недели.
- **Критерии приемки:** (1) корректные статусы; (2) перенос запускает день; (3) пропуск/отмена пропуска.
- **Тесты:** widget-тесты: статусы, перенос, пропуск.
- **Зависимости:** Задачи 4.2, 3.1, 0.5, 0.6.

### Задача 4.4: Экран подготовки к тренировке (UI)
- **Статус:** [x] выполнена
- **Модуль:** features / workout / ui
- **Ветка:** `task/4.4-workout-prepare`
- **Описание:** Экран (роут `/workout/prepare/:programDayId`): список упражнений дня; если есть альтернативный набор — выбор «Основной набор / Альтернативный» (SegmentedButton), список меняется; предупреждение о противопоказаниях (диалог из 7.5); кнопка «Начать тренировку» (переход в 4.5 с выбранным вариантом).
- **Входные данные:** Задачи 3.1, 4.1, 0.5, 7.5.
- **Выходные данные:** старт тренировки с выбором варианта.
- **Критерии приемки:** (1) выбор варианта меняет список; (2) предупреждения перед стартом; (3) старт создаёт контроллер.
- **Тесты:** widget-тесты: выбор варианта, диалог предупреждений, старт.
- **Зависимости:** Задачи 3.1, 4.1, 0.5, 7.5.

### Задача 4.5: Экран выполнения тренировки (UI)
- **Статус:** [x] завершена
- **Модуль:** features / workout / ui
- **Ветка:** `task/4.5-workout-run`
- **Описание:** Экран (роут `/workout/run`): прогресс (индекс/всего), название упражнения, анимированная подсказка (Image.file), «Подход N из M», поля ввода результата по типу, «Подход выполнен» → панель отдыха с обратным отсчётом и «Пропустить отдых», «Завершить тренировку» (confirm, completeWorkout + saveSession) → экран итогов → «Прогресс». **Wake lock**: `WakelockPlus.enable()` при входе на экран, disable при выходе. Выход без завершения — confirm «Тренировка не будет сохранена» (cancelWorkout).
- **Входные данные:** Задачи 4.1, 4.2, 4.4.
- **Выходные данные:** полный цикл выполнения.
- **Критерии приемки:** (1) фиксация подходов всех типов; (2) таймер отдыха; (3) wake lock включён на экране и выключен после; (4) завершение сохраняет и показывает итоги; (5) отмена ничего не сохраняет.
- **Тесты:** widget-тесты (tester.pump): фиксация, отдых, завершение, отмена, wake lock (mock).
- **Зависимости:** Задачи 4.1, 4.2, 4.4.

# Этап 5. Отслеживание прогресса

### Задача 5.1: Агрегатор статистики (бизнес-логика)
- **Статус:** [x] завершена
- **Модуль:** features / progress / domain
- **Ветка:** `task/5.1-stats-aggregator`
- **Описание:** `StatsAggregator`. `StatPeriod { week, month, year }` + разбиение на срезы. Методы: `workoutCount(period)`, `totalDistance(period)`, `totalPlankTime(period)`, `maxWeight(exerciseId, period)`, `totalReps(exerciseId, period)`, `workoutCountPerSlice(period)`, **`muscleLoadPercent(period)`** (primary=1.0, secondary=0.5 на выполненный подход, по текущей карте `exercise_muscles`; нормализация в %, ограничение: удалённые упражнения не участвуют).
- **Входные данные:** Задачи 4.2, 1.1.
- **Выходные данные:** функции агрегации для UI.
- **Критерии приемки:** (1) суммы/максимумы на фиксированных данных; (2) границы недели/месяца/года; (3) `muscleLoadPercent` в сумме ≈100%; (4) пустые периоды → 0.
- **Тесты:** unit-тесты: метрики, периоды, распределение нагрузки по мышцам.
- **Зависимости:** Задачи 4.2, 1.1.

### Задача 5.2: Экран прогресса (UI + графики + схема мышц)
- **Статус:** [x] завершена
- **Модуль:** features / progress / ui
- **Ветка:** `task/5.2-progress-screen`
- **Описание:** `ProgressController` (signals: period, selectedExerciseId). Экран: SegmentedButton (Неделя/Месяц/Год), карточки («Тренировок», «Дистанция», «Время планки»), `BarChart` (fl_chart) «Тренировки по срезам», `LineChart` «Прогресс метрики» (dropdown выбора упражнения: max вес / дистанция / время), **панель «Нагрузка на мышцы»**: MuscleDiagram (2.6) с процентами + список топ-мышц. Реактивное обновление через `SignalBuilder`.
- **Входные данные:** Задачи 5.1, 2.6, 0.5.
- **Выходные данные:** экран статистики.
- **Критерии приемки:** (1) смена периода обновляет всё; (2) выбор упражнения меняет график; (3) схема мышц отражает проценты; (4) пустое состояние.
- **Тесты:** widget-тесты: периоды, выбор упражнения, рендер диаграммы, пустое состояние.
- **Зависимости:** Задачи 5.1, 2.6, 0.5.

### Задача 5.3: История тренировок (UI)
- **Статус:** [x] реализована (PR #26)
- **Модуль:** features / progress / ui
- **Ветка:** `task/5.3-workout-history`
- **Описание:** Экран списка сессий (дата, программа, вариант main/alt, кол-во упражнений, суммарное время; свежие сверху) + экран деталей сессии (по упражнениям: подходы с весом/повторами/временем/дистанцией). Роуты `/history`, `/history/:id`.
- **Входные данные:** Задачи 4.2, 5.1, 0.5.
- **Выходные данные:** просмотр истории.
- **Критерии приемки:** (1) список из БД; (2) детали по подходам; (3) пустое состояние.
- **Тесты:** widget-тесты: список, детали, пустое состояние.
- **Зависимости:** Задачи 4.2, 5.1, 0.5.

# Этап 6. LLM (отложено после MVP)

### Задача 6.1: Подготовка архитектуры генерации контента (в MVP)
- **Статус:** [x] выполнена (2026-08-11)
- **Модуль:** features / llm / domain
- **Ветка:** `task/6.1-llm-contract`
- **Описание:** Абстрактный `ExerciseContentGenerator { Future<ExerciseSuggestion> generate(String nameHint); }`. DTO `ExerciseSuggestion` (name, description, type, contraindications[], muscles, instructions, commonMistakes). **JSON-контракт** в `docs/llm_contract.md` (промпт на русском, строгий JSON, правила валидности). Парсер `SuggestionJsonParser.fromJson` с обработкой ошибок (`MalformedSuggestionException`). Заглушка `UnsupportedGenerator` регистрируется в ServiceLocator (UI не использует).
- **Входные данные:** Задачи 1.1, 0.2.
- **Выходные данные:** интерфейс, контракт, парсер, заглушка.
- **Критерии приемки:** (1) парсер разбирает валидный JSON и отклоняет невалидный с typed-ошибкой; (2) генератор зарегистрирован в DI.
- **Тесты:** unit-тесты парсера: валидный JSON, пропущенные/неверные поля, лишние поля.
- **Зависимости:** Задачи 1.1, 0.2.

### Задачи 6.2–6.6 (после MVP, не блокируют)
- **6.2** Выбор GGUF-модели (кандидат Qwen2.5 1.5B/3B) и способа интеграции (llama_cpp_dart vs HTTP-сервер).
- **6.3** Менеджмент моделей: экран настроек (локальные/удалённые, размер на устройстве, добавление по URL, выбор из нескольких вариантов, удаление модели). Таблица `llm_models`.
- **6.4** Реализация `ExerciseContentGeneratorImpl` + промпт-инжиниринг (структурированный JSON на русском).
- **6.5** LLM-анализ программы: незадействованные мышцы + предложение упражнений для повышения эффективности (по данным `exercise_muscles`).
- **6.6** Генерация изображений/анимаций упражнений (вне текстовой LLM; согласовать подход на этапе).

# Этап 7. Профиль, замеры, противопоказания, уведомления, синхронизация

### Задача 7.1: Противопоказания — бизнес-логика
- **Статус:** [x] выполнена (2026-08-11)
- **Модуль:** features / profile / domain
- **Ветка:** `task/7.1-contraindications-logic`
- **Описание:** `UserProfileRepository`: `get()`, `setContraindicationTags(keys)`, `getContraindicationTags()`. `ContraindicationService`: `warningsFor(exercise, userTags)` → сообщения, `hasContraindications(exercise)`, `filterAllowed(List<Exercise>, userTags)`.
- **Входные данные:** Задачи 1.3, 2.1, 1.1.
- **Выходные данные:** сервисы профиля и предупреждений.
- **Критерии приемки:** (1) профиль создаётся один раз; (2) пересечение тегов даёт предупреждения; (3) фильтрация.
- **Тесты:** unit-тесты: профиль, пересечение, фильтрация.
- **Зависимости:** Задачи 1.3, 2.1, 1.1.

### Задача 7.2: Замеры тела — репозиторий (бизнес-логика)
- **Статус:** [x] выполнена (2026-08-11)
- **Модуль:** features / profile / data
- **Ветка:** `task/7.2-body-metrics-repo`
- **Описание:** `BodyMeasurementRepository`: `add(BodyMeasurement)`, `getAll()` (по дате), `latest()`, `delete(id)`, `history(metricKey, period)`. Валидация значений (неотрицательные, разумные диапазоны). Метрики: рост, вес, шея, грудь, талия, бёдра, бицепс, предплечье, бедро, икра (все кроме роста/веса опциональны).
- **Входные данные:** Задачи 1.3, 1.1.
- **Выходные данные:** CRUD замеров.
- **Критерии приемки:** (1) CRUD; (2) валидация диапазонов; (3) `latest()`.
- **Тесты:** unit-тесты: CRUD, валидация, выборка по периодам.
- **Зависимости:** Задачи 1.3, 1.1.

### Задача 7.3: Экран профиля (замеры + графики, UI)
- **Статус:** [x] выполнена (2026-08-11)
- **Модуль:** features / profile / ui
- **Ветка:** `task/7.3-profile-screen`
- **Описание:** Экран профиля: текущие значения (рост, вес, 8 обхватов) с кнопкой «Добавить замер» (форма: дата + числовые поля, всё кроме даты опционально), история замеров (список), `LineChart` (fl_chart) изменения выбранной метрики во времени (dropdown метрики). Ссылки на «Противопоказания» (7.4) и «Синхронизация» (7.7).
- **Входные данные:** Задачи 7.2, 0.5.
- **Выходные данные:** экран профиля.
- **Критерии приемки:** (1) добавление/удаление замеров; (2) график по метрике; (3) пустое состояние.
- **Тесты:** widget-тесты: форма замера, график, удаление.
- **Зависимости:** Задачи 7.2, 0.5.

### Задача 7.4: Экран противопоказаний пользователя (UI)
- **Статус:** [x] выполнена (2026-08-11)
- **Модуль:** features / profile / ui
- **Ветка:** `task/7.4-contraindications-screen`
- **Описание:** Экран «Моё здоровье»: список тегов из каталога (SwitchListTile с описанием), «Сохранить» → 7.1.
- **Входные данные:** Задачи 7.1, 0.5.
- **Выходные данные:** экран настройки противопоказаний.
- **Критерии приемки:** (1) теги сохраняются и восстанавливаются; (2) русский UI.
- **Тесты:** widget-тесты: загрузка/сохранение.
- **Зависимости:** Задачи 7.1, 0.5.

### Задача 7.5: Предупреждения в UI
- **Статус:** [x] выполнена (2026-08-11)
- **Модуль:** features / exercises + workout / ui
- **Ветка:** `task/7.5-warnings-ui`
- **Описание:** (1) Бейдж «⚠ противопоказания» в списке упражнений (2.3) и блок в деталях (2.4) — привязка к `ContraindicationService`. (2) Диалог на экране подготовки (4.4): «В программе есть упражнения с противопоказаниями: [список]. Продолжить?» (Да/Отмена) — при пересечении тегов.
- **Входные данные:** Задачи 7.1, 4.4, 2.4.
- **Выходные данные:** видимые предупреждения.
- **Критерии приемки:** (1) бейджи по данным; (2) диалог только при пересечении; (3) «Продолжить» запускает, «Отмена» возвращает.
- **Тесты:** widget-тесты: бейджи, диалог.
- **Зависимости:** Задачи 7.1, 4.4, 2.4.

### Задача 7.6: Напоминания о тренировках
- **Статус:** [x] выполнена (2026-08-11)
- **Модуль:** features / programs + core / notifications
- **Ветка:** `task/7.6-reminders`
- **Описание:** `ReminderService` на `flutter_local_notifications` + `timezone`: `schedule(programDayId, hour, minute)` (еженедельное по dayOfWeek через `zonedSchedule` + `matchDateTimeComponents: weekday`), `cancel(programDayId)`, `rescheduleAll()` (после импорта БД). Настройка времени в диалоге дня программы (3.3): время + переключатель «Напоминать» / «Отключить». Пермишен на точное время для API 31+ (`SCHEDULE_EXACT_ALARM`, fallback — неточное). При смене dayOfWeek/времени — перепланирование.
- **Входные данные:** Задачи 3.3, 1.3.
- **Выходные данные:** уведомления о тренировочных днях.
- **Критерии приемки:** (1) планирование по дням недели; (2) отмена; (3) перепланирование при изменении дня; (4) перепланирование после импорта БД.
- **Тесты:** unit-тесты планировщика (mock плагина уведомлений), widget-тесты диалога настройки.
- **Зависимости:** Задачи 3.3, 1.3.

### Задача 7.7: Синхронизация — экспорт/импорт БД
- **Статус:** [x] выполнена
- **Модуль:** features / sync
- **Ветка:** `task/7.7-sync`
- **Реализация:** интерфейс `SyncService` + `LocalFileSyncService`. Экспорт: `VACUUM INTO '<file>'` в каталог БД (documents), share через `share_plus`. Импорт: `file_picker` → копия во временный файл → `sqlite3`-проверка `PRAGMA integrity_check` и `PRAGMA user_version` (ожидается `appDatabaseSchemaVersion = 1`) → закрытие БД, замена файла, перезапуск приложения: `locator.reset()` + перерегистрация сервисов, идемпотентный `ReferenceSeeder.seed()`, `ReminderService.initialize()` + `rescheduleAll()`. Полное обновление виджет-дерева через `restartApp()` (счётчик `appRestartTick`, `ValueKey` в `main.dart`). Экран «Синхронизация» (роут `/sync` вместо заглушки): кнопки «Экспортировать БД» / «Импортировать БД», статус последней операции, подсказка о будущей облачной синхронизации.
- **Входные данные:** Задачи 1.2, 7.6.
- **Выходные данные:** ручной перенос данных.
- **Критерии приемки:** (1) экспорт создаёт корректный файл БД; (2) импорт валидной БД работает; (3) несовместимая версия схемы отклоняется с сообщением; (4) повреждённый файл отклоняется.
- **Тесты:** 6 unit-тестов `LocalFileSyncService` (временные файлы): экспорт (копия + рабочая БД), импорт (замена + `onImported`), повреждённый файл, несовместимая версия, пустой файл; 5 widget-тестов `SyncScreen` (рендер, экспорт+share, отмена выбора, ошибка валидации, диалог перезапуска).
- **Зависимости:** Задачи 1.2, 7.6.

# Этап 8. Тестирование и оптимизация

### Задача 8.1: Integration-тесты критических сценариев
- **Статус:** [x] выполнена
- **Модуль:** integration_test
- **Ветка:** `task/8.1-integration-tests`
- **Описание:** Сценарии: (1) сид БД → создание упражнения → программа (2 дня, альтернативный набор) → план недели → старт с выбором альтернативы → фиксация подходов (все 3 типа) → завершение → история и прогресс; (2) противопоказания: профиль + тег → предупреждение при старте → продолжить; (3) перенос/пропуск дня в плане недели. Запуск локально на эмуляторе/устройстве: `flutter test integration_test -d <device>` (в CI не входит).
- **Выполнено:** 14 сценариев (`integration_test/app_flow_test.dart`) проходят на Linux desktop (`flutter test integration_test -d linux`): (1) полный флоу — упражнения, программа, основная тренировка; (2) перенос дня + альтернативный набор; (3) противопоказания при старте; (4) пропуск/отмена/перенос дня; (5) тренировка по сторонам с фиксированным весом; (6) прогресс и история после тренировки; (7) главный экран: активная программа и быстрый старт; (8) настройки: тема, звук, проверка обновления; (9) тип «свой вес» + разминка перед тренировкой; (10) редактирование и удаление: блокировка удаления используемого упражнения, сохранение активности при редактировании, удаление программы и очистка плана недели; (11) замеры тела: добавление, текущие значения и график; (12) противопоказания: «больше не показывать для этой программы»; (13) непривязанный день (без дня недели) → «Быстрый старт» на главном экране; (14) планка по таймеру удержания: цель, кнопка «Начать», перенос фактического времени (без ручного ввода). В тестах `SoundService` и `WakelockService` подменены на заглушки (`pumpApp`), т.к. `AudioplayersSoundService` (GStreamer) блокирует event loop, а `WakelockPlusService` на Linux без DBus portal роняет фоновый async-вызов; для настроек дополнительно стабятся `UpdateService`/`SyncService`. `goToTab` ищет иконку и в `NavigationBar`, и в `NavigationRail`, учитывая filled/outlined вариант. В приложении сохранение дня в конструкторе программы переведено на `ProgramRepository.replaceDayExercises` — сохраняет только текущий день без полной валидации программы (валидация выполняется при сохранении программы), что разблокировало заполнение дней по одному. В `WorkoutRunScreen` сервис wakelock переведён на резолв из DI-контейнера (`locator`) с fallback на `WakelockPlusService` — это позволило подменять его в тестах (паттерн как у `SoundService`).
- **Входные данные:** Этапы 2–7.
- **Выходные данные:** сквозная проверка.
- **Критерии приемки:** сценарии 1–3 проходят на чистой установке.
- **Тесты:** 14 integration-сценариев.
- **Зависимости:** Задачи 4.5, 5.3, 7.5, 4.3.

### Задача 8.2: Оптимизация производительности
- **Статус:** [x] выполнена
- **Модуль:** все фичи
- **Ветка:** `task/8.2-perf`
- **Описание:** `ListView.builder` везде; дебаунс поиска; кэш изображений/анимаций (`MediaCache` + `cacheWidth`); индексы Drift на FK; `SliverList` для больших списков; профилирование `--profile` на типовом наборе; проверка отсутствия лишних перестроений (DevTools).
- **Выполнено:** (1) добавлены 12 `@TableIndex` на FK-колонки (exercise_muscles, exercise_contraindications, user_contraindications, program_days, program_day_exercises, workout_reminders, workout_sessions, workout_set_results, schedule_marks) — перегенерирован `app_database.g.dart`; (2) `MediaCache` подключён к экрану выполнения тренировки (`_ExerciseMedia`), чтобы не создавать `FileImage` при каждом перестроении; (3) все крупные списки уже на `ListView.builder`/`ListView.separated` (упражнения, программы, история, план недели), дебаунс поиска 300 мс в `ExerciseListController`; (4) поиск по упражнениям фильтруется в Dart (LIKE не учитывает регистр в SQLite). Профилирование `--profile` на AVD не выполнялось (нет эмулятора в окружении) — покрывается ручным чек-листом в 8.3. Пришлось исправить `app_database.dart`: аннотация `@DriftDatabase` перенесена с top-level `const` на класс (в drift_dev 2.34 детекция БД сломалась при аннотации на константе).
- **Входные данные:** Этапы 2–5.
- **Выходные данные:** отсутствие jank.
- **Критерии приемки:** (1) `analyze` 0 замечаний; (2) 200+ упражнений скроллятся без лагов; (3) регрессионные тесты зелёные.
- **Тесты:** регрессионные unit/widget-тесты.
- **Зависимости:** Этапы 2–5.

### Задача 8.3: Финальная валидация и README
- **Статус:** [x] выполнена
- **Модуль:** проект
- **Ветка:** `task/8.3-final`
- **Описание:** Полный прогон (analyze, format, test, build apk debug + локально release). Создать AVD API 31+ (в т.ч. профили Samsung Fold и планшет), ручной чек-лист обхода всех экранов на разных размерах. Обновить README (описание, стек, структура, как запускать, git-процесс). Проверить отсутствие незакрытых TODO (кроме отложенного LLM и заглушек изображений).
- **Выполнено:** `flutter analyze --fatal-infos` — 0 замечаний; `dart format --set-exit-if-changed` — чисто; `flutter test` — 381 тест зелёный; `flutter build apk --debug` и `flutter build apk --release` (67.7MB) собираются; README обновлён (возможности, стек, структура, запуск, проверки, git-процесс); TODO/FIXME в `lib/` отсутствуют.
- **Ограничение:** AVD `Pixel_9_API_36` (API 36 ≥ 31) существует, но в WSL2 у пользователя нет прав на `/dev/kvm` (нужен sudo: `sudo gpasswd -a $USER kvm`). Без аппаратного ускорения эмулятор нестабилен: PackageManager падает при установке APK, integration-тесты (8.1) не запускаются. Ручной чек-лист обхода экранов и integration-прогон отложены до выдачи прав KVM.
- **Входные данные:** Этапы 0–7, 8.2.
- **Выходные данные:** релизо-способная MVP-версия.
- **Критерии приемки:** (1) все проверки зелёные; (2) APK release собирается; (3) чек-лист пройден; (4) README актуален.
- **Тесты:** полный набор зелёный.
- **Зависимости:** все предыдущие.

# Этап 9. Устранение багов (каждый баг — с тестом на починку)

> Стартовый набор упражнений (`ExerciseSeeder`) реализован и покрыт тестами, но нигде не вызывается при старте — исправление в 9.6.

### Задача 9.1: Реактивность списков после записи в БД
- **Статус:** [x] выполнена
- **Модуль:** все фичи
- **Ветка:** `task/9.1-data-refresh`
- **Описание:** В `lib/` нет ни одного `drift watch()`, экраны живут в `StatefulShellRoute.indexedStack`, загрузка одноразовая в конструкторе — после создания/редактирования/удаления данные не обновляются. Ввести `DataChangeNotifier` (синглтон в ServiceLocator); репозитории (exercise, program, workout, body_measurement, user_profile, workout_reminder) оповещают после успешного write; контроллеры (exercise list/detail, program list, week plan, progress, history) подписываются и перезагружаются (coalescing в кадр). Баги 2 (список после создания), 3 (деталь после редактирования), 6 (прогресс после тренировки), 7 (сохранить→удалить→добавить).
- **Входные данные:** Этапы 2–5.
- **Выходные данные:** списки/детали обновляются сразу после записи.
- **Критерии приемки:** тесты 2, 3, 6, 7 зелёные; `analyze` 0; регрессия зелёная.
- **Тесты:** `exercises_screen_test` (вставка через репозиторий + notify → новое упражнение видно), `exercise_detail_screen_test` (обновление → новые данные), `progress_screen_test`/`history_screen_test` (сессия после notify → обновление), `programs_screen_test` (полный сценарий сохранить→удалить→добавить).
- **Реализация:** `lib/core/data/data_change_notifier.dart` — `DataChangeNotifier`, глобальный `appDataChanges`, `ChangeReloadSubscription` (coalescing через microtask, `dispose` снимает подписку). Репозитории: optional `changes` (дефолт на глобал) + `notifyChanged()` после успешных write. Контроллеры: подписка + `dispose()`; экраны вызывают `_controller.dispose()` (добавлен в exercise_detail, programs, week_plan, progress, history). Новые регрессионные тесты багов 2, 3, 6, 7 добавлены и зелёные.
- **Зависимости:** Этапы 2–5.

### Задача 9.2: Крах при выборе анимации упражнения
- **Статус:** [x] выполнена
- **Модуль:** фичи/упражнения
- **Ветка:** `task/9.2-exercise-form`
- **Описание:** `_pickAnimation` (`exercise_form_screen.dart`) — `async void` без `try/catch`; `MediaStore.importFromPicker` не обрабатывает `PlatformException` (file_picker) и `FileSystemException` (несуществующий путь); `MediaCache` не чистится при удалении файла. Обработка ошибок + типизированное `MediaImportException`, SnackBar с l10n, `MediaCache.remove`.
- **Критерии приемки:** клик «выбрать анимацию» не роняет приложение при любом исходе.
- **Тесты:** `media_store_test` (пикер бросает ошибку → ошибка, отмена → null), `exercise_form_screen_test` (бросающий пикер → SnackBar, без unhandled exception).
- **Реализация:** `lib/core/media/media_store.dart` — `MediaImportException` + `try/catch` в `importFromPicker` (ошибка пикера, чтения/копирования файла). `lib/core/media/media_cache.dart` — `remove(path)` (удаляет провайдеры файла из кэша, включая cacheWidth-варианты). `ExerciseRepository` принимает optional `MediaCache` и чистит кэш при `delete` (зарегистрирован в DI). `_pickAnimation` — `try/catch` + SnackBar с ключом `exerciseFormAnimationError` (добавлен в арб). Новые тесты: `media_store_test` (2), `media_cache_test` (2), `exercise_form_screen_test` (SnackBar без краха). Локально: format/analyze 0, `flutter test` 391 зелёных.

### Задача 9.3: Сохранение невалидной программы и попап
- **Статус:** [x] выполнена
- **Модуль:** фичи/программы
- **Ветка:** `task/9.3-program-validation`
- **Описание:** `ProgramBuilderScreen._persist` валидирует только имя; `create/update(days:)` не проверяет упражнения. Полный `ProgramValidator.validate` перед записью в обеих save-точках; при `!isValid` — `AlertDialog` со списком ошибок и действиями «Продолжить редактирование»/«Выйти» (вместо SnackBar day-builder'а). Запрет сохранения пустой программы.
- **Критерии приемки:** программа без основного упражнения в дне не сохраняется; показывается, что именно заполнить.
- **Тесты:** `program_builder_screen_test` (попап для каждого сценария недостаточных данных, «Продолжить»/«Выйти», валидная сохраняется), `program_day_builder_screen_test` (попап вместо SnackBar + список ошибок).
- **Реализация:** новый `lib/features/programs/ui/program_validation_dialog.dart` — общий `AlertDialog` (список ошибок + «Продолжить редактирование»/«Выйти»). `ProgramBuilderScreen._save` — сначала `_structureErrors()` (полный `ProgramValidator.validate`: имя, дни, упражнения из БД по индексу дня); при ошибках — диалог (без спиннера), «Выйти» покидает построитель. `ProgramDayBuilderScreen._save` — `ProgramValidationException` показывает тот же диалог вместо SnackBar (спиннер останавливается до показа). Ключи l10n `programValidation*`, ключ `programBuilderFillAllDays` удалён. Заодно поправлен дате-зависимый флейк `week_plan_screen_test` (tap мимо вьюпорта → `ensureVisible`). Локально: format/analyze 0, `flutter test` 392 зелёных.
- **Зависимости:** Этапы 2–5, 9.1.

### Задача 9.4: Удалённая программа в плане недели + тултип
- **Статус:** [x] выполнена
- **Модуль:** фичи/workout
- **Ветка:** `task/9.4-week-plan`
- **Описание:** После удаления программы (FK-cascade) в плане остаются stale-айтемы: «Начать» ведёт в «День не найден», «Пропустить» — FK-исключение. Защитные проверки в `markSkipped/clearSkip/_start` + реактивная перезагрузка из 9.1. Тултипы стрелок периода берутся из фреймворка (`nextMonthTooltip`) — заменить на l10n «Предыдущая/Следующая неделя», поправить `integration_test/app_flow_test.dart`.
- **Критерии приемки:** удалённая программа исчезает из плана; действия по stale-айтему не падают.
- **Тесты:** `week_plan_screen_test` (удаление → айтем исчез; markSkipped по удалённому id без краша; тултипы «неделя»).
- **Реализация:** `WeekPlanController` — новый `dayExists(int)`, в `markSkipped`/`clearSkip` перед записью проверка существования дня, при stale-айтеме — только перезагрузка (без FK-вставки). `WeekPlanScreen._start` — проверка `dayExists`, при отсутствии SnackBar «День не найден» + перезагрузка вместо навигации. Тултипы стрелок переключения недели — новые l10n-ключи `weekPlanPrevWeek`/`weekPlanNextWeek` (заменяют `previousMonthTooltip`/`nextMonthTooltip`), правка `reloadWeekPlan` в `integration_test/app_flow_test.dart`. Реактивность списка (9.1) уже удаляет айтем после удаления программы — тест «удаление → айтем исчез».

### Задача 9.5: Селект «Динамика» в профиле
- **Статус:** [x] выполнена
- **Модуль:** фичи/профиль
- **Ветка:** `task/9.5-profile-dropdown`
- **Описание:** `DropdownButton`/график читают `selectedMetric`/`chartPoints` вне замыкания `SignalBuilder` → нет пересборки, меню не меняет значение. Обернуть карточку метрики в собственный `SignalBuilder`.
- **Критерии приемки:** можно выбрать любую из 10 метрик, график перестраивается.
- **Тесты:** `profile_screen_test` (выбор «Рост» → `DropdownButton.value == BodyMetric.height`, график по данным роста; слабый тест переписать).
- **Реализация:** `_MetricChartCard` в `profile_screen.dart` обёрнут в собственный `SignalBuilder` — `selectedMetric` и `chartPoints` читаются внутри его замыкания и передаются в `_MetricLineChart` как неизменяемый параметр `points` (ранее читались в `build` карточки/графика, из-за чего сигналы не отслеживались и селект не обновлял ни себя, ни график). Слабый тест «график строится по выбранной метрике» переписан в «выбор метрики меняет Dropdown и график»: проверяет `DropdownButton.value == BodyMetric.height` и y-координаты точек графика `[180, 179, 180]` (на старом коде падает — `Actual: weight`).

### Задача 9.6: Стартовый набор упражнений
- **Статус:** [x] выполнена
- **Модуль:** core/exercises
- **Ветка:** `task/9.6-exercise-seed`
- **Описание:** `ExerciseSeeder` (16 упражнений, `assets/data/exercises_seed.json`, анимации) нигде не вызывается. Вынести bootstrap в `lib/app/bootstrap.dart` (registerCoreServices → ExerciseSeeder.seed → ReminderService.initialize), `main()` ждёт до `runApp`; вызов в `_rebuildAfterImport`.
- **Критерии приемки:** при первом запуске в БД 16 упражнений с мышцами/тегами, повторный запуск не дублирует.
- **Тесты:** `test/app/bootstrap_test.dart` (bootstrap на memory-БД → 16 упражнений, флаг в app_meta, идемпотентность).
- **Реализация:** `lib/app/bootstrap.dart` — `bootstrap({container, database, mediaStore, seedJsonLoader})`: `registerCoreServices` → `seedExercises` → `ReminderService.initialize` (в `catch (_)` — напоминания не блокируют запуск). `main()`: `await bootstrap()` перед `runApp`. `register_core_services.dart`: новый `seedExercises(sl, …)` (ExerciseSeeder с `rootBundle.loadString(ExerciseSeeder.seedAssetPath)`) вызывается и в `_rebuildAfterImport` после `ReferenceSeeder` — импортированная БД тоже получает стартовый набор. `ExerciseSeeder.seedAssetPath` — общий путь к `assets/data/exercises_seed.json`. Тест `bootstrap_test`: `TestWidgetsFlutterBinding.ensureInitialized()`, bootstrap на memory-БД + фейковый MediaStore/загрузчик JSON → 16 упражнений и флаг `exercises_seeded` в app_meta; повторный bootstrap не дублирует.

# Этап 10. Доработки

### Задача 10.1: Детализация групп мышц
- **Статус:** [x] выполнена
- **Модуль:** core + фичи/упражнения
- **Ветка:** `task/10.1-muscles-detail`
- **Описание:** Колонка `parentKey` в `muscle_groups`, schemaVersion 1→2 + миграция + реген `app_database.g.dart`/`drift_schemas`; дельты: `shoulders_front`/`middle` (front), `shoulders_rear` (back); регионы в `MuscleDiagram`; группировка в форме упражнения.
- **Критерии приемки:** выбор подгрупп дельт, корректная подсветка на схеме.
- **Тесты:** `reference_seeder_test`, `muscle_diagram_test`, `exercise_form_screen_test`, round-trip/миграция.
- **Реализация:** `muscle_groups.parentKey` (nullable text), `appDatabaseSchemaVersion` 1→2, `onUpgrade`: `m.addColumn` + повторный `ReferenceSeeder.seed()` (добавляет подгруппы). `ReferenceSeeder.muscleParentKeys` — карта `shoulders_*` → `shoulders`, сидится 3 дельты (всего 18 групп). `MuscleDiagram._frontRegions`: подрегионы `shoulders_front`/`shoulders_middle` поверх полных «плеч»; `_backRegions`: новый `shoulders_rear`. Форма упражнения: `_groupedMuscles` группирует по `parentKey`, подгруппы с отступом под родителем, у родителя «Плечи» остаются чипы выбора. Заодно починен устаревший `drift_schemas/drift_schema_v1.json` (был только `app_meta`) и добавлен `drift_schema_v2.json`.
- **Тесты (факт):** `migration_test.dart` — открытие v1-БД (raw sqlite, user_version=1) → после миграции 3 дельты с `parentKey=shoulders` и `user_version=2`; `reference_seeder_test` — 18 групп, `parentKey` на дельтах, идемпотентность; `muscle_diagram_test` — 18 регионов, дельты разнесены по видам; `exercise_form_screen_test` — подгруппы видны под «Плечи», выбор «Передняя дельта» сохраняется в `exercise_muscles`. Всего 404 теста зелёные.

### Задача 10.2: Чекбокс «больше не показывать» предупреждения
- **Статус:** [x] выполнена
- **Модуль:** фичи/workout
- **Ветка:** `task/10.2-warning-dismiss`
- **Описание:** Таблица `program_warning_dismissals` (programId FK cascade), schemaVersion→3, `isDismissed`/`dismiss`; попап при старте показывается, только если `visibleWarnings.notEmpty && !dismissed`; `CheckboxListTile` в диалоге.
- **Критерии приемки:** отметка «не показывать» для программы запоминается, повторно попап не выводится.
- **Тесты:** `workout_prepare_screen_test` (чекбокс, persist, повторный старт без диалога), репозиторий dismiss.
- **Реализация:** таблица `program_warning_dismissals` (`program_id` FK cascade, `dismissed_at`), `appDatabaseSchemaVersion` 2→3, `onUpgrade`: `m.createTable`. `ProgramRepository.isWarningDismissed`/`dismissWarnings` (идимпотентный delete+insert в транзакции, `_notify`). `WorkoutPrepareController.programId` — новый сигнал (выставляется из `day.programId`). `_start`: диалог показывается только при `visibleWarnings.notEmpty && !dismissed`; выделенный `_WarningDialog` (StatefulWidget) с `CheckboxListTile` «Больше не показывать для этой программы» (новый ключ `workoutWarningsDontShow`), при продолжении с чекбоксом вызывает `dismissWarnings`. Дроп `drift_schema_v3.json`.
- **Тесты (факт):** `migration_test` — путь 1→3 (parentKey+сид+createTable) и отдельный 2→3 (вставка/чтение dismissal, `user_version=3`); `program_repository_test` — 4 теста dismiss (false→true, повторный dismiss идемпотентен, независимость программ, каскад при удалении программы); `workout_prepare_screen_test` — чекбокс скрывает диалог при повторном старте, без чекбокса отметка не сохраняется. Всего 411 тестов зелёные.

### Задача 10.3: Углублённая статистика
- **Статус:** [x] выполнена
- **Модуль:** фичи/прогресс
- **Ветка:** `task/10.3-deep-stats`
- **Описание:** `StatsAggregator.exerciseProgression` (max вес/время/дистанция/повторы по датам); из прогресса — деталь дня/сессии и экран динамики упражнения (fl_chart).
- **Критерии приемки:** из прогресса видно состав тренировки за день и прогресс по упражнению.
- **Тесты:** `stats_aggregator_test`, widget-тесты навигации/графика. (Зависит от 10.5.)
- **Зависимости:** 10.5.
- **Реализация:** в `StatsAggregator` добавлен `ProgressionPoint` (date + metric) и `exerciseProgression(exerciseId)` — прогрессия за всё время по датам тренировок, сортировка по дате: strength — max вес за дату, bodyweight — сумма повторов, running — сумма дистанции, plank — сумма времени. Новый экран `ExerciseProgressionScreen` (`/progress/exercise/:id`): LineChart (fl_chart) с датами по оси X, значением метрики по типам упражнения, максимумом и списком дат с единицами. Новый экран `DayDetailScreen` (`/progress/day?start=&end=`) — сессии в диапазоне среза (день/неделя/месяц) со составом и длительностью, тап открывает детали сессии `/history/:id`. Входные точки на `ProgressScreen`: тап по бару графика «Тренировки по срезам» (с тултипом) и кнопка `trending_up` «Открыть динамику» на карточке «Прогресс метрики». l10n: `progressProgression`, `progressProgressionOpen`, `progressProgressionEmpty`, `progressProgressionMax`, `progressDayEmpty`. В `test_services` зарегистрирован `StatsAggregator` для deep-link тестов роутера.
- **Тесты (факт):** `stats_aggregator_test` (3 новых: max вес по датам с сортировкой, сумма повторов bodyweight по датам, неизвестное упражнение — пусто); `exercise_progression_screen_test` (3: график/максимум/список дат, пусто, не найдено); `day_detail_screen_test` (3: сессии диапазона со составом, заголовок одиночного дня + пусто, тап открывает детали сессии); `progress_screen_test` (2: кнопка динамики открывает экран, тап по бару открывает деталь дня); `router_test` (2 deep-link: динамика упражнения и детали дня). Всего 434 теста зелёные.

### Задача 10.4: UX конструктора программ
- **Статус:** [x] выполнена
- **Модуль:** фичи/программы
- **Ветка:** `task/10.4-program-ux`
- **Описание:** При добавлении упражнения сразу открывать экран параметров (`_openExerciseParams`); клик по строке дня ведёт в day builder, настройки — на иконке `tune`.
- **Критерии приемки:** flow добавления упражнения — один переход без лишних тапов.
- **Тесты:** `program_day_builder_screen_test`, `program_builder_screen_test` (обновлённые).
- **Реализация:** в `ProgramDayBuilderScreen._addExercise` после выбора упражнения из диалога сразу вызывается `_openExerciseParams(draft)` — экран параметров открывается без отдельного тапа (черновик сохраняется в день до открытия, при отмене остаётся «Параметры не заданы»). В `ProgramBuilderScreen` тап по строке дня ведёт в day builder (`_openDayFill`), настройки дня (день недели/напоминание) остались на иконке `tune`.
- **Тесты (факт):** `program_day_builder_screen_test` переведён на роутер (маршруты day builder и параметров); «добавление упражнения через диалог выбора» заменён на «добавление упражнения сразу открывает экран параметров» (выбор → экран «Параметры упражнения» → отмена → строка с «Параметры не заданы»); тесты с добавлением через диалог закрывают экран параметров через back; «невалидные позиции блокируют сохранение» проверяет, что метрики не сохраняются (sets/reps == null); финальный тест заполняет параметры сразу после выбора без тапа по `tune`. `program_builder_screen_test`: тап «День 1» теперь открывает «Наполнение дня», настройки — через `tune`; `program_builder_reminder_test` открывает диалог настроек через `tune`. Всего 434 теста зелёные.

### Задача 10.5: Тип упражнения «свой вес»
- **Статус:** [x] выполнена
- **Модуль:** все фичи
- **Ветка:** `task/10.5-bodyweight`
- **Описание:** `ExerciseType.bodyweight` (без параметра веса); пройти все исчерпывающие switch (валидатор, параметры, day-builder, workout_run, контроллер, prepare, каталог, статистика); l10n `exerciseTypeBodyweight` = «Свой вес». Схема БД не меняется (TEXT by name).
- **Критерии приемки:** новый тип создаётся, заполняется повторами без веса, считается в статистике.
- **Тесты:** модели/конвертеры/валидатор/params/workout_run/контроллер/агрегатор.
- **Реализация:** в `enum ExerciseType` добавлен `bodyweight` (схема не меняется — TEXT by name). Пройдены все исчерпывающие switch/выражения: `ProgramDayExerciseValidator` (sets+reps, вес игнорируется), `ProgramDayExerciseParamsScreen` (поля подходы+повторения, вес не выводится), `ProgramDayBuilderScreen._metricsSummary`/`_typeLabel`, `WorkoutPrepareScreen._paramsSummary` + иконки (`Icons.accessibility_new`), `WorkoutController._buildResult` (reps, без веса), `WorkoutRunScreen` (ввод повторов без веса, иконка), `StatsAggregator.exerciseMetricPerSlice` (bodyweight = сумма повторов в срезе), `ProgressScreen._displayValues`, `HistoryScreen._formatSet`, `_typeLabel` в каталоге/детали/форме, `integration_test/app_flow_test` (выбор «Свой вес»). l10n `exerciseTypeBodyweight` = «Свой вес». В `assets/data/exercises_seed.json` «Отжимания» и «Подтягивания» переведены на `bodyweight`.
- **Тесты (факт):** `exercise_test` (ключ `bodyweight`), `program_day_exercise_validator_test` (3 новых: валидно sets+reps, без повторов невалидно, вес игнорируется), `exercise_seed_parser_test` (реальный seed содержит bodyweight — «Отжимания»), `params_screen_test` (набор полей bodyweight, сохранение повторов без веса), `day_builder_screen_test` (сводка «3 × 15 · отдых 45 с» без «кг»), `workout_prepare_screen_test` («3 × 15 повт» без веса), `workout_run_screen_test` (ввод повторов, нет «Вес (кг)»), `workout_controller_test` (фиксация повторов, weightKg null), `stats_aggregator_test` (метрика по срезам = 30 повторов в срезе), `history_screen_test` («3. 15 повт»). Всего 421 тест зелёные.

### Задача 10.6: Название, быстрый старт, README
- **Статус:** [x] выполнена
- **Модуль:** проект
- **Ветка:** `task/10.6-app-name-quickstart`
- **Описание:** Отображаемое имя приложения «Личный тренер» (android:label, MaterialApp title, iOS/web); кнопка «Быстрый старт» ближайшей тренировки на экране плана (`nextPending` в WeekPlanController → `/workout/prepare/:programDayId`); README приложения — раздел «Быстрый старт» (команды запуска, генерация файлов, тесты, сборка).
- **Критерии приемки:** имя в лаунчере «Личный тренер», кнопка старта видна только при наличии pending-дня, README покрывает быстрый старт.
- **Тесты:** widget-тест заголовка, `week_plan_screen_test` (кнопка быстрого старта).
- **Реализация:** имя «Личный тренер» установлено в `MaterialApp.title`, `android:label` (AndroidManifest), `web/index.html` (title + apple-mobile-web-app-title) и `web/manifest.json` (name/short_name); iOS-каталога в проекте нет. В `WeekPlanController` добавлен геттер `nextPending` — ближайший pending-день начиная с сегодня (иначе первый pending недели). На `WeekPlanScreen` под переключателем недели добавлена кнопка «Быстрый старт» (отображается только при наличии pending-дня), ведёт на `/workout/prepare/:programDayId`. README приложения (`fitnessappai/README.md`): раздел «Быстрый старт» с командами (pub get, gen-l10n, build_runner, run, test, build apk) и пояснением про закоммиченные генерируемые файлы.
- **Тесты (факт):** `test/app/app_title_test.dart` (новый, title = «Личный тренер»); `week_plan_screen_test` +2 (кнопка видна при pending-дне и открывает подготовку; скрыта без pending-дней), тест «пропуск и отмена пропуска» дополнен `ensureVisible`; `week_plan_controller_test` +1 (nextPending: возвращает ближайший с сегодня, после пропуска — предыдущий pending). Всего 438 тестов зелёные.

# Этап 11. UX, настройки, домашний экран

### Задача 11.1: Маркировка обязательных полей (звёздочка)
- **Статус:** [x] выполнена
- **Модуль:** фичи/упражнения
- **Ветка:** `task/11.1-required-marker`
- **Описание:** Обязательное поле «Название» в форме упражнения помечается звёздочкой (`«Название» *` в error-цвете темы).
- **Критерии приемки:** поле «Название» имеет метку со звёздочкой; валидация пустого названия не изменена.
- **Тесты:** widget-тест метки со звёздочкой; существующие тесты формы обновлены на `«Название *»`.
- **Реализация:** в `exercise_form_screen.dart` у поля имени метка заменена на `Text.rich` (`Название *`, звёздочка в `colorScheme.error`). Тест-файл формы: 5 локаторов обновлены на `'Название *'`, добавлен тест «поле названия отмечено звёздочкой как обязательное». Заодно исправлен pre-existing flaky-баг тестов плана недели: `_PlannedWorkoutCard` использовал реальный `DateTime.now()` вместо инъекционного часов — в `WeekPlanScreen` добавлен параметр `clock`, `isToday` прокинут в карточку; `week_plan_screen_test` переведён на фиксированную дату (понедельник). Тесты больше не зависят от дня недели.
- **Тесты (факт):** `exercise_form_screen_test` +1; `week_plan_screen_test` детерминирован (были 2 flaky-фейла по субботам из-за ленивой отрисовки 7-й карточки и переноса сессии за границу недели). Всего 439 тестов зелёные.

### Задача 11.2: Чекбокс «скрыть необязательные поля»
- **Статус:** [x] выполнена
- **Модуль:** БД (схема v3→v4) + фичи/упражнения + workout
- **Ветка:** `task/11.2-hide-optional`
- **Описание:** Колонка `exercises.hide_optional` (bool, default false), schemaVersion 3→4, миграция `m.addColumn` + дамп `drift_schema_v4.json`, реген `.g.dart`. Модель `Exercise.hideOptional`. В форме упражнения — `CheckboxListTile` «Скрывать необязательные поля в тренировке». При включённом флаге на экранах подготовки/выполнения тренировки и в деталях упражнения скрываются необязательные блоки (описание, техника, ошибки, отдых/вес-сводка).
- **Критерии приемки:** флаг сохраняется; миграция 3→4; при флаге скрываются необязательные поля в тренировке и деталях.
- **Тесты:** миграция 3→4, форма сохраняет флаг, деталь/подготовка скрывают поля при флаге.

### Задача 11.3: Изображения упражнений
- **Статус:** [x] выполнена
- **Модуль:** фичи/упражнения + media
- **Ветка:** `task/11.3-exercise-image`
- **Описание:** Отдельный выбор миниатюры-изображения в форме (в дополнение к анимации) через `MediaStore.importFromPicker` с фильтром изображений; превью и удаление. Починить баг `_populate`: сейчас `animationPath ?? thumbnailPath` склеивает поля при редактировании — хранить их раздельно. Миниатюра показывается в карточке списка и как fallback.
- **Критерии приемки:** thumbnail сохраняется отдельно от анимации; миниатюра в списке.
- **Тесты:** форма сохраняет thumbnail отдельно, список выводит миниатюру.

### Задача 11.4: Стартовый экран с анимацией
- **Статус:** [x] выполнена
- **Модуль:** app
- **Ветка:** `task/11.4-splash`
- **Описание:** `main()` → `runApp(SplashGate())`: анимированная заставка (логотип с Fade/Scale-анимацией через `AnimationController`) на время `bootstrap()`, затем плавный переход в `FitnessAppAi`. bootstrap остаётся инъектируемым для тестов.
- **Критерии приемки:** заставка видна во время инициализации, приложение стартует после.
- **Тесты:** стартовый тест — заставка видна, после bootstrap появляется приложение.

### Задача 11.5: Фильтр тренировок по мышцам в конструкторе
- **Статус:** [x] выполнена
- **Модуль:** фичи/программы
- **Ветка:** `task/11.5-muscle-filter`
- **Описание:** В диалоге выбора упражнения day-builder — селект «Все / группа мышц» (из `getAllMuscleGroups` + `muscleGroupsByExercise`), фильтрует список.
- **Критерии приемки:** фильтр по группе оставляет только соответствующие упражнения.
- **Тесты:** фильтр по группе оставляет только подходящие упражнения.

### Задача 11.6: Красивая анимация мышц
- **Статус:** [x] выполнена
- **Модуль:** фичи/упражнения
- **Ветка:** `task/11.6-muscle-animation`
- **Описание:** `MuscleDiagram`: плавная анимация подсветки (`TweenAnimationBuilder` + lerp интенсивности), градиентная заливка регионов и мягкая тень/свечение. Регионы и API не меняются.
- **Критерии приемки:** подсветка анимируется плавно, регионы прежние.
- **Тесты:** `muscle_diagram_test` зелёный, анимация интенсивности.

### Задача 11.7: Ошибка создания тренировки в debug (тесты)
- **Статус:** [x] выполнена
- **Модуль:** фичи/workout
- **Ветка:** `task/11.7-workout-debug`
- **Описание:** Воспроизвести полный флоу «программа → день → подготовка → выполнение → финиш → saveSession» в тестах; проверить вероятные места краша в debug: `WakelockPlusService` в тестах, `_submit` для running (`_parseDouble(...)!`), hold-таймер планки, `saveSession` с пустыми результатами. Найденное чинить + регрессионные тесты.
- **Критерии приемки:** полный флоу создания тренировки проходит в debug без краша.
- **Тесты:** флоу репозиторий/контроллер/экран.

### Задача 11.8: Удаление базовых упражнений
- **Статус:** [x] выполнена
- **Модуль:** фичи/упражнения + программы
- **Ветка:** `task/11.8-exercise-delete`
- **Описание:** Решение: **блокировать удаление**, если упражнение используется в программах. `ExerciseRepository.referencedPrograms(exerciseId)` (список названий). В деталях упражнения удаление доступно для всех типов; при наличии ссылок — диалог со списком программ, удаление не выполняется; неиспользуемые удаляются (медиа чистятся).
- **Критерии приемки:** при ссылках показывается диалог со списком программ, удаление блокируется; неиспользуемые удаляются.
- **Тесты:** repo `referencedPrograms`, диалог при ссылках, удаление неиспользуемого, позиции программы не ломаются.

### Задача 11.9: Экран настроек + перенос синхронизации
- **Статус:** [x] выполнена
- **Модуль:** фичи/settings + profile + sync
- **Ветка:** `task/11.9-settings`
- **Описание:** Новый `/settings`: секция «Синхронизация» (перенос UI/контроллера экспорт/импорт из `SyncScreen`), секция «Тема» (место под 11.10). Удалить маршрут `/sync` и `SyncScreen`; в профиле ссылка «Настройки» (Icons.settings) вместо «Синхронизация».
- **Критерии приемки:** `/settings` доступен, синхронизация работает оттуда, `/sync` удалён.
- **Тесты:** роутер `/settings`, настройки показывают экспорт/импорт, ссылка профиля.

### Задача 11.10: Переключение темы (светлая/тёмная)
- **Статус:** [x] выполнена
- **Модуль:** app/theme + settings
- **Ветка:** `task/11.10-theme-toggle`
- **Описание:** `AppTheme.light()`; `ThemeSettingsRepository` на `app_meta` (ключ `theme_mode`); `ThemeController` (ValueNotifier<ThemeMode>), загрузка до `runApp`; `MaterialApp` слушает контроллер. В настройках — `SegmentedButton` «Тёмная/Светлая».
- **Критерии приемки:** выбор темы персистится и применяется при старте.
- **Тесты:** переключение персистится, `MaterialApp` меняет brightness.

### Задача 11.11: Проверка отступов
- **Статус:** [x] выполнена
- **Модуль:** все UI
- **Ветка:** `task/11.11-spacing`
- **Описание:** Аудит отступов: единый gutter 16, увеличение воздушных промежутков на карточках/списках/формах. Полный прогон тестов после визуального прохода.
- **Критерии приемки:** отступы согласованы, нет переполнений.
- **Тесты:** полный прогон зелёный.

### Задача 11.12: Экспорт программы и истории в JSON для LLM
- **Статус:** [x] завершена
- **Модуль:** фичи/программы + прогресс
- **Ветка:** `task/11.12-llm-export`
- **Описание:** `LlmExportService`: `programToJson(id)` и `historyToJson()` (сессии: дата, программа, упражнения, подходы). В списке программ — PopupMenu «Скопировать JSON», на истории — AppBar-действие; копирование в буфер (`Clipboard`) + SnackBar.
- **Критерии приемки:** JSON валидный, кнопки копируют и показывают SnackBar.
- **Тесты:** сервис отдаёт валидный JSON с ожидаемыми полями, кнопки копируют.

### Задача 11.13: Перенос кнопки быстрого старта с календаря
- **Статус:** [x] завершена
- **Модуль:** фичи/workout
- **Ветка:** `task/11.13-quickstart-move`
- **Описание:** Убрать `_QuickStartBar` с `WeekPlanScreen`; вынести переиспользуемый виджет/логику старта. `nextPending` остаётся в контроллере.
- **Критерии приемки:** на плане кнопки нет, логика старта переиспользуется.
- **Тесты:** на плане кнопки нет.

### Задача 11.14: Домашний экран
- **Статус:** [x] реализована (PR #62)
- **Модуль:** БД (схема v4→v5) + app + фичи
- **Ветка:** `task/11.14-home`
- **Описание:** Колонка `programs.is_active` (bool default false), schemaVersion 4→5, миграция + дамп `drift_schema_v5.json`; `Program.isActive`, `ProgramRepository.getActiveProgram`/`setActive` (в транзакции очищает остальных). В списке программ — «Сделать активной» + бейдж «Активная». Новый маршрут `/home` — первый таб навигации «Главная» (6 вкладок), `initialLocation: '/home'`. Контент: кнопка «Быстрый старт» (11.13) при `nextPending`; карточка активной программы (имя, ближайший день по `dayOfWeek` и его упражнения); история — последние 3 тренировки со статистикой, тап → `/history/:id`; если программы нет — ссылка на экран программ; если есть, но нет активной — подсказка «Сделайте программу активной».
- **Критерии приемки:** одна активная программа, домашний экран стартовый, карточки активной программы и последних тренировок.
- **Тесты:** миграция 4→5, repo getActive/setActive, home-controller, widget (быстрый старт, пустое состояние), роутер initialLocation.

**Зависимости этапа 11:** 11.2 (схема v4) → 11.14 (схема v5); 11.9 → 11.10; 11.13 → 11.14; 11.12 независима; 11.8 опирается на репозитории.

---

# Этап 12. Доработки по обратной связи

### Задача 12.1: Плавающая кнопка быстрого старта на главной
- **Статус:** [x] реализована
- **Модуль:** фичи/home + workout
- **Ветка:** `task/12.1-home-fab`
- **Описание:** Полноширинную кнопку «Быстрый старт» (`QuickStartBar`) в начале списка `HomeScreen` заменить на `Scaffold.floatingActionButton` — `FloatingActionButton.extended` с иконкой `Icons.play_arrow` и подписью «Быстрый старт» (`l10n.weekPlanQuickStart`), `heroTag: 'home-fab'`. Показывается только при `nextPending != null`. Логика `startPlannedWorkout` сохраняется (используется и `WeekPlanScreen`); виджет `QuickStartBar` из `quick_start_bar.dart` удаляется.
- **Критерии приемки:** при ближайшей тренировке на главной плавающая кнопка с иконкой play и подписью; тап открывает подготовку к тренировке; при отсутствии `nextPending` FAB скрыта.
- **Тесты:** `home_screen_test` — FAB видна, тап запускает тренировку; без `nextPending` FAB нет; `week_plan_screen_test` не регрессирует.

### Задача 12.2: Контраст бейджа «Перенесено» на плане недели
- **Статус:** [x] реализована
- **Модуль:** фичи/workout
- **Ветка:** `task/12.2-rescheduled-color`
- **Описание:** В `_StatusBadge` (`week_plan_screen.dart`) текст всех статусов раскрашен `onSurfaceVariant` — на фонах `tertiary` («Перенесено») и `primary` («Выполнено») он плохо читается в обеих темах. Сменить на пер-статусный цвет: `pending` → `onSurfaceVariant`, `performed` → `onPrimary`, `rescheduled` → `onTertiary`, `skipped` → `onError`.
- **Критерии приемки:** «Перенесено» и «Выполнено» контрастны в светлой и тёмной темах; остальные статусы не деградируют.
- **Тесты:** widget-тест цвета текста бейджа для каждого статуса в светлой и тёмной темах.

### Задача 12.3: Импорт/экспорт БД без исключений в debug
- **Статус:** [x] реализована
- **Модуль:** фичи/settings + sync
- **Ветка:** `task/12.3-sync-errors`
- **Описание:** `SyncController.exportDatabase`/`importDatabase` ловят только `on Exception`, а `share_plus` на Linux бросает `UnimplementedError` (это `Error`) — в debug-режиме десктопа тап по «Экспортировать БД» роняет приложение. Расширить обработку: ловить `Error` тоже (включая `UnimplementedError`/`MissingPluginException`), выставлять `statusText` + `hasError` без повторного броска. На Linux `_defaultShare` — показывать статус о недоступности расшаривания (или фолбэк на путь файла), не падать.
- **Критерии приемки:** тап по экспорту/импорту в debug не вызывает необработанного исключения; в UI показывается понятный статус.
- **Тесты:** `settings_screen_test` — инъекция `shareFile`/`pickFile`, бросающих `Error` → контроллер возвращает `false`, статус ошибки, без rethrow.

### Задача 12.4: Иконка настроек в AppBar профиля
- **Статус:** [x] реализована
- **Модуль:** фичи/profile
- **Ветка:** `task/12.4-profile-settings`
- **Описание:** Перенести пункт «Настройки» из `_LinksCard` (`ProfileScreen`) в `AppBar.actions` — `IconButton` с `Icons.settings_outlined` на уровне заголовка справа, тап → `context.push('/settings')`. Из `_LinksCard` оставить только «Противопоказания».
- **Критерии приемки:** иконка настроек справа в AppBar профиля; тап открывает `/settings`; ListTile «Настройки» в списке ссылок удалён.
- **Тесты:** `profile_screen_test` — иконка в AppBar, тап открывает настройки, ListTile удалён (тест «ссылка „Настройки"» переписан).

### Задача 12.5: Прогресс-метрики только по выполненным упражнениям
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/progress + workout
- **Ветка:** `task/12.5-progress-performed`
- **Описание:** `ProgressController._load` заполняет `exercises` из `exerciseRepository.getAll()` (весь каталог) — в выпадающем списке «Прогресс метрики» видны невыполнявшиеся упражнения. Добавить получение id выполненных упражнений (distinct `exerciseId` из `workout_set_results` за всё время — `WorkoutRepository.getPerformedExerciseIds()` или метод `StatsAggregator`) и фильтровать список упражнений по ним. Если выбранного id больше нет — сбросить на первый; при пустом списке график скрыт (текущее поведение `exercises.isNotEmpty`).
- **Критерии приемки:** в дропдауне метрик только упражнения с хотя бы одним выполненным подходом (за всё время).
- **Тесты:** `stats_aggregator_test`/`progress_screen_test` — каталог из 3 упражнений, выполнено 2 → в списке только 2; ни одного — график скрыт.

### Задача 12.6: Выбор миниатюры/анимации без ложной ошибки
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** core/media + фичи/exercises
- **Ветка:** `task/12.6-media-pick`
- **Описание:** `MediaStore.importFromPicker` копирует файл через `File(picked).readAsBytes()` в `getApplicationDocumentsDirectory()` — в debug на Linux `path_provider` бросает `MissingPlatformDirectoryException` (нет XDG-каталогов), на Android content-URI не читаются `File`. В итоге пикер работает, но форма показывает `exerciseFormAnimationError`. Сделать чтение через `XFile(picked)` (поддержка content://), директорию провайдера с фолбэком (documents → support → temp), ошибку показывать только при реальном сбое копирования.
- **Критерии приемки:** выбор миниатюры/анимации в debug успешно копирует файл и ставит превью без SnackBar; при настоящей ошибке копирования — сообщение без краха.
- **Тесты:** `media_store_test` — чтение по XFile-пути, фолбэк провайдера; `exercise_form_screen_test` — успешный выбор без SnackBar, сбой копирования показывает SnackBar.

### Задача 12.7: Обязательный выбор мышц в форме упражнения
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/exercises
- **Ветка:** `task/12.7-muscles-required`
- **Описание:** `ExerciseFormScreen._save` не проверяет `_muscleSelections` — упражнение создаётся без мышц. При пустом выборе мышц на сохранении показывать ошибку (SnackBar `exerciseFormMusclesRequired` — новый l10n-ключ) и не сохранять. Действует при создании и при редактировании (единое правило).
- **Критерии приемки:** сохранение без выбранных мышц блокируется с сообщением; с выбранными — сохраняется.
- **Тесты:** `exercise_form_screen_test` — без мышц SnackBar + пустая БД; с мышцами сохраняется. Обновить существующие тесты, сохраняющие без мышц («выбор типа меняет тип упражнения» и др.).

### Задача 12.8: Кнопка «перейти к следующему дню» в конструкторе программы
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/programs
- **Ветка:** `task/12.8-builder-next-day`
- **Описание:** При сохранении с незаполненными днями `ProgramBuilderScreen._save` показывает диалог «У дня N должно быть хотя бы одно основное упражнение». Заменить UX: пока есть день без основного упражнения, нижняя кнопка называется «Заполнить день N» (первый незаполненный) и ведёт в `_openDayFill(index)`; «Сохранить» появляется только когда все дни заполнены. Статус заполненности пересчитывается после `_openDayFill` (для новой программы программа сохраняется в `_persist`, затем перезагрузка дней) и при `_load`. Диалог `_structureErrors` остаётся для прочих ошибок (название и т.п.).
- **Критерии приемки:** при незаполненном втором дне кнопка «Заполнить день 2» открывает его наполнение; после заполнения всех дней — «Сохранить».
- **Тесты:** `program_builder_screen_test` — 2 дня, второй пустой → кнопка «Заполнить день 2», тап переходит в day-builder; после заполнения обоих — «Сохранить». Обновить существующие сценарии сохранения.

### Задача 12.9: Фильтр по категориям в выборе упражнения
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/programs
- **Ветка:** `task/12.9-type-filter`
- **Описание:** В `_ExercisePickerDialog` (`program_day_builder_screen.dart`) рядом с фильтром по мышечной группе добавить фильтр по категории — `ExerciseType` (силовые, свой вес, планка, бег; подписи из `exerciseType*`). Новый `ExerciseType? _selectedType` + выпадающий список (l10n `programBuilderTypeFilter`); `_filtered` комбинирует оба фильтра.
- **Критерии приемки:** фильтр по категории оставляет только упражнения нужного типа и комбинируется с фильтром по мышцам.
- **Тесты:** `program_day_builder_screen_test` — фильтр по типу; комбинация «тип + мышцы».

**Зависимости этапа 12:** независимы, выполняются по порядку; 12.3 опирается на настройки (11.9), 12.5 — на workout/progress (этапы 4–5), 12.8 — на конструктор программ (3.3–3.4).

---

# Этап 13. Доработки по обратной связи (вторая волна)

Пункты (а)–(т) из отчёта пользователя. Нумерация 13.1–13.18 соответствует порядку отчёта; порядок выполнения — по зависимостям (см. ниже).

### Задача 13.1: Ошибка валидации названия не очищается при вводе
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/programs
- **Ветка:** `task/13.1-field-error-clear`
- **Описание:** В `ProgramBuilderScreen` после невалидного сохранения ошибка «Название программы обязательно» остаётся на экране даже когда пользователь уже ввёл название — она исчезает только при повторном нажатии кнопки. Включить авто-очистку ошибки при вводе: `AutovalidateMode.onUserInteraction` для `Form` конструктора (для единообразия — и для формы упражнения `ExerciseFormScreen`), либо сброс ошибки в `onChanged` контроллера названия.
- **Критерии приемки:** после набора текста в поле «Название» текст ошибки исчезает без повторного нажатия кнопки.
- **Тесты:** widget-тест — невалидное сохранение (ошибка видна), затем ввод названия → ошибка отсутствует.

### Задача 13.2: Кнопка «Сохранить» вместо перехода к следующему дню (регрессия 12.8)
- **Статус:** [x]
- **Модуль:** фичи/programs
- **Ветка:** `task/13.2-builder-next-day-fix`
- **Описание:** Пользователь сообщает: после заполнения дня в конструкторе программы активна кнопка «Сохранить», хотя остаются незаполненные дни — ожидалась кнопка перехода к следующему дню (логика задачи 12.8). Сначала воспроизвести сценарий тестом (создание программы на N дней, заполнение части дней, возврат в конструктор), затем найти причину расхождения в связке `_firstUnfilledDayIndex` / `_filledDayIndexes` (кандидаты: возврат из day-builder без `_save`, реордер дней и переиндексация `dayIndex` vs позиция в `_days`, статус после повторного `_openDayFill`). Исправить так, чтобы «Заполнить день N» показывалась, пока есть незаполненный день, а «Сохранить» — только когда все дни заполнены.
- **Критерии приемки:** после заполнения дня кнопка ведёт к следующему незаполненному дню; «Сохранить» появляется только при полном заполнении; реордер дней не ломает статус заполненности.
- **Тесты:** `program_builder_screen_test` — сценарий из отчёта; реордер + заполненность.

### Задача 13.3: Фокус на поле «Описание» после возврата из наполнения дня
- **Статус:** [x]
- **Модуль:** фичи/programs
- **Ветка:** `task/13.3-description-focus`
- **Описание:** После возврата с экрана наполнения дня фокус автоматически встаёт в поле «Описание» конструктора; это нежелательно, если описание уже заполнено. Снять фокус перед переходом в `_openDayFill` (`FocusScope.of(context).unfocus()` / `FocusManager.instance.primaryFocus?.unfocus()`), чтобы после возврата фокус не восстанавливался и клавиатура не открывалась.
- **Критерии приемки:** после возврата из day-builder ни одно поле конструктора не в фокусе.
- **Тесты:** widget-тест — после возврата `FocusScope`/`FocusManager` не указывает на поле «Описание».

### Задача 13.4: Счётчик удержания планки малозаметен
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/workout UI
- **Ветка:** `task/13.4-hold-counter`
- **Описание:** В `_buildExercise` (`workout_run_screen.dart`) текст `workoutRunHold(hold)` выводится стилем `titleMedium` обычным цветом и легко теряется на фоне. Сделать счётчик заметным в рамках новой логики таймера 13.17: крупный таймер (`displayLarge`/`headlineLarge`), выделенный блок (например `primaryContainer`/`surfaceContainerHigh` с отступами и скруглением), цель удержания — рядом в скобках. Выполнять после 13.17.
- **Критерии приемки:** счётчик планки крупный и контрастный в светлой и тёмной темах; цель видна.
- **Тесты:** widget-тест — наличие блока счётчика и текста цели, стиль крупного шрифта.

### Задача 13.5: Перенос значения счётчика удержания в результат планки
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/workout
- **Ветка:** `task/13.5-hold-carryover`
- **Описание:** Сейчас форма планки требует ручной ввод секунд (валидатор `exerciseParamsRequired`), а счётчик удержания никак не влияет на результат. По новой логике 13.17 таймер считает фактическое время вверх от нуля; при нажатии «Подход выполнен» с пустым полем времени подставлять текущее значение таймера, при заполненном — приоритет ручного ввода. Реализация: передавать сигнал фактического времени удержания в `_ExerciseInputForm` для планки.
- **Критерии приемки:** планка без ручного ввода сохраняет фактическое время удержания; с ручным вводом — ручное значение.
- **Тесты:** unit-тест `WorkoutController` + widget-тест формы планки (пустое поле → значение таймера).

### Задача 13.6: «1 минута» вместо «1.5» у планки на экране прогресса
- **Статус:** [x]
- **Модуль:** фичи/progress
- **Ветка:** `task/13.6-plank-minutes`
- **Описание:** `_StatCards` (`progress_screen.dart:124`) форматирует `plankTime.value.inMinutes` — целое количество минут, поэтому 90 секунд показываются как «1 мин». Форматировать планку с дробной частью (`_fmt(plankTime.value.inSeconds / 60)` — «1.5 мин») или как mm:ss. Проверить аналогичные места: `exercise_progression_screen.dart` (там уже `value / 60` через `_fmt` — ок), `history_screen.dart` (общая длительность сессии — проверить отображение планки в деталях сессии/дня).
- **Критерии приемки:** суммарные 90 секунд планки за период отображаются «1.5 мин» (или «1:30»).
- **Тесты:** widget-тест `progress_screen_test` — 90 с планки → «1.5».

### Задача 13.7: Нижняя навигация переносит текст на узком экране
- **Статус:** [x]
- **Модуль:** app/responsive
- **Ветка:** `task/13.7-nav-icons-only`
- **Описание:** `NavigationBar` (`adaptive_navigation.dart`) с шестью пунктами на компактной ширине переносит русские подписи на две строки. На узких экранах (по `AppBreakpoints`, например < 400 логических px) убирать подписи — `NavigationDestinationLabelBehavior.alwaysHide`, остаются только иконки; на широких экранах подписи сохраняются как сейчас.
- **Критерии приемки:** на узком экране подписи навигации отсутствуют, иконки не обрезаются; на широких — подписи остаются.
- **Тесты:** widget-тест с узким `MediaQuery` — тексты навигации отсутствуют; с широким — присутствуют.

### Задача 13.8: Таймер планки не стартует сразу после предыдущего упражнения
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/workout domain + UI
- **Ветка:** `task/13.8-plank-start-gate`
- **Описание:** `_startHoldTimerIfPlank()` запускается автоматически при старте тренировки и при переходе от предыдущего упражнения (`confirmSet`, `skipRest`, `nextExercise`, `_startRest`) — после подтягиваний сразу тикает удержание планки. По новой логике 13.17 таймер удержания не стартует сам: при переходе к планке показывать кнопку «Начать», запуск — только по тапу.
- **Критерии приемки:** при переходе к планке таймер не идёт и видна кнопка «Начать»; после тапа таймер запускается.
- **Тесты:** unit-тест `WorkoutController` — после `confirmSet` на предыдущем упражнении удержание не активно; после `startHold` активно; widget-тест — кнопка «Начать» присутствует до старта.

### Задача 13.9: «Убрать изображение» не влезает на узком экране
- **Статус:** [x]
- **Модуль:** фичи/exercises
- **Ветка:** `task/13.9-remove-icon`
- **Описание:** В `_thumbnailEditor`/`_animationEditor` (`exercise_form_screen.dart`) рядом с кнопкой выбора стоит `TextButton` с текстом `exerciseFormThumbnailRemove`/`exerciseFormAnimationRemove` («Убрать изображение»/«Убрать анимацию») — на узком экране строка переполняется. Заменить на `IconButton` с `Icons.delete_outline` (tooltip — тот же текст l10n).
- **Критерии приемки:** на узком экране строка выбора не переполняется; иконка удаляет превью.
- **Тесты:** widget-тест на узкой ширине — нет overflow, иконка удаления убирает превью.

### Задача 13.10: Изображение подогнано под блок (center/cover)
- **Статус:** [x]
- **Модуль:** фичи/exercises
- **Ветка:** `task/13.10-media-fixed-box`
- **Описание:** Превью миниатюры/анимации в форме (`exercise_form_screen.dart`) и в деталях упражнения (`exercise_detail_screen.dart`) используют `fit: BoxFit.contain` без фиксированной ширины — блок «прыгает» по пропорциям файла. Зафиксировать блок (`width: double.infinity` + фиксированная высота 120/160/220) и `BoxFit.cover` с центрированием, как уже сделано в `_ExerciseMedia` экрана выполнения и в списке упражнений (`exercises_screen.dart`).
- **Критерии приемки:** превью всегда занимает блок целиком, обрезка по центру, блок фиксированного размера.
- **Тесты:** widget-тест — размер превью соответствует фиксированному блоку.

### Задача 13.11: Сбрасывается активность программы при редактировании
- **Статус:** [x]
- **Модуль:** фичи/programs
- **Ветка:** `task/13.11-active-program`
- **Описание:** `_persist` и `_structureErrors` (`program_builder_screen.dart`) создают `Program` без `isActive` (по умолчанию false), а `ProgramRepository.update` пишет `isActive` из модели — после редактирования активной программы на главном экране активная программа пропадает. Сохранять текущее значение: в `_load` запоминать `detail.program.isActive` и передавать его в обе конструкции `Program`.
- **Критерии приемки:** после редактирования активной программы флаг `isActive` не сбрасывается; `getActiveProgram` возвращает её.
- **Тесты:** репозиторий-тест `update` с `isActive: true` → флаг сохраняется; widget-тест редактирования активной программы → после сохранения активная программа на месте.

### Задача 13.12: Разминка перед тренировочным днём
- **Статус:** [x]
- **Модуль:** core/database + фичи/programs + фичи/workout
- **Ветка:** `task/13.12-warmup`
- **Описание:** Добавить необязательную разминку:
  * Схема v6: колонка `program_days.warmupMinutes` (int, nullable) + `m.addColumn` в `onUpgrade`, `appDatabaseSchemaVersion = 6`; перегенерация `app_database.g.dart` (`dart run build_runner build`) и дампа `drift_schemas/drift_schema_v6.json`.
  * Модель `ProgramDay.warmupMinutes` + сохранение в `ProgramRepository` (`create`, `update`/`_replaceDays`, `updateDay`).
  * UI: в `_DaySettingsDialog` поле «Разминка, мин» (необязательное, digitsOnly); вывод в тайле дня и в `WorkoutPrepareScreen`.
  * Экран разминки: при `warmupMinutes > 0` в `WorkoutPrepareScreen._start` открывать экран разминки (обратный отсчёт, кнопка «Пропустить»), после завершения/пропуска — переход к `/workout/run`; без разминки — сразу тренировка.
  * Звуковой сигнал по завершении разминки — в 13.16.
- **Критерии приемки:** у дня с заданной разминкой перед тренировкой показывается экран с таймером и «Пропустить»; без разминки — сразу тренировка; время разминки сохраняется при редактировании программы.
- **Тесты:** тест миграции/схемы v6, repository-тест сохранения `warmupMinutes`, widget-тесты диалога дня, экрана разминки и перехода.

### Задача 13.13: Импорт БД: ограничение выбора файлов и некорректный формат
- **Статус:** [x]
- **Модуль:** фичи/settings + sync
- **Ветка:** `task/13.13-import-validation`
- **Описание:** Фильтр пикера (`FileType.custom` + `allowedExtensions: sqlite/db/sqlite3` в `SyncController._defaultPick`) на ряде платформ не ограничивает выбор. Усилить валидацию: проверка SQLite magic-bytes («SQLite format 3\0») в `LocalFileSyncService._validate` до копирования и понятное сообщение для файлов-не-БД; сохранить `integrity_check` и `user_version`. Убедиться, что выбор случайного файла не роняет приложение и даёт понятный статус в UI.
- **Критерии приемки:** импорт файла-не-БД показывает «Файл не является базой данных FitnessAppAI» без краха; корректный файл импортируется.
- **Тесты:** `local_file_sync_service_test`/`sync_controller_test` — битый файл, не-SQLite файл, файл с другой версией схемы.

### Задача 13.14: Экспорт БД в файловую систему устройства
- **Статус:** [x]
- **Модуль:** фичи/settings + sync
- **Ветка:** `task/13.14-export-save`
- **Описание:** Сейчас экспорт только через `SharePlus` (`SyncController.exportDatabase`). Добавить вариант сохранения в файловую систему: `FilePicker.platform.saveFile(...)` с записью байтов экспортированного файла в выбранное место (или `flutter_file_dialog` на Android). В `_SyncSection` — две кнопки: «Поделиться» и «Сохранить в файлы»; расширить инъекцию контроллера (новая функция `saveFile`).
- **Критерии приемки:** экспорт предлагает сохранение в файловую систему; сохранённый файл импортируется обратно.
- **Тесты:** unit-тест контроллера с инъекцией функции сохранения.

### Задача 13.15: Переключение темы уводит на главный экран
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** app
- **Ветка:** `task/13.15-theme-router`
- **Описание:** `main.dart` при каждом rebuild `ValueListenableBuilder<ThemeMode>` вызывает `AppRouter.create()` — создаётся новый `GoRouter`, который сбрасывает стек на `initialLocation` (`/home`). Вынести роутер в состояние `StatefulWidget`: создавать один раз, пересоздавать только при изменении `appRestartTick` (импорт БД).
- **Критерии приемки:** переключение темы в настройках не меняет текущий экран и стек навигации.
- **Тесты:** widget-тест — открыть `/settings`, сменить тему, убедиться, что остались на `/settings`.

### Задача 13.16: Звуковой сигнал окончания таймеров и выбор звука в настройках
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** app/settings + фичи/workout
- **Ветка:** `task/13.16-timer-sound`
- **Описание:** Добавить пакет `audioplayers` и bundled beep-ассет (`assets/sounds/`). Абстракция `SoundService` (инжектируемая, заглушка для тестов) играет сигнал при завершении таймера отдыха и разминки (13.12). В настройках — секция звука: переключатель вкл/выкл и выбор звукового файла с устройства (file_picker), хранение в `app_meta` через `SoundSettingsRepository` по образцу `ThemeSettingsRepository`.
- **Критерии приемки:** по завершении таймера играет сигнал (по умолчанию или выбранный файл); выбор сохраняется; выкл — тишина.
- **Тесты:** unit-тест с fake `SoundService` (вызов при окончании таймера), widget-тест секции звука в настройках.

### Задача 13.17: Логика таймера планки: счёт от нуля, цель в скобках
- **Статус:** [x] выполнена (2026-08-16)
- **Модуль:** фичи/workout domain + UI
- **Ветка:** `task/13.17-plank-timer`
- **Описание:** Переработать таймер удержания в `WorkoutController`: вместо обратного отсчёта `holdRemainingSeconds` — счёт фактического времени вверх от нуля (`holdElapsedSeconds`), заданное время удержания — как цель в скобках (например «0:24 (цель 30 с)»); запуск — только по кнопке «Начать» (13.8); при «Подход выполнен» с пустым полем переносить фактическое время (13.5); по достижении цели таймер останавливается и счётчик подсвечивается. Оформление — 13.4. При этом убрать автоматический `_startHoldTimerIfPlank` из `start`/`confirmSet`/`skipRest`/`nextExercise`/`_startRest`.
- **Критерии приемки:** таймер считает от нуля, цель показана в скобках; результат — фактическое время; авто-старта нет.
- **Тесты:** unit-тесты `WorkoutController` (не начинается до старта, считает вверх, остановка при достижении цели), widget-тесты экрана выполнения.

### Задача 13.18: Ограничение форматов миниатюр и анимаций
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** core/media + фичи/exercises
- **Ветка:** `task/13.18-media-formats`
- **Описание:** `Image` во Flutter декодирует jpg/jpeg/png/gif (в т.ч. анимированные)/bmp/webp (в т.ч. анимированные); остальные форматы (heic, tiff, svg, mp4 и т.п.) дают «битое» превью или ошибку. `_pickAnimation` использует `MediaFileType.any` (любой файл), `_pickThumbnail` — `MediaFileType.image` (широкий MIME-фильтр). Ограничить выбор: `FileType.custom` с `allowedExtensions: ['jpg','jpeg','png','webp','gif','bmp']` для миниатюры и анимации, валидация расширения в `MediaStore.importFromPicker` с понятной ошибкой (`MediaImportException`). Пересмотреть `MediaFileType` (использование `any` для анимации убрать).
- **Критерии приемки:** в пикере доступны только допустимые форматы; копирование файла другого формата отклоняется с сообщением.
- **Тесты:** `media_store_test` — валидация расширения; widget-тест формы — пикер вызывается с нужным фильтром.

**Зависимости этапа 13:** 13.17 — базовое переосмысление таймера планки, выполняется до 13.4/13.5/13.8; 13.12 — изменение схемы БД (v6) и экран разминки, выполняется до 13.16 (звук разминки); 13.15 — независимо от остального, можно раньше; остальные задачи независимы и выполняются по порядку.

---

# Этап 14. Публикация, лицензия, история упражнений, выполнение по сторонам, таймеры и навигация

Доработки по обратной связи (третья волна). Нумерация 14.1–14.10; порядок выполнения — по зависимостям (см. ниже).

### Задача 14.1: Публикация релизов по тегу + ссылка в README
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** CI + README
- **Ветка:** `task/14.1-release-workflow`
- **Описание:** Добавить `.github/workflows/release.yml`: триггер `push: tags: ['v*']`; шаги — checkout → flutter-action → `flutter pub get` → `flutter test` → `flutter build apk --release` → публикация GitHub Release с артефактом `app-release.apk` через `softprops/action-gh-release`. Релизный APK подписывается debug-ключом шаблона (подходит для sideload, не для Play). В корневой `README.md` — блок «Установка» с бейджем/ссылкой на последний релиз (`https://github.com/garryk3/fitnessappai/releases/latest/download/app-release.apk`); упомянуть в `fitnessappai/README.md`.
- **Критерии приемки:** пуш тега `v*` создаёт GitHub Release с APK; ссылка ведёт на последний билд.
- **Тесты:** CI, проверка вручную (релиз по тегу).

### Задача 14.2: Лицензия (некоммерческая)
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** репо
- **Ветка:** `task/14.2-license`
- **Описание:** Добавить в корень `LICENSE` — Creative Commons Attribution-NonCommercial-ShareAlike 4.0 (CC BY-NC-SA 4.0): только некоммерческое использование, с указанием автора, share-alike. Ссылка на лицензию в корневом `README.md` и `fitnessappai/README.md`.
- **Критерии приемки:** файл `LICENSE` в корне + упоминание в README.
- **Тесты:** не требуются.

### Задача 14.3: Версия в настройках + проверка обновления из GitHub
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/settings + core/update
- **Ветка:** `task/14.3-app-update`
- **Описание:** Добавить `package_info_plus` в явные зависимости и `url_launcher`. Секция «О приложении» в настройках: строка версии (`PackageInfo.versionName`) и кнопка «Проверить обновление». `UpdateService` (абстракция) → `GithubUpdateService` через `dart:io` `HttpClient` (`api.github.com/repos/garryk3/fitnessappai/releases/latest`, парсинг `tag_name`), заглушка для тестов. `UpdateCheckController` (signals, по образцу `SyncController`): статусы проверки, сравнение версий (semver), наличие обновления. При наличии более новой версии — диалог «Доступна версия X» с кнопкой «Обновить» → открытие URL релиза/APK в браузере (`url_launcher`), установку предлагает Android. Регистрация в `register_core_services.dart` и стаб в `test_services.dart`.
- **Критерии приемки:** в настройках видна версия; кнопка проверяет последний релиз GitHub, корректно обрабатывает «обновления нет / есть / нет сети».
- **Тесты:** unit `UpdateCheckController` (fake-клиент: новее/равно/ошибка сети), widget-тест секции «О приложении».

### Задача 14.4: История упражнения — из тренировки к описанию, история за 3 дня, последняя тренировка
- **Статус:** [x]
- **Модуль:** фичи/exercises + фичи/workout
- **Ветка:** `task/14.4-workout-exercise-history`
- **Описание:** `WorkoutRepository.lastResultsForExercise(exerciseId)` — результаты упражнения из последней сессии, где оно выполнялось. `StatsAggregator.exerciseHistoryLastDays(exerciseId, {days: 3})` — агрегация по дням (не более 3 последних дат выполнения). `ExerciseDetailScreen`: секция «История за последние 3 дня» (дата → метрика, формат как в прогрессии). `WorkoutRunScreen`: иконка/тап по имени упражнения → `context.push('/exercises/${exercise.id}')`; над формой ввода — карточка «Последняя тренировка» с результатами прошлой сессии (если есть). `WorkoutPrepareScreen`: карточки упражнений кликабельны → детали.
- **Критерии приемки:** с экрана выполнения и подготовки открывается описание упражнения; на деталях — история за последние 3 дня; на выполнении — результаты последней тренировки по текущему упражнению.
- **Тесты:** unit репозитория/агрегатора, widget-тесты run/prepare/detail.

### Задача 14.5: Чекбоксы «фиксированный вес» и «по сторонам» в форме упражнения
- **Статус:** [x]
- **Модуль:** фичи/exercises + БД (схема v7)
- **Ветка:** `task/14.5-exercise-flags`
- **Описание:** Схема v7: `exercises.fixedWeight` (bool, default false), `exercises.perSide` (bool, default false), `workout_set_results.side` (text nullable, 'left'/'right'); `appDatabaseSchemaVersion = 7`, миграция `from < 7`, регенерация `app_database.g.dart` (`build_runner`) и `drift_schemas/drift_schema_v7.json`. Модель `Exercise` + copyWith + `==`/`hashCode`. В `ExerciseFormScreen` два `CheckboxListTile` («Фиксированный вес», «По сторонам (левая/правая)») по образцу `_hideOptionalField`; сохранение в `_save`, загрузка в `_populate`. Новые l10n-ключи.
- **Критерии приемки:** в форме есть оба чекбокса; значения сохраняются/загружаются; миграция с v6 не ломает данные.
- **Тесты:** миграция 6→7 (обновить миграционные тесты), unit модели, widget-тест формы.

### Задача 14.6: Автозаполнение фиксированного веса при выполнении
- **Статус:** [x]
- **Модуль:** фичи/workout
- **Ветка:** `task/14.6-fixed-weight`
- **Описание:** В `_ExerciseInputForm` (`workout_run_screen.dart`) для силовых упражнений: если `exercise.exercise.fixedWeight` и `position.weightKg != null` — автозаполнение поля веса значением из параметров дня; поле остаётся редактируемым.
- **Критерии приемки:** вес подставлен при старте подхода, пользователь может изменить его.
- **Тесты:** widget-тест run-экрана (автозаполнение для strength с включённым флагом).

### Задача 14.7: Выполнение упражнения «по сторонам» (левая/правая) с дополнительным таймером
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/workout (контроллер + UI) + БД (v7, `side`)
- **Ветка:** `task/14.7-per-side`
- **Описание:** `WorkoutSetInput.side`/`WorkoutSetResult.side` (nullable, 'left'/'right') + copyWith/==. `WorkoutController`: сигналы `currentSide` (left/right) и `sideRest`; `confirmSet()` для perSide-упражнений записывает два результата на подход: левая → side-rest (тот же `restSeconds`, что и обычный отдых) → правая → обычный переход к следующему подходу/упражнению; `_startRest(restSeconds, {side})`; сброс `currentSide`/`sideRest` в `start`/`skipRest`/`nextExercise`/`cancelWorkout`. Run-экран: поле повторов с подписью стороны («Повторения — левая/правая»); на rest-экране бейдж «Отдых между сторонами». История/детали сессии: отображение стороны (Л/П).
- **Критерии приемки:** для упражнения «по сторонам» каждый подход даёт 2 результата (left/right) с отдыхом между ними; сохранённые результаты содержат сторону.
- **Тесты:** unit `WorkoutController` (флоу left/right, side-rest, звук), widget-тесты run-экрана, репозиторий (side в save/load).
- **Тесты (факт):** `WorkoutController`: 4 теста — левая → отдых между сторонами → правая (2 результата на подход), skipRest к правой, обычное упражнение без стороны, 2 подхода = 4 результата (left/right/left/right, setIndex [1,1,2,2]); репозиторий: save/load стороны; run-экран: подписи «Повторения — левая/правая», бейдж «Отдых между сторонами», результат с `side`; история: префикс `1л.`/`1п.`. Итого 601 тест зелёные.

### Задача 14.8: Таймер отдыха — пересчёт от времени старта (исправление сна)
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/workout
- **Ветка:** `task/14.8-rest-wallclock`
- **Описание:** Сейчас отдых — декремент счётчика в `Timer.periodic`; при уходе в сон и возврате отсчёт «замораживается». `_startRest`: хранить `_restEndsAt = _clock().add(Duration(seconds: restSeconds))`; каждый тик пересчитывать `remaining = _restEndsAt.difference(_clock()).inSeconds`; при `<= 0` — завершение отдыха (сброс `sideRest`, фаза → exercise, звук). Перевести rest-тесты на изменяемые `clock()` (синхронно с `async.elapse`).
- **Критерии приемки:** после длительного прерывания (сон) первый тик корректно завершает отдых, а не продолжает счёт с прежнего значения.
- **Тесты:** unit `WorkoutController` — отдых завершается по wall-clock (имитация «сна» через скачок времени).
- **Тесты (факт):** `WorkoutController`: wall-clock тест — rest завершается после скачка времени 5 мин; timerFactory тесты обновлены на mutable clock (`var now = startTime`). Всего 602 теста зелёные.

### Задача 14.9: Кнопка «Завершить и сохранить» в диалоге выхода из тренировки
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/workout
- **Ветка:** `task/14.9-finish-early`
- **Описание:** `WorkoutController.finishEarly()` — из фаз exercise/rest собрать сессию (startedAt → endedAt, разрешён 0 результатов) и перейти в `finished`; `WorkoutRunController.completeAndSave()` после этого работает штатно. Диалог `_handlePopRequest`: третья кнопка «Завершить и сохранить» (после сохранения — возврат с главного экрана); l10n-ключи.
- **Критерии приемки:** незавершённая тренировка сохраняется частично по кнопке в диалоге выхода.
- **Тесты:** unit `finishEarly` из exercise/rest, widget-тест диалога.
- **Тесты (факт):** `WorkoutController`: 3 теста — finishEarly из exercise (rest:0), finishEarly из rest, finishEarly без результатов (пустая сессия); widget: диалог «Завершить и сохранить» сохраняет и pop. Всего 606 тестов зелёные.

### Задача 14.10: Убрать «Программы» из навигации на узких экранах + кнопка на главной
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** app/responsive + фичи/home
- **Ветка:** `task/14.10-nav-programs`
- **Описание:** `AdaptiveNavigation`: на узких экранах (порог `hidesBarLabels`, ширина < 400dp) исключить вкладку «Программы» из `NavigationBar` (на `NavigationRail` и широких экранах остаётся); маппинг индекс-вкладка → branch для `goBranch`. `HomeScreen`: рядом с карточкой активной программы — кнопка «Программы» → `context.push('/programs')`.
- **Критерии приемки:** на экранах < 400dp в нижней навигации нет вкладки «Программы», остальные вкладки работают; на главной у активной программы есть переход к программам.
- **Тесты:** `adaptive_navigation_test` (узкий — нет «Программ», широкий — есть; маппинг индексов), widget-тест home (кнопка).
- **Тесты (факт):** `adaptive_navigation_test`: обновлён тест <400dp (Programs не найден в NavigationBar) + новый тест 400–600dp (Programs видна); `home_screen_test`: новый тест «К программам» рядом с активной программой → /programs. Всего 608 тестов зелёные.

**Зависимости этапа 14:** 14.1 → 14.3 (проверка обновления опирается на релизы); 14.5 (схема v7) → 14.6 и 14.7; остальные задачи (14.2, 14.4, 14.8, 14.9, 14.10) независимы. Порядок: 14.1 → 14.2 → 14.3; 14.5 → 14.6 → 14.7; параллельно 14.4, 14.8, 14.9, 14.10.

---

# Этап 15. Баги и доработки

Исправления ошибок и UX-доработки по обратной связи. Нумерация 15.1–15.11.

### Задача 15.1: Перенос поля разминки из модалки дня в экран программы
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/programs
- **Ветка:** `task/15-bugfixes`
- **Описание:** Поле ввода `warmupMinutes` в `_DaySettingsDialog` (модалка привязки дня) — разминка является параметром программы, а не дня недели. Вынести поле из `_DaySettingsDialog` в основной экран редактирования программы (ниже описания). Значение хранится в `_ProgramBuilderScreenState` и применяется ко всем дням при `_persist`. Убрать `warmupMinutes` из `DaySettings` и `_DaySettingsDialog`.
- **Тесты:** widget-тест — поле разминки видно на экране редактирования, не в модалке.

### Задача 15.2: Ограничение формата звука при выборе файла
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** app/sound
- **Ветка:** `task/15-bugfixes`
- **Описание:** `_defaultPick()` в `SoundSettingsController` использует `FileType.audio` без белого списка расширений. Заменить на `FileType.custom` + `allowedExtensions: ['wav', 'mp3', 'ogg', 'm4a', 'aac', 'flac']`.
- **Тесты:** unit-тест — picker вызывается с `FileType.custom` и правильными расширениями.

### Задача 15.3: Остановка звука через 5 сек и при старте следующего упражнения
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** app/sound + фичи/workout
- **Ветка:** `task/15-bugfixes`
- **Описание:** Звук не останавливается после воспроизведения — `AudioPlayer` продолжает играть. Добавить метод `stop()` в `SoundService`; в `playCompletion()` после `play()` запустить `Timer(const Duration(seconds: 5), stop)`. В `WorkoutController.confirmSet()` и `WorkoutWarmupScreen` вызывать `_soundService?.stop()` при старте нового подхода/экрана.
- **Тесты:** unit-тест `SoundService` — `stop()` вызывается через 5 сек; widget-тест — при новом подходе звук останавливается.

### Задача 15.4: Исправление ошибки INTERNET permission (socket exception)
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** android
- **Ветка:** `task/15-bugfixes`
- **Описание:** `INTERNET` permission есть только в `debug/` и `profile/` манифестах, отсутствует в `main/` → в release-сборке DNS lookup блокируется → `SocketException: Failed host lookup`. Добавить `<uses-permission android:name="android.permission.INTERNET"/>` в `android/app/src/main/AndroidManifest.xml`.
- **Тесты:** проверка вручную (release-сборка → кнопка «Проверить обновление»).

### Задача 15.5: Исправление экспорта БД в файлы (invalid arguments)
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/settings
- **Ветка:** `task/15-bugfixes`
- **Описание:** `FilePicker.platform.saveFile()` на Android возвращает `content://` URI, а `File(filePath).copy(destination)` не работает с content URI → `Invalid argument`. Заменить `_defaultSave` на вызов `SharePlus` с `XFile` (share уже работает в `exportDatabase`) или использовать `flutter_file_dialog`.
- **Тесты:** widget-тест — «Сохранить в файлы» не вызывает ошибку.

### Задача 15.6: Запуск непривязанных дней тренировки (dayOfWeek == null)
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/workout + фичи/home
- **Ветка:** `task/15-bugfixes`
- **Описание:** `WeekPlanController._load()` пропускает дни с `dayOfWeek == null` (line 126-129). `HomeController._findUpcomingDay()` тоже фильтрует по `dayOfWeek != null`. Результат: непривязанные дни недоступны для быстрого старта. Решение: для дней с `dayOfWeek == null` создавать `WeekPlanItem` с `scheduledDate = today`, статус `pending`. Приоритет: сначала linked по расписанию, потом unlinked.
- **Тесты:** unit-тест `WeekPlanController` — unlinked day появляется в `nextPending`; widget-тест home — FAB виден с unlinked днём.

### Задача 15.7: Чекбокс «Только свои упражнения» на экране упражнений
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/exercises
- **Ветка:** `task/15-bugfixes`
- **Описание:** Добавить `SwitchListTile` «Только свои упражнения» в `ExercisesScreen`. В `ExerciseListController` — сигнал `onlyCustom` (default false), фильтр в `_load()`: `.where((e) => e.exercise.isCustom)`.
- **Тесты:** widget-тест — переключение фильтра показывает только пользовательские упражнения.

### Задача 15.8: Кнопка «Следующий день» вместо «Сохранить» при незаполненных днях
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/programs
- **Ветка:** `task/15-bugfixes`
- **Описание:** В конструкторе программ при незаполненных днях вместо кнопки «Сохранить» показывать «Заполнить день N». Кнопка «Сохранить» — только после заполнения всех дней. Проверить/исправить логику `_nextDay` в `_buildBottomBar`.
- **Тесты:** widget-тест — при 1 заполненном дне из 3 кнопка «Заполнить день 2»; после всех дней — «Сохранить».

### Задача 15.9: Стилизация графиков — сетка и границы
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/progress + фичи/profile
- **Ветка:** `task/15-bugfixes`
- **Описание:** Все графики имеют `FlBorderData(show: false)` и `FlGridData(show: false)`. Включить `FlGridData(show: true)` с горизонтальными линиями, `FlBorderData(show: true)` с минимальными границами. Применить ко всем LineChart и BarChart (progress_screen, exercise_progression_screen, profile_screen).
- **Тесты:** widget-тест — наличие `FlGridData` с `show: true`.

### Задача 15.10: Профиль — динамика: интервалы и overflow
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/profile
- **Ветка:** `task/15-bugfixes`
- **Описание:** При нескольких замерах за день — повторяющиеся интервалы на оси X. При 1 дне данных — показывать «Сегодня». Overflow из-за большого количества точек. Решение: дедупликация по дате, ограничение точек (макс. 30), `maxLines: 1` на labels.
- **Тесты:** widget-тест — при 1 дне данных label «Сегодня»; при нескольких замерах за день — дедупликация.

### Задача 15.11: Проверка ссылки на скачивание APK в README
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** репо
- **Ветка:** `task/15-bugfixes`
- **Описание:** Ссылка `https://github.com/garryk3/fitnessappai/releases/latest/download/app-release.apk` → 404. Проверить формат ссылки (workflow публикует `app-release.apk`); убедиться что первый релиз создан; при необходимости создать тег `v1.0.0`.
- **Тесты:** проверка вручную (переход по ссылке).

### Задача 15.12: Профиль — график: фиксированный интервал и линия при одном замере
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/profile
- **Ветка:** `task/15.12-profile-chart-fix`
- **Описание:** `LineChart` в `profile_screen.dart`: `bottomTitles` — `interval: 1` (фиксированный шаг 1 день вместо автоматического); `_refreshChart()` в `profile_controller.dart`: если `points.isEmpty` (было `points.length < 2`), добавить синтетическую точку «сегодня» с текущим значением, чтобы при одном замере отображалась горизонтальная линия.
- **Критерии приемки:** при одном замере видна горизонтальная линия на «Сегодня»; при нескольких замерах — правильные интервалы на оси X.
- **Тесты:** widget-тест — при 1 точке проверяется наличие «Сегодня»; interval=1.

### Задача 15.13: Главный экран — кнопка «Перейти к программам» в State B
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/home
- **Ветка:** `task/15.13-home-programs-btn`
- **Описание:** `HomeScreen` State B (есть программы, нет активной) — добавить кнопку `homeGoToPrograms` → `context.push('/programs')`, обновить l10n-ключ.
- **Критерии接受ки:** в State B видна кнопка, она ведёт на `/programs`.
- **Тесты:** widget-тест — кнопка в State B ведёт на `/programs`.

### Задача 15.14: Разминка — wakelock + центрирование текста завершения
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/workout
- **Ветка:** `task/15.14-warmup-wakelock`
- **Описание:** `WorkoutWarmupScreen`: `WakelockPlus.enable()` в `initState`, `WakelockPlus.disable()` в `dispose`; `SingleChildScrollView` + `TextAlign.center` для текста «Разминка завершена» (fix overflow на маленьких экранах).
- **Критерии:** экран не гаснет во время разминки; текст «Разминка завершена» по центру.
- **Тесты:** widget-тест — `WakelockPlus.enable()` вызывается; `TextAlign.center`.

### Задача 15.15: Прогресс — дедупликация perSide результатов в статистике
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/progress
- **Ветка:** `task/15.15-per-side-dedup`
- **Описание:** `StatsAggregator._dedupPerSide()` — для упражнений «по сторонам» (side != null) группировать по `(exerciseId, setIndex)`, оставлять лучший результат; `side == null` — без изменений. Исправляет проблему дублирования в статистике (каждый подход давал 2 записи: left + right).
- **Критерии:** в статистике perSide-упражнений каждый подход считается один раз.
- **Тесты:** unit-тест `_dedupPerSide()` — 2 записи (left+right) → 1.

### Задача 15.16: Настройки — сохранение БД через SAF на Android (MethodChannel)
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/settings + android
- **Ветка:** `task/15.16-saf-save`
- **Описание:** `MainActivity.kt`: MethodChannel `com.example.fitnessappai/file_saver` с `ACTION_CREATE_DOCUMENT` + `contentResolver.openOutputStream()`. `SyncController._defaultSave()`: на Android вызывает MethodChannel вместо `FilePicker.platform.saveFile()`. Исправлена ложная ошибка (возврат `false` при отмене).
- **Критерии:** на Android «Сохранить в файлы» работает через SAF, открывает пикер, файл записывается.
- **Тесты:** widget-тест — `_defaultSave` не вызывает исключение на Android.

### Задача 15.17: Попап выхода — отступы между кнопками
- **Статус:** [x] выполнена (2026-08-17)
- **Модуль:** фичи/workout
- **Ветка:** `task/15.17-exit-popup-spacing`
- **Описание:** `workout_run_screen.dart`: `SizedBox(width: 8)` между кнопками в диалоге `_handlePopRequest` (fix визуального слияния кнопок).
- **Критерии:** кнопки в попапе разделены отступом 8px.
- **Тесты:** widget-тест — наличие `SizedBox(width: 8)` между кнопками.

### Задача 15.18: Блокировать сон при работе таймера
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** фичи/workout
- **Ветка:** `task/15.18-wakelock-timer`
- **Описание:** Убедиться, что при активном таймере (отдых/удержание) экран тренировки не гаснет. Wakelock (`WakelockService`) уже активен на всём экране `workout_run_screen` (вкл. в `initState`, выкл. в `dispose`) и в `workout_warmup_screen`. Зафиксировать тестом, что `enable()` вызывается при старте тренировки, а `disable()` — при dispose.
- **Критерии:** при активной тренировке сон заблокирован; покрыто widget-тестом.
- **Тесты:** widget-тесты `workout_run_screen_test.dart` и `workout_warmup_screen_test.dart` — `_FakeWakelock.enable()` на старте, `disable()` при dispose (для run-экрана тест уже был, для разминки добавлен).

### Задача 15.19: Настройки — ошибка импорта БД (invalid icon is_stat_launcher)
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** настройки / notifications
- **Ветка:** `task/15.19-import-icon-fix`
- **Описание:** При импорте БД (`_rebuildAfterImport` → `reminders.initialize()`) `AndroidInitializationSettings('ic_stat_launcher')` вызывает Platform exception «invalid icon, is_stat_launcher not be found». Причина — векторный drawable `ic_stat_launcher.xml` ненадёжен как small icon уведомлений. Исправлено: вектор заменён на bitmap `ic_stat_launcher.png` (белый силуэт с прозрачным фоном, 96×96, RGBA) в `res/drawable/` — `getIdentifier(context, name, "drawable", ...)` и `setSmallIcon` работают без исключения. Код Dart не менялся.
- **Критерии:** импорт БД на Android не роняет приложение; уведомления планируются.
- **Тесты:** Android-сборка в CI (`flutter build apk --debug`) зелёная; остальные проверки без изменений.

### Задача 15.20: Перенести иконку настроек на главный экран
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** app/home + profile
- **Ветка:** `task/15.20-settings-icon-home`
- **Описание:** Иконка настроек (`Icons.settings_outlined` → `/settings`) перенесена с AppBar профиля (`profile_screen.dart`) на главный экран (`home_screen.dart`). С профиля иконка убрана.
- **Критерии:** настройки доступны с главного экрана; с профиля иконка убрана.
- **Тесты:** widget-тест навигации главного (новая иконка → настройки), профиль без иконки; e2e-сценарий настроек обновлён (навигация с главного), 14/14 зелёных.

### Задача 15.21: Конфигурация мышц — группировка рук/спины/ног и подгруппы груди
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** фичи/exercises
- **Ветка:** `task/15.21-muscle-groups`
- **Описание:** В `reference_seeder.dart` добавлены родительские группы «Руки» (бицепс/трицепс/предплечья), «Спина» (трапеции/широчайшие/поясница), «Ноги» (квадрицепсы/бицепс бедра/икры/ягодицы) — кликабельные родители (как «Плечи», по решению пользователя). Грудь разделена на подгруппы «Верх/Низ/Центр груди» (`parentKey: chest`). `seed()` теперь обновляет `parentKey` существующих групп (не только вставляет новые), чтобы подгруппы корректно обновились в существующих БД.
- **Критерии:** в форме выбора мышц группы сгруппированы с отступами, как «Плечи».
- **Тесты:** обновлены `reference_seeder_test.dart` (24 группы, проверка parentKey) и `exercise_form_screen_test.dart` (подгруппы груди/рук/спины/ног с отступами).

### Задача 15.22: Прогресс — пустое состояние по центру по вертикали
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** фичи/progress
- **Ветка:** `task/15.22-progress-empty-center`
- **Описание:** Пустое состояние «Нет тренировок за период» в `progress_screen.dart` переведено с `ListView` на `LayoutBuilder` + `Column` с `Expanded`/`Center` — текст центрируется по вертикали в оставшейся области под segmented-контролом (и по горизонтали).
- **Критерии:** текст «Нет тренировок за период» по центру экрана.
- **Тесты:** widget-тест — текст в нижней половине экрана (вертикальное центрирование под segmented-контролом); e2e 14/14 зелёных.

### Задача 15.23: Звуки — timer.mp3 для таймера, notification.mp3 для напоминаний
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** app/sound + notifications
- **Ветка:** `task/15.23-sounds`
- **Описание:** Добавлены звуковые файлы: `assets/sounds/timer.mp3` (звук окончания таймера — отдых/разминка) заменяет `defaultAssetPath` в `SoundService.playCompletion()`; `android/app/src/main/res/raw/notification.mp3` (звук напоминания о тренировке) подключён через `RawResourceAndroidNotificationSound('notification')` в `AndroidNotificationDetails` (`ReminderService`).
- **Критерии:** окончание таймера играет timer.mp3; Android-уведомления напоминаний играют notification.mp3.
- **Тесты:** unit/widget/e2e 14/14 зелёных без регрессий; Android-сборка в CI зелёная.

### Задача 15.31: Wake lock — проброс ошибок и warning в UI
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** workout/wakelock
- **Ветка:** `task/15.31-wakelock-fix`
- **Описание:** `WakelockPlusService.enable()` молча глотает ошибки — если `WakelockPlus.enable()` падает на устройстве, пользователь не знает. Проброс ошибки наверх, показ warning в UI тренировки. Проверить корректность работы на Android 12+.
- **Критерии:** при ошибке wakelock показывается предупреждение; логируются все ошибки.
- **Тесты:** unit/widget без регрессий; e2e 14/14.
- **PR:** #116

### Задача 15.32: Звук таймера — исправить AssetSource путь
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** app/sound
- **Ветка:** `task/15.32-sound-asset-fix`
- **Описание:** `defaultAssetPath = 'assets/sounds/timer.mp3'`, но `AssetSource` в `audioplayers` ожидает путь без префикса `assets/`. Плагин ищет `assets/assets/sounds/timer.mp3` — файла нет, воспроизведение молча проваливается. Заменить на `'sounds/timer.mp3'`, добавить try-catch.
- **Критерии:** при отсутствии пользовательского звука играет timer.mp3 по умолчанию.
- **Тесты:** unit/widget/e2e без регрессий; 619/619 unit тестов.

### Задача 15.33: Звук в фоне — AudioContext и foreground service
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** app/sound + android
- **Ветка:** `task/15.33-sound-background`
- **Описание:** `AudioPlayer` без `AudioContext` для фонового воспроизведения. В `AndroidManifest.xml` нет `WAKE_LOCK` permission. На Android 12+ ОС убивает процесс при блокировке. Добавить `WAKE_LOCK`, настроить `AudioContext` для notification usage, foreground service для таймера.
- **Критерии:** звук таймера играет при заблокированном экране и в фоне.
- **Тесты:** Android-сборка в CI; ручное тестирование на устройстве.
- **PR:** #117

### Задача 15.34: Импорт БД — монохромная иконка уведомлений
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** notifications + android
- **Ветка:** `task/15.34-import-icon-fix`
- **Описание:** PNG `ic_stat_launcher.png` — полнокветровая RGBA, а не монохромная. На Android 12+ small icon **должен** быть alpha-only. Также нет `manifestPlaceholders` в `build.gradle.kts`. При импорте БД создаётся новый `FlutterLocalNotificationsPlugin` → иконка не резолвится. Заменить на vector drawable, добавить `manifestPlaceholders["default_notification_icon"]`.
- **Критерии:** импорт БД не вызывает ошибку иконки; уведомления отображаются корректно.
- **Тесты:** Android-сборка в CI; e2e 14/14.

### Задача 15.35: Главный экран — обновление после создания программы
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** home
- **Ветка:** `task/15.35-home-refresh`
- **Описание:** После создания программы главный экран показывает «Нет программ» (homeNoProgramsTitle). Новая программа создаётся с `isActive: false`, `activeProgram == null`, показывается пустой экран. Изменить текст на «Выберите активную программу»; убедиться, что `hasPrograms` обновляется после `pop()` из конструктора.
- **Критерии:** после создания программы главный экран показывает «Выберите активную программу» вместо «Нет программ».
- **Тесты:** widget-тест home_screen; e2e 14/14.
- **PR:** #114

### Задача 15.36: Версия тега в APK через GitHub Actions
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** ci/release
- **Ветка:** `task/15.36-release-version`
- **Описание:** `release.yml` запускает `flutter build apk --release` без `--build-name`/`--build-number`. Версия всегда `1.0.0+1`. Тег `${{ github.ref_name }}` доступен, но не передаётся в сборку. Извлечь версию из тега и передать в `flutter build apk`.
- **Критерии:** APK содержит версию из тега; настройки показывают правильную версию.
- **Тесты:** проверка release workflow (CI).
- **PR:** #118

### Задача 15.37: Напоминания — явный канал и проверка permission
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** notifications
- **Ветка:** `task/15.37-reminder-fix`
- **Описание:** 3 дефекта: (1) Android notification channel не создаётся явно — на Xiaomi/Samsung может быть无声; (2) `bootstrap.dart` молча глотает ошибки инициализации; (3) `POST_NOTIFICATIONS` permission может быть отозван — повторные `zonedSchedule` молча падают. Явно создавать channel с `Importance.high` + звуком; проверять `areNotificationsEnabled()` перед `schedule()`; логировать ошибки.
- **Критерии:** напоминания приходят с звуком при заблокированном экране; channel создан явно.
- **Тесты:** unit-test ReminderService; Android-сборка в CI.
- **PR:** #113

### Задача 15.38: Подсветка мышц — parent/child маппинги
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** exercises/muscle-map
- **Ветка:** `task/15.38-muscle-highlight`
- **Описание:** `muscle_map.dart:71-79` функция `_isActive()` знает только `shoulders` → children. Новые группы (`arms`, `back`, `legs`, `chest_upper/lower/center`) не обрабатываются. Расширить `_isActive()` на все parent→child и child→parent маппинги.
- **Критерии:** подсвечиваются руки/спина/ноги (родительские группы) и подгруппы груди.
- **Тесты:** widget-тест muscle_diagram; e2e 14/14.
- **PR:** #115

### Задача 15.30: Видео-заставка на экране загрузки
- **Статус:** [x] выполнена (2026-08-19)
- **Модуль:** app/splash
- **Ветка:** `task/15.30-video-splash`
- **Описание:** Заменить анимированный логотип (иконка + «Личный тренер») на полноэкранную видео-заставку. Добавлен пакет `video_player` и ассет `assets/videos/splash.mp4` (h264, 416×928, ~4 с). Новый виджет `SplashVideo` — зацикленное видео без звука, `BoxFit.cover`, с заглушкой при ошибке инициализации (на платформах без реализации видео, например Linux, показывается пустой фон без краха). `SplashGate` принимает инъектируемый `splashBody` для тестов.
- **Критерии:** при старте воспроизводится видео; после инициализации — плавный переход в приложение; на Linux без видео-реализации нет краха.
- **Тесты:** widget-тесты `splash_gate_test.dart` (заставка во время инициализации и после её завершения).

**Зависимости этапа 15:** 15.2 → 15.3 (звук); 15.4, 15.5 независимы; остальные независимы.

---

# Этап 16. Доработки UI и UX

### Задача 16.1: Конструктор программы — список мышц с % и незадействованные
- **Статус:** [x] выполнена (2026-08-21)
- **Модуль:** фичи/programs
- **Ветка:** `task/16.1-muscle-panel`
- **Описание:** `_MusclePanel` в `ProgramDayBuilderScreen` показывает только схему мышц. Добавить текстовый список под схемой: каждая группа с `%` нагрузки (жирным для родительских, с отступом для подгрупп). Также показать список незадействованных мышц (все группы минус задействованные), чтобы тренер видел, какие группы пропущены.
- **Критерии:** список групп с % виден; незадействованные мышцы показаны серым цветом.
- **Тесты:** widget-тест — наличие списков задействованных и незадействованных мышц.

### Задача 16.2: Настройки — кнопка «Прослушать» для выбранного звука
- **Статус:** [x] выполнена (2026-08-21)
- **Модуль:** фичи/settings + app/sound
- **Ветка:** `task/16.2-sound-listen`
- **Описание:** В секции звука `_SoundSection` добавить кнопку «Прослушать» (`Icons.play_arrow`), которая воспроизводит текущий выбранный звук (пользовательский файл или встроенный `timer.mp3`). Кнопка активна только когда `enabled == true`. Использовать `SoundService.playCompletion()` (или новый метод preview). Кнопка рядом с кнопкой сброса.
- **Критерии:** тап «Прослушать» играет текущий звук; без звука — встроенный по умолчанию; при выключенном звуке — кнопка неактивна.
- **Тесты:** widget-тест — кнопка видна, вызывает SoundService.

### Задача 16.3: Прогресс — подписи оси Y графика планки: единицы и наложение
- **Статус:** [x] выполнена (2026-08-21)
- **Модуль:** фичи/progress
- **Ветка:** `task/16.3-plank-yaxis`
- **Описание:** `LineChart` в `_MetricChart` (`progress_screen.dart`) для планки показывает значения в минутах (÷60), но левая ось Y не имеет подписей единиц. Добавить форматирование: для планки — «Xм» (минуты), для дистанции — «Xкм». Также `reservedSize: 32` мал — увеличить до 48 при планке/дистанции, чтобы длинные подписи не налезали на график.
- **Критерии:** подписи Y содержат единицы (мин/км/кг/повт); нет наложения на график.
- **Тесты:** widget-тест — наличие единиц в подписях оси Y.

### Задача 16.4: Прогресс — карточка «Тренировок» кликабельна → история
- **Статус:** [x] выполнена (2026-08-21)
- **Модуль:** фичи/progress
- **Ветка:** `task/16.4-workout-count-tap`
- **Описание:** `_StatCard` в `_StatCards` (`progress_screen.dart`) — добавить необязательный `onTap` callback. Карточка «Тренировок» получает `onTap: () => context.push('/history')`. Визуально: `InkWell` поверх `Card` или `GestureDetector`.
- **Критерии:** тап по карточке «Тренировок» открывает экран истории; остальные карточки без изменений.
- **Тесты:** widget-тест — тап по карточке ведёт на `/history`.

### Задача 16.5: Экран завершения тренировки — упражнения и мышцы
- **Статус:** [x] выполнена (2026-08-21)
- **Модуль:** фичи/workout
- **Ветка:** `task/16.5-finished-exercises`
- **Описание:** `_FinishedView` (`workout_run_screen.dart`) показывает только число подходов и время. Добавить список выполненных упражнений (название + тип) и список задействованных мышц. Данные доступны: `controller.workout.exercises` (список `WorkoutExercise`). Для мышц — загрузить `exerciseRepository.getMuscles(id)` для каждого упражнения и агрегировать уникальные группы. Показать списки в расширяющемся блоке между статистикой и кнопками.
- **Критерии:** на экране завершения видны упражнения и задействованные мышцы.
- **Тесты:** widget-тест — наличие списка упражнений и мышц на finished-экране.

### Задача 16.6: Wakelock warning — OK закрывает баннер на сессию
- **Статус:** [x] выполнена (2026-08-21)
- **Модуль:** фичи/workout
- **Ветка:** `task/16.6-wakelock-dismiss`
- **Описание:** `MaterialBanner` в `WorkoutRunScreen` показывается когда `_wakelock.isEnabled == false`. Кнопка `OK` ничего не делает — баннер остаётся. Добавить `ValueNotifier<bool> _wakelockDismissed` и скрывать баннер при тапе OK (для текущей сессии, без персиста).
- **Критерии:** тап OK скрывает баннер; при выходе и возврате баннер может появиться снова.
- **Тесты:** widget-тест — OK скрывает баннер.

### Задача 16.7: Заставка — 2-секундная задержка перед стартом видео
- **Статус:** [x] выполнена (2026-08-21)
- **Модуль:** app/splash
- **Ветка:** `task/16.7-splash-delay`
- **Описание:** `SplashVideo` сразу начинает воспроизведение видео. По требованию — добавить 2-секундную задержку перед стартом видео (чтобы пользователь увидел статичный кадр или логотип до начала анимации). Реализовать через `Future.delayed(Duration(seconds: 2))` в `initState` перед `_controller.play()`.
- **Критерии:** видео начинает воспроизведение через 2 секунды после появления заставки.
- **Тесты:** widget-тест — видео не стартует сразу.

**Зависимости этапа 16:** все задачи независимы, выполняются по порядку.

---

# Этап 17. Доработки изображений, UX и визуальные фиксы

## 17.1 Панель мышц в конструкторе программ

- **Описание:** В конструкторе программ (ProgramBuilderScreen) вывести агрегированную панель мышц с детьми (MuscleLoad → MuscleGroupLoad → children), аналогично тренировочному дню. Извлечь `MusclePanel` в отдельный виджет для переиспользования.
- **Статус:** [x]
- **Тесты:** 10 widget-тестов (program_builder_screen_test.dart), lazy-load muscle data.

## 17.2 Автозаполнение разминки из первого дня

- **Описание:** При загрузке конструктора программы (`_load()`), если `warmupMinutes` равен 0, взять значение разминки из первого дня программы для автоматического заполнения.
- **Статус:** [x]

## 17.3 Play/Stop toggle для звука

- **Описание:** Добавить `SoundService.isPlaying` / `isPlayingStream`, `SoundSettingsController.togglePreview()`, UI toggle с play/stop иконками в настройках звука.
- **Статус:** [x]

## 17.4 Кнопка «Поделиться» — OutlinedButton

- **Описание:** В экране завершённой тренировки заменить `FilledButton.icon` на `OutlinedButton.icon` для кнопки «Поделиться».
- **Статус:** [x]

## 17.5 Изображения — хранение blob в БД (схема v8)

- **Описание:** Добавить колонки `thumbnailBlob` и `animationBlob` (nullable BLOB) в таблицу `Exercises`. Модель `Exercise` получает поля `Uint8List? thumbnailBlob / animationBlob`. `MediaStore.importFromPicker` возвращает `({String path, Uint8List bytes})?`. `ExerciseFormScreen` сохраняет blob при выборе. Миграция v7→v8. Дамп `drift_schema_v8.json`.
- **Статус:** [x]
- **Ветка/PR:** task/17.5-image-blob / PR #123
- **Дата:** 2026-08-23
- **Тесты:** 624 unit tests pass, format/analyze clean.

## 17.6 Валидация формата и размера изображений (5 МБ)

- **Описание:** Добавить ограничение максимального размера загружаемого файла — 5 МБ. Показать info-текст на форме упражнения с указанием допустимых форматов и лимита. При превышении — `SnackBar` с ошибкой.
- **Статус:** [x]
- **Зависимости:** 17.5.

## 17.7 Переименовать «анимация» → «изображение»

- **Описание:** Заменить все UI-строки и l10n-ключи, содержащие «анимация/анимацию/анимации» на «изображение/изображение/изображения» для единообразия (blob хранит статичные изображения, не только gif).
- **Статус:** [x]

## 17.8 Убрать надпись «Проверка обновлений через GitHub»

- **Описание:** В настройках (`_AboutSection` в `settings_screen.dart`) убрать подсказку «Проверка обновлений через GitHub» — она создаёт впечатление, что пользователю нужно что-то делать вручную.
- **Статус:** [x]

## 17.9 Заставка 2с → 3с

- **Описание:** Увеличить задержку заставки с 2 до 3 секунд (`Duration(seconds: 2)` → `3` в `splash_video.dart`).
- **Статус:** [x]

## 17.10 Прогресс: ось Y bodyweight → «повт» вместо «кг»

- **Описание:** В `progress_screen.dart` для упражнений типа bodyweight ось Y должна показывать «повт» вместо «кг» (`_unitSuffix` switch).
- **Статус:** [x]

**Зависимости этапа 17:** 17.5 → 17.6; 17.7 независима; 17.8–17.10 независимы. Порядок: 17.1–17.5 (выполнены), затем 17.6–17.10 по порядку.

---

# Этап 18. Исправления и доработки (вторая волна)

## 18.1 Понятный текст ошибки проверки версии

- **Описание:** В `UpdateCheckController` при ошибке сети (`SocketException`/`HttpException`) показывать дружелюбный текст вместо сырого `Exception`. Добавить два ключа l10n: `updateCheckErrorNetwork` и `updateCheckError`.
- **Статус:** [x]

## 18.2 Убрать панель мышц из конструктора дня

- **Описание:** В `ProgramDayBuilderScreen` удалить `MusclePanel` (виджет схемы мышц дублирует панель в конструкторе программы). Расширить список упражнений на освободившееся место. Тесты: убрать ожидания `MuscleDiagram` и удалить тест «схема мышц обновляется».
- **Статус:** [x]

## 18.3 Не задействованные мышцы — только группы

- **Описание:** В `MusclePanel` в списке «Не задействованы» фильтровать подгруппы (у которых `parentKey != null`), показывая только группы верхнего уровня.
- **Статус:** [x]

## 18.4 Реордер дней — содержимое следует за днём

- **Описание:** В `ProgramBuilderScreen._onReorder` после перемещения дня вызывать `repository.reorderDays()` для обновления `dayIndex` в БД. Автосортировка по `dayOfWeek` при наличии привязки у обоих дней.
- **Статус:** [x]

## 18.5 Конфликт обновления программы

- **Описание:** `_replaceDays` в `ProgramRepository` сопоставляла дни по `dayIndex` (позиции), а не по `id`. При реордере/удалении упражнения оставались с привязкой к старому дню. Исправление: приоритет по `day.id`, fallback на `dayIndex`. `_persist` теперь пробрасывает реальные `id` дней.
- **Статус:** [x]

## 18.6 Импорт БД — совместимость схемы

- **Описание:** `LocalFileSyncService._validate` отклонял файлы с версией схемы `< appDatabaseSchemaVersion`. Drift мигрирует при открытии, поэтому старые версии допустимы. Блокируем только `version > appDatabaseSchemaVersion` (БД новее приложения).
- **Статус:** [x]

**Зависимости этапа 18:** Задачи независимы. 18.4 и 18.5 связаны (обе dotyczą `ProgramBuilderScreen` и `ProgramRepository`).

---

## Таблица прогресса

| Задача | Название | Статус | Ветка / PR | Дата завершения |
|---|---|---|---|---|
| 0.1 | Настройка проекта и CI | [x] | task/0.1-setup / PR #1 | 2026-08-07 |
| 0.2 | Зависимости, структура, DI | [x] | task/0.2-structure-di / PR #2 | 2026-08-07 |
| 0.3 | Темизация M3 dark | [x] | task/0.3-theme / PR #3 | 2026-08-08 |
| 0.4 | Локализация ru | [x] | task/0.4-l10n / PR #4 | 2026-08-08 |
| 0.5 | Роутинг и каркас | [x] | task/0.5-routing / PR #5 | 2026-08-09 |
| 0.6 | Адаптивная навигация | [x] | task/0.6-responsive / PR #6 | 2026-08-09 |
| 1.1 | Domain-модели и валидаторы | [x] | task/1.1-models / PR #7 | 2026-08-09 |
| 1.2 | Drift-инфраструктура | [x] | task/1.2-drift-core / PR #8 | 2026-08-09 |
| 1.3 | Таблицы Drift и конвертеры | [x] | task/1.3-drift-tables | 2026-08-10 |
| 1.4 | MediaStore | [x] | task/1.4-media-store | 2026-08-10 |
| 2.1 | Репозиторий упражнений | [x] | task/2.1-exercise-repository | 2026-08-10 |
| 2.2 | Seed-данные (wger) | [x] | task/2.2-seed | 2026-08-10 |
| 2.3 | Список упражнений (UI) | [x] | task/2.3-exercise-list | 2026-08-10 |
| 2.4 | Детали упражнения (UI) | [x] | task/2.4-exercise-detail | 2026-08-10 |
| 2.5 | Форма упражнения (UI) | [x] | task/2.5-exercise-form | 2026-08-10 |
| 2.6 | Схема мускулатуры | [x] | task/2.6-muscle-diagram | 2026-08-10 |
| 3.1 | Репозиторий программ | [x] | task/3.1-program-repository | 2026-08-10 |
| 3.2 | Список программ (UI) | [x] | task/3.2-program-list | 2026-08-10 |
| 3.3 | Конструктор: параметры и дни | [x] | task/3.3-program-builder-days | 2026-08-10 |
| 3.4 | Конструктор: наполнение дней | [x] | task/3.4-program-builder-exercises / PR #17 | 2026-08-10 |
| 3.5 | Параметры упражнения в дне | [x] | task/3.5-day-exercise-params / PR #18 | 2026-08-10 |
| 4.1 | Контроллер тренировки | [x] | task/4.1-workout-controller / PR #19 | 2026-08-10 |
| 4.2 | Репозиторий тренировки | [x] | task/4.2-workout-repository / PR #20 | 2026-08-10 |
| 4.3 | План недели (UI) | [x] | task/4.3-week-plan / PR #21 | 2026-08-10 |
| 4.4 | Подготовка к тренировке (UI) | [x] | task/4.4-workout-prepare / PR #22 | 2026-08-10 |
| 4.5 | Выполнение тренировки (UI) | [x] | task/4.5-workout-run | 2026-08-10 |
| 5.1 | Агрегатор статистики | [x] | task/5.1-stats-aggregator | 2026-08-10 |
| 5.2 | Экран прогресса (UI) | [x] | task/5.2-progress-screen | 2026-08-10 |
| 5.3 | История тренировок (UI) | [x] | task/5.3-history-screen | 2026-08-11 |
| 6.1 | Архитектура генерации (LLM) | [x] | task/6.1-llm-contract | 2026-08-11 |
| 7.1 | Противопоказания: логика | [x] | task/7.1-contraindications-logic | 2026-08-11 |
| 7.2 | Замеры тела: репозиторий | [x] | task/7.2-body-metrics-repo | 2026-08-11 |
| 7.3 | Экран профиля (UI) | [x] | task/7.3-profile-screen | 2026-08-11 |
| 7.4 | Противопоказания (UI) | [x] | task/7.4-contraindications-screen | 2026-08-11 |
| 7.5 | Предупреждения в UI | [x] | task/7.5-warnings-ui | 2026-08-11 |
| 7.6 | Напоминания | [x] | task/7.6-reminders | 2026-08-11 |
| 7.7 | Синхронизация (экспорт/импорт) | [x] | task/7.7-sync | 2026-08-11 |
| 8.1 | Integration-тесты | [x] | task/8.1-integration-tests / PR #35, #36 | 2026-08-12 |
| 8.2 | Оптимизация | [x] | task/8.2-perf | |
| 8.3 | Финальная валидация и README | [x] | task/8.3-final | 2026-08-13 |
| 9.1 | Реактивность списков | [x] | task/9.1-data-refresh | |
| 9.2 | Крах при выборе анимации | [x] | task/9.2-exercise-form | |
| 9.3 | Валидность программ + попап | [x] | task/9.3-program-validation | |
| 9.4 | Удалённая программа в плане + тултип | [x] | task/9.4-week-plan | |
| 9.5 | Селект «Динамика» | [x] | task/9.5-profile-dropdown | |
| 9.6 | Стартовый набор упражнений | [x] | task/9.6-exercise-seed | |
| 10.1 | Детализация мышц | [x] | task/10.1-muscles-detail | |
| 10.2 | Чекбокс «больше не показывать» | [x] | task/10.2-warning-dismiss | |
| 10.3 | Углублённая статистика | [x] | task/10.3-deep-stats | PR #46 |
| 10.4 | UX конструктора программ | [x] | task/10.4-program-ux | PR #47 |
| 10.5 | Тип «свой вес» | [x] | task/10.5-bodyweight | |
| 10.6 | Название, быстрый старт, README | [x] | task/10.6-app-name-quickstart | PR #48 |
| 11.1 | Маркировка обязательных полей (звёздочка) | [x] | task/11.1-required-marker | PR #49 |
| 11.2 | Чекбокс «скрыть необязательные поля» | [x] | task/11.2-hide-optional | PR #50 |
| 11.3 | Изображения упражнений | [x] | task/11.3-exercise-image | PR #51 |
| 11.4 | Стартовый экран с анимацией | [x] | task/11.4-splash | PR #52 |
| 11.5 | Фильтр по мышцам в конструкторе | [x] | task/11.5-muscle-filter | PR #53 |
| 11.6 | Красивая анимация мышц | [x] | task/11.6-muscle-animation | PR #54 |
| 11.7 | Ошибка создания тренировки в debug | [x] | task/11.7-workout-debug | PR #55 |
| 11.8 | Удаление базовых упражнений | [x] | task/11.8-exercise-delete | PR #56 |
| 11.9 | Экран настроек + перенос синхронизации | [x] | task/11.9-settings | PR #57 |
| 11.10 | Переключение темы | [x] | task/11.10-theme-toggle | PR #58 |
| 11.11 | Проверка отступов | [x] | task/11.11-spacing | PR #59 |
| 11.12 | Экспорт JSON для LLM | [x] | task/11.12-llm-export | PR #60 |
| 11.13 | Перенос кнопки быстрого старта | [x] | task/11.13-quickstart-move | PR #61 |
| 11.14 | Домашний экран | [x] | task/11.14-home / PR #62 | 2026-08-15 |
| 12.1 | FAB быстрого старта на главной | [x] | task/12.1-home-fab | 2026-08-16 |
| 12.2 | Контраст бейджа «Перенесено» | [x] | task/12.2-rescheduled-color | 2026-08-16 |
| 12.3 | Импорт/экспорт БД без исключений в debug | [x] | task/12.3-sync-errors | 2026-08-16 |
| 12.4 | Иконка настроек в AppBar профиля | [x] | task/12.4-profile-settings | 2026-08-16 |
| 12.5 | Прогресс-метрики по выполненным упражнениям | [x] | task/12.5-progress-performed | 2026-08-16 |
| 12.6 | Выбор миниатюры/анимации без ложной ошибки | [x] | task/12.6-media-pick | 2026-08-16 |
| 12.7 | Обязательные мышцы в форме упражнения | [x] | task/12.7-muscles-required | 2026-08-16 |
| 12.8 | Кнопка «следующий день» в конструкторе | [x] | task/12.8-builder-next-day | 2026-08-16 |
| 12.9 | Фильтр по категориям в выборе упражнения | [x] | task/12.9-type-filter | 2026-08-16 |
| 13.1 | Ошибка валидации не очищается при вводе | [x] | task/13.1-field-error-clear | 2026-08-16 |
| 13.2 | Кнопка «Сохранить» вместо перехода к следующему дню | [x] | task/13.2-builder-next-day-fix | 2026-08-16 |
| 13.3 | Фокус на «Описание» после возврата из дня | [x] | task/13.3-description-focus | 2026-08-16 |
| 13.4 | Счётчик удержания планки малозаметен | [x] | task/13.4-hold-counter | 2026-08-16 |
| 13.5 | Перенос значения счётчика в результат планки | [x] | task/13.5-hold-carryover | 2026-08-16 |
| 13.6 | «1 минута» вместо «1.5» у планки | [x] | task/13.6-plank-minutes | 2026-08-16 |
| 13.7 | Нижняя навигация: только иконки на узком экране | [x] | task/13.7-nav-icons-only | 2026-08-16 |
| 13.8 | Таймер планки не стартует без «Начать» | [x] | task/13.8-plank-start-gate | 2026-08-16 |
| 13.9 | «Убрать изображение» → иконка удаления | [x] | task/13.9-remove-icon | 2026-08-16 |
| 13.10 | Изображение подогнано под блок | [x] | task/13.10-media-fixed-box | 2026-08-16 |
| 13.11 | Не сбрасывать активность программы при правке | [x] | task/13.11-active-program | 2026-08-16 |
| 13.12 | Разминка перед тренировочным днём | [x] | task/13.12-warmup | 2026-08-17 |
| 13.13 | Импорт БД: ограничение и проверка формата | [x] | task/13.13-import-validation | 2026-08-17 |
| 13.14 | Экспорт БД в файловую систему | [x] | task/13.14-export-save | 2026-08-17 |
| 13.15 | Переключение темы не уводит на главный экран | [x] | task/13.15-theme-router | 2026-08-16 |
| 13.16 | Звук окончания таймеров + выбор звука | [x] | task/13.16-timer-sound | 2026-08-17 |
| 13.17 | Логика таймера планки (счёт от нуля, цель) | [x] | task/13.17-plank-timer | 2026-08-16 |
| 13.18 | Ограничение форматов миниатюр/анимаций | [x] | task/13.18-media-formats | 2026-08-17 |
| 14.1 | Публикация релизов по тегу + ссылка в README | [x] | task/14.1-release-workflow | 2026-08-17 |
| 14.2 | Лицензия (некоммерческая) | [x] | task/14.2-license | 2026-08-17 |
| 14.3 | Версия в настройках + проверка обновления | [x] | task/14.3-app-update | 2026-08-17 |
| 14.4 | История упражнения (из тренировки, 3 дня, последняя) | [x] | task/14.4-workout-exercise-history | 2026-08-17 |
| 14.5 | Чекбоксы «фикс. вес» и «по сторонам» (схема v7) | [x] | task/14.5-exercise-flags | 2026-08-17 |
| 14.6 | Автозаполнение фиксированного веса | [x] | task/14.6-fixed-weight | 2026-08-17 |
| 14.7 | Выполнение по сторонам с доп. таймером | [x] | task/14.7-per-side | 2026-08-17 |
| 14.8 | Таймер отдыха по времени старта (сон) | [x] | task/14.8-rest-wallclock | 2026-08-17 |
| 14.9 | Кнопка «Завершить и сохранить» в диалоге | [x] | task/14.9-finish-early | 2026-08-17 |
| 14.10 | Навигация: убрать «Программы» на узких + кнопка на главной | [x] | task/14.10-nav-programs | 2026-08-17 |
| 15.1 | Перенос поля разминки из модалки в экран программы | [x] | task/15-bugfixes | |
| 15.2 | Ограничение формата звука при выборе файла | [x] | task/15-bugfixes | |
| 15.3 | Остановка звука через 5 сек + при старте след. упражнения | [x] | task/15-bugfixes | |
| 15.4 | Исправление INTERNET permission (socket exception) | [x] | task/15-bugfixes | |
| 15.5 | Исправление экспорта БД в файлы (invalid arguments) | [x] | task/15-bugfixes | |
| 15.6 | Запуск непривязанных дней (dayOfWeek == null) | [x] | task/15-bugfixes | |
| 15.7 | Чекбокс «Только свои упражнения» | [x] | task/15-bugfixes | |
| 15.8 | Кнопка «Следующий день» вместо «Сохранить» | [x] | task/15-bugfixes | |
| 15.9 | Стилизация графиков — сетка и границы | [x] | task/15-bugfixes | |
| 15.10 | Профиль — динамика: интервалы и overflow | [x] | task/15-bugfixes | |
| 15.11 | Проверка ссылки на скачивание APK в README | [x] | task/15-bugfixes | |
| 15.12 | Профиль — график: интервал + линия при одном замере | [x] | task/15.12-profile-chart-fix | 2026-08-17 |
| 15.13 | Главный экран — кнопка «Перейти к программам» | [x] | task/15.13-home-programs-btn | 2026-08-17 |
| 15.14 | Разминка — wakelock + центрирование текста | [x] | task/15.14-warmup-wakelock | 2026-08-17 |
| 15.15 | Прогресс — дедуп perSide результатов | [x] | task/15.15-per-side-dedup | 2026-08-17 |
| 15.16 | Настройки — SAF сохранение на Android | [x] | task/15.16-saf-save | 2026-08-17 |
| 15.17 | Попап выхода — отступы между кнопками | [x] | task/15.17-exit-popup-spacing | 2026-08-17 |
| 15.18 | Блокировать сон при работе таймера | [x] | task/15.18-wakelock-timer | 2026-08-19 |
| 15.19 | Настройки — ошибка импорта БД (is_stat_launcher) | [x] | task/15.19-import-icon-fix | 2026-08-19 |
| 15.20 | Перенести иконку настроек на главный экран | [x] | task/15.20-settings-icon-home | 2026-08-19 |
| 15.21 | Мышцы — группировка рук/спины/ног + подгруппы груди | [x] | task/15.21-muscle-groups | 2026-08-19 |
| 15.22 | Прогресс — пустое состояние по центру по вертикали | [x] | task/15.22-progress-empty-center | 2026-08-19 |
| 15.23 | Звуки — timer.mp3 + notification.mp3 | [x] | task/15.23-sounds | 2026-08-19 |
| 15.31 | Wake lock — проброс ошибок | [x] | task/15.31-wakelock-fix | 2026-08-19 |
| 15.32 | Звук таймера — AssetSource путь | [x] | task/15.32-sound-asset-fix | 2026-08-19 |
| 15.33 | Звук в фоне — AudioContext + foreground | [x] | task/15.33-sound-background | 2026-08-19 |
| 15.34 | Импорт БД — монохромная иконка | [x] | task/15.34-import-icon-fix | 2026-08-19 |
| 15.35 | Главный экран — обновление после программы | [x] | task/15.35-home-refresh | 2026-08-19 |
| 15.36 | Версия тега в APK | [x] | task/15.36-release-version | 2026-08-19 |
| 15.37 | Напоминания — канал + permission | [x] | task/15.37-reminder-fix | 2026-08-19 |
| 15.38 | Подсветка мышц — parent/child | [x] | task/15.38-muscle-highlight | 2026-08-19 |
| 15.30 | Видео-заставка на экране загрузки | [x] | task/15.30-video-splash | 2026-08-19 |
| 16.1 | Конструктор: список мышц с % + незадействованные | [x] | task/16.1-muscle-panel | 2026-08-21 |
| 16.2 | Настройки: кнопка «Прослушать» звук | [x] | task/16.2-sound-listen | 2026-08-21 |
| 16.3 | Прогресс: подписи оси Y планки (единицы) | [x] | task/16.3-plank-yaxis | 2026-08-21 |
| 16.4 | Прогресс: карточка «Тренировок» → история | [x] | task/16.4-workout-count-tap | 2026-08-21 |
| 16.5 | Финиш тренировки: упражнения + мышцы | [x] | task/16.5-finished-exercises | 2026-08-21 |
| 16.6 | Wakelock warning: OK закрывает баннер | [x] | task/16.6-wakelock-dismiss | 2026-08-21 |
| 16.7 | Заставка: 2с задержка перед видео | [x] | task/16.7-splash-delay | 2026-08-21 |
| 17.1 | Конструктор: агрегированная панель мышц с детьми | [x] | task/17.1-muscle-panel / PR #121 | 2026-08-22 |
| 17.2 | Автозаполнение разминки из первого дня | [x] | task/17.1-muscle-panel / PR #121 | 2026-08-22 |
| 17.3 | Play/Stop toggle для звука | [x] | task/17.1-muscle-panel / PR #121 | 2026-08-22 |
| 17.4 | Кнопка «Поделиться» — OutlinedButton | [x] | task/17.1-muscle-panel / PR #121 | 2026-08-22 |
| 17.5 | Изображения — blob в БД (схема v8) | [x] | task/17.5-image-blob / PR #123 | 2026-08-23 |
| 17.6 | Валидация формата/размера (5 МБ) + info-текст | [x] | task/17.6-17.10-ui-fixes / PR #124 | 2026-08-23 |
| 17.7 | Переименовать анимация → изображение | [x] | task/17.6-17.10-ui-fixes / PR #124 | 2026-08-23 |
| 17.8 | Убрать «Проверка обновлений через GitHub» | [x] | task/17.6-17.10-ui-fixes / PR #124 | 2026-08-23 |
| 17.9 | Заставка 2с → 3с | [x] | task/17.6-17.10-ui-fixes / PR #124 | 2026-08-23 |
| 17.10 | Прогресс: ось Y bodyweight → повт | [x] | task/17.6-17.10-ui-fixes / PR #124 | 2026-08-23 |
| 18.1 | Понятный текст ошибки проверки версии | [x] | task/18.1-update-check-error | 2026-08-23 |
| 18.2 | Убрать панель мышц из конструктора дня | [x] | task/18.1-update-check-error | 2026-08-23 |
| 18.3 | Не задействованные мышцы — только группы | [x] | task/18.1-update-check-error | 2026-08-23 |
| 18.4 | Реордер дней — содержимое следует за днём | [x] | task/18.1-update-check-error | 2026-08-23 |
| 18.5 | Конфликт обновления программы | [x] | task/18.1-update-check-error | 2026-08-23 |
| 18.6 | Импорт БД — совместимость схемы | [x] | task/18.1-update-check-error | 2026-08-23 |
| 19.6 | Исправить загрузку мышц в конструкторе программ | [x] | task/19.6-muscle-panel-bug / PR #126 | 2026-08-24 |
| 19.1 | Исправить PlatformException при выборе изображения | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.2 | Диалог перезапуска после импорта — без барьерного закрытия | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.3 | Сохранение времени разминки при импорте | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.4 | Предупреждение при импорте БД | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.5 | Fallback на blob при отсутствии файла изображения | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.7 | Автосохранение при отмене тренировки | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.8 | Расстояние между кнопками при узком экране (диалог выхода) | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.9 | Копирование JSON программы иконкой в конструкторе | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.10 | Переупорядочивание нижней навигации на узких экранах + увеличение иконки «Главная» | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.11 | Автосохранение тренировки при завершении, убрать кнопку «Сохранить» | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.12 | Мульти-активные программы | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.13 | Подробная информация об упражнении во время тренировки (подходы/повторы/вес) | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |
| 19.14 | Привязка дня недели в конструкторе дня программы | [x] | task/19.1-19.5-import-fixes | 2026-08-24 |

## Порядок выполнения

1. **Критический путь:** 0.1 → 0.2 → 1.1 → 1.2 → 1.3 → 2.1 → 2.2 → 2.3–2.5 → 3.1–3.5 → 4.1–4.5 → 5.1–5.3 → 8 → **9.1** → **9.2–9.6** → **10.1–10.6** → **11.1–11.14** → **12.1–12.9** → **13.1–13.18** → **14.1–14.10**. Внутри этапа 11: 11.2 → 11.14 (схема v4→v5), 11.9 → 11.10, 11.13 → 11.14. Внутри этапа 13: 13.17 → 13.4/13.5/13.8, 13.12 → 13.16, 13.15 — в начале этапа. Внутри этапа 14: 14.1 → 14.3, 14.5 → 14.6/14.7, 14.2 — в начале этапа.
2. **Параллельные ветки:** 1.4 (после 1.1), 2.6 (после 1.1+0.3), 7.1 (после 1.3+2.1), 6.1 (после 1.1) — не блокируют основную цепочку.
3. **Зависимости этапа 9–10:** 9.2–9.6 опираются на 9.1 (реактивность); 10.3 зависит от 10.5; 10.6 минимально пересекается с 9.4/10.4 по экрану плана.
3. Ветки/PR строго по зависимостям: PR задачи N открывается после мержа всех её зависимостей.
4. **Этап 16:** задачи 16.1–16.7 независимы, выполняются по порядку.
5. **Этап 17:** 17.1–17.5 выполнены; 17.5 → 17.6; 17.7–17.10 независимы.
6. **Этап 18:** задачи 18.1–18.6 независимы, выполняются по порядку.
7. **Этап 19:** 19.6, 19.1–19.5, 19.7–19.14 выполнены.
